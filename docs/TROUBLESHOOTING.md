# 故障排查

- 页面打不开：检查端口 `4327` 是否监听，并查看运行数据目录里的 `dashboard-server.err.log`。
- 页面为空：确认项目根目录存在 `01-素材库`、`02-模板库`、`03-成品库`。
- 路径仍指向旧目录：检查是否设置了旧的 `TEAMBUILDING_ROOT` 环境变量。
- 提示词不可用：检查 `TEAMBUILDING_SKILL_ROOT` 或默认的团建提示词资料目录。
- 状态异常：备份后删除运行数据目录中的 `state.json`，服务会自动重建。
