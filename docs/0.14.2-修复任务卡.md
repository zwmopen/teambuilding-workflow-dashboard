# 0.14.2 任务卡：现存故障全修 + 真实 Plus 闭环实测

> 创建：2026-08-01
> 来源：用户 2026-08-01 上午反馈（TRAE 会话）
> 接手优先级：仅修 5 个现存故障，不写新功能
> 状态：本任务卡已写入；执行由 CodeX / 后续 AI 续做
> 真源文件：`docs/HANDOFF.md`（项目根目录）+ 本文件

---

## 0. 用户反馈原话（保留证据链）

1. 悬浮助手："换成一只可爱小猫咪吧，会轻微跳动的小猫咪可爱的。"
2. 自动闭环卡住："目前全都卡在了，发了图片，获取计划，之后就卡住了，不了了之了。"
3. 下载/打包按钮崩坏："不能下载，下载失败，反正都是问题。"
4. 提示词输入点击不动："那个对话框那个提示词，点击不动……感觉也不稳定啊，不能用。"
5. 网页点击费劲："这个网页点击很费劲很费劲，有时候不反应。"
6. 卡在 "正在准备 GPT 网页"："他还在检测异常自己停了，然后右侧的浏览器显示正在准备 GPT 网页，准备个毛线啊，这个不是一开始就加载好了吗……一直加载。"
7. 下载目录只能粘贴："这个目录是不是应该可以设置啊，你所有的地址设置不是应该有一个选择吗，好像只能粘贴，不行。"
8. 流程设置缺失："手动准备，自动闭环……怎么不能设置呢，应该有一个设置界面，就在生产模式切换或者直接在右上角设置内。最好。"
9. 插件市场缺 weFlow："我觉得插件市场需要增加一个 weFlow 的下载地址。"
10. 微信聊天记录："我本地其实上已经有了。转化界面其实有一些数据，但不是那么完整，没有加工过，有些很粗糙……到时候专门针对转化类给他调几个技能。"

---

## 1. 本轮唯一目标

**5 个现存故障全部修完 + 真实 Plus 闭环 2 套验收**。

| # | 故障 | 修法要点 | 涉及文件 |
|---|---|---|---|
| F1 | 下载/打包按钮崩坏 | server.js 抓真实异常并 4xx/5xx；app.js 按钮 disable/enable 状态机 | `src/server.js` `src/public/app.js` |
| F2 | 对话框提示词输入点击无反应 | 改用原生 `<textarea>`，不再用浮层 DIV | `src/public/app.js` `src/desktop/main.js` |
| F3 | ChatGPT 网页点击费劲/不响应 | 扩展侧 shadowRoot + 显式 pointer-events | `D:\AICode\工具开发\projects\teambuilding-gpt-production-extension\src\` |
| F4 | 右侧 webview 长期 "正在准备 GPT 网页" | main.js 服务启动失败退避 + 状态栏报错；webview 与 service-ready 信号绑定 | `src/desktop/main.js` |
| F5 | 下载目录无文件夹选择器 | 抽出统一 `<FolderPicker>` 组件 | `src/public/app.js` `src/server.js` |

---

## 2. 待办但本轮不做（写入后续版本）

- **小猫咪悬浮助手**（用户原话：换成可爱小猫咪，会轻微跳动）
  - 状态：默认气泡=状态/日志/任务进度展示
  - 点击助手本体=打开对话框对话（覆盖式）
  - 关闭时长选项：1 分钟 / 5 分钟 / 1 小时
  - 排到 0.14.3

- **手动/自动/分步流程设置界面**
  - 挂在生产模式切换或右上角
  - 排到 0.14.3

- **插件市场增加 weFlow 入口**
  - 排到 0.14.3

- **微信聊天记录转化技能**
  - 先调研本地 weFlow 导出目录
  - 写 `D:\AICode\AI\data\01-团建策划-江湖有旅人\02-技能\微信聊天记录\SKILL.md`
  - 挂在转化助手内调用
  - 排到 0.14.3 之后

---

## 3. 真实 Plus 闭环实测（每套 1 个母版 × 1 套素材）

准备：5 套复制素材（27 张图 + 5 份 TXT），原素材未动。本轮选 **2 套**做验收。

每套要跑通的链路：
```
发图 → 识别计划 → 扣 1 → 等 3-11 分钟出图
     → 批量下载 → 输入提示词取文案
     → 复制文案 → 写入本地 TXT
     → 把下载的图和文案打包成 ZIP
     → 归档素材
```

防火墙授权（用户决定）：
- 只测 GPT 生产 → 取消
- 还要测手机局域网 → 允许

---

## 4. 限制（Codex 不能违反）

- 不写新功能
- 不动 GPT 登录分区（`persist:teambuilding-gpt-production-*`）
- 不删除或覆盖 Electron `userData`
- 不把残缺文本/TXT 冒充成功
- 不向界面回传明文密钥
- 0.14.0 / 0.14.1 仍保留为回退包
- MiniMax 401 必须如实写
- 不写"文案链路已通"等不实陈述

---

## 5. 验收产物

- 0.14.2 便携包 + SHA-256
- 真实 2 套素材输出目录（PNG + TXT + ZIP + 质量报告）
- 整链截图 10+ 张
- 测试报告：自动化 ≥ 138/138；真实 2 套 Plus 单/多窗口全过
- 桌面快捷方式切换记录
- 0.14.0 / 0.14.1 SHA-256 校验通过
- HANDOFF.md / CHANGELOG.md 同步到 0.14.2

---

## 6. 复检清单（任务结束前必须走完）

- [ ] 5 个故障全部关闭
- [ ] 4327 服务自动起来并稳定可连
- [ ] `node --check public/app.js` `node --check server.js` 通过
- [ ] 自动化测试 ≥ 138/138
- [ ] 真实 2 套 Plus 跑通
- [ ] 0.14.0 / 0.14.1 SHA-256 校验通过
- [ ] MiniMax 401 状态如实写
- [ ] 桌面快捷方式指向 0.14.2
- [ ] 0.14.2 段写进 HANDOFF.md
- [ ] CHANGELOG.md 同步
