# 交接

> 开发和交付前必须同时读取 `docs/REGRESSION-CHECKLIST.md`。历史问题是永久回归门禁，不能只修当前现象后交付。

## 0.14.7 当前状态（2026-08-02）

- 自动与手动打包只有一个真源：网页回复按钮和自动状态机都调用扩展 `packageDownloadedReply`，随后进入工作台 `/api/extension/work-package`、本地查重和成品库落盘；手动与自动只在触发方式上不同。
- 工作台会把当前页面配置的下载暂存目录和成品目录作为本批工作包的确定配置，并用 UTF-8 `GPT作品记录.json` 按批次号恢复权威成品路径，避免 PowerShell 中文输出编码造成假失败。
- 模板区支持本地文件夹与在线链接两种来源。在线链接真源为模板库根目录 `链接模板.txt`；格式为 `名称 + ChatGPT c/share URL + 可选 accountId`，界面增删与文件内容双向一致。
- 浏览器标签顺序和名称写入持久浏览器档案。切换标签只改变当前可见网页；运行任务继续绑定启动时的账号。新增多账号全天模式，每个可见浏览器是独立串行 worker，一次只接一帖。
- 在线模板在多账号模式下可分配给不同 worker；每个任务先打开模板链接。分享页若提供“继续此对话”，扩展会转为可编辑会话再上传，否则安全失败。
- 素材与模板行现在都有同一视觉和语义的“上传”按钮。本地帖子/模板只把当前文件夹附件注入当前 GPT 输入框，不自动发送；在线模板则打开所保存的会话链接。
- 真实双素材串行验收已完成：第一套先完整完成 7 图、TXT、打包和归档，第二套随后才上传。第二套曾因官网把 7 张成品折叠成外层 `P1…P7` 沙盒文件按钮而误报 3 张；扩展改为在当前回复的 `conversation-turn` 外层读取文件组后，从同一检查点恢复，未重复上传或发送 `1`，最终 7/7 图片、TXT、作品记录和归档全部成功。
- 第二套真实成品：`D:\AICode\项目推进\projects\江湖有旅人\主项目\成品库（GPT+本地脚本制作）\20260802_015816_可以，给你一版和这套 「杭州湖畔农庄团建」7页图直接配套的小红书成品文案，整体走“真实体验＋项目丰富＋精准获客”的方向，（可以，给你一版和这套 「杭州湖畔农庄团建」7页图直接配套的小红书成品文案，整体走“真实体验＋项目丰富＋精准获客”的方向，不硬塞价格。）`。
- 当前自动化门禁：工作台 `177/177`、扩展 `24/24` 和扩展语法检查均通过；GPT 登录分区和两个既有会话在开发版重启后保持。

## 0.14.6 当前状态（2026-08-01）

- 已定位真实生产误停：旧扩展在第一张图后 8 秒静默就收口，实际同一助手回复最终含 6 个唯一图片地址，网页按钮显示 `下载本组 0/6`。
- 扩展 0.2.18 改用“当前助手回复 + 原生回复完成动作 + 唯一图片组”联合检测；网页证据不足时返回 `IMAGE_COUNT_UNCERTAIN`，工作台保留当前队列索引并暂停，不把它当额度触顶。
- 当前队列仍暂停，后续素材没有送入 GPT。发布后应从当前已有 6 张回复恢复下载、文案和打包，不重复上传或发送 `1`。

## 0.14.2 持续开发稳定节点：真实 Plus 自动闭环 + 全局小猫助手（2026-08-01）

### 当前使用方式

- 用户长期使用开发版热更新，不以便携正式包作为每次开发的前置条件。稳定节点仍需提交并推送源码；便携包只在用户需要固定交付或回退点时构建。
- Electron 原生层改动需要重启开发进程；普通前端、样式和服务端改动可直接刷新验证。重启不得删除 `C:\Users\z\AppData\Roaming\Electron`，浏览器档案 ID 与 `persist:teambuilding-gpt-production-*` 分区保持不变。
- 当前运行地址 `http://127.0.0.1:4327/`，主源码位于 `src/`，独立 GPT 网页执行扩展仍以 `D:\AICode\工具开发\projects\teambuilding-gpt-production-extension` 为唯一真源，工作台内只保留发布同步副本。
- 已确认下一项生产设计：模板区提供“链接模板／文件夹模板”切换。链接模板保存名称、ChatGPT 会话 URL 与可选浏览器绑定，直接复用已调教会话；文件夹模板上传真实母版图片和提示词初始化新会话。链接模板以模板库根目录的 `链接模板.txt` 为真源，界面手动维护与用户直接编辑 TXT 必须双向同步。实现和验收标准见 `docs/REGRESSION-CHECKLIST.md` 的 `TEMPLATE-001`。
- 已确认生产模式需要完整的可编辑流程：右键模式名称或从生产设置进入，按触发条件编辑上传、确认、下载、请求文案、复制、打包和归档阶段；配置写入 `app-settings.json`，启动任务时冻结为批次快照。见 `WORKFLOW-001`。

### 已实现并验证

- 单窗口自动闭环已真实跑通：上传素材、等待迁移计划、只发送一次 `1`、等待图片、下载本轮图片、请求小红书文案、写入 TXT、查重打包、完整成功后归档测试副本。
- 两套成功样本共生成 9 张图片和 2 份完整文案；平均单套约 8分10秒。另有一套只生成 3 张，低于安全线 4 张时正确跳过，未补图、未打包、未归档。
- 生产历史按完成时间排序，显示计划/出图/文案/总耗时，可直接打开成品目录；队列运行后冻结本批选择，暂停按钮切换为继续，跳过只影响当前素材。
- GPT 页面首次加载后只隐藏/显示；弹窗、生产记录或小猫对话关闭后强制恢复原生视图，修复“网页仍在但视图白屏”。
- 下载本组、下载并打包、复制文案、下载 TXT 由独立扩展绑定当前回复；用户上传消息不显示下载按钮。自动和手动模式复用同一底层能力。
- 全局助手使用透明原生子窗口，能盖在 GPT 原生视图上方；状态气泡与 AI 对话分层，支持拖动、滚动日志和右键静音 1/5/60 分钟。小猫气泡、菜单、文字、滚动条和阴影随四套主题切换，浅色不再显示黑色菜单。
- 主题为拟态浅色、玻璃浅色、拟态深色、玻璃深色四套；GPT 原生网页同步对应色板和正文底色。GPT 外层重复卡片、阴影、白边及 12px 假内缩已移除，只留一层 16px 圆角。
- 自动生产页禁止整页滚动，左侧素材/模板库内部独立滚动，右侧 GPT 原生视图保持同一容器坐标；已实测左侧滚动 40px 时 GPT 容器纵坐标不变，解决“底层页面移动、网页悬浮不动”的视觉割裂。
- 自动测试：工作台 149/149；独立扩展 14/14，扩展 `npm run check` 通过。

### 真实验收产物

- 报告：`D:\AICode\运行数据\江湖有旅人\图文生产控制台\验收\0.14.2\accept-0142-20260801-final\报告\生产验收报告.md`
- 女神节成品：`D:\AICode\项目推进\projects\江湖有旅人\主项目\成品库（GPT+本地脚本制作）\_测试验收\0.14.2\accept-0142-20260801-1740-imagefix\20260801_173257_3.8女神节福利通知｜插花＋礼盒＋下午半天假`
- 朋友圈成品：`D:\AICode\项目推进\projects\江湖有旅人\主项目\成品库（GPT+本地脚本制作）\_测试验收\0.14.2\accept-0142-20260801-second-final\20260801_181324_公司团建朋友圈文案合集｜16条九宫格配文直接参考`

### 当前边界与下一步

- ChatGPT 官网 DOM、额度和验证提示可能变化；遇到验证、登录失效或无法判断的页面状态必须暂停，不绕过。
- 本地滚动三小时账本是估算，不是官方实时额度。MiniMax 文案接口当前机器仍返回 HTTP 401，不能宣称真实连通。
- 多窗口调度结构已经存在，但本轮只完成单窗口真实闭环；下一阶段应先跑 3 窗口小批量验收，再决定是否默认开放多窗口。
- 继续优先优化“选择素材 → 自动产出成品”这条主链，非主链 UI 功能按真实使用反馈增量调整。

## 0.14.1 正式版候选：生图与文案接口解耦（2026-08-01）

- 设置页已将生图和文案拆成独立的平台、地址、模型和密钥；文案默认 `minimax / https://api.minimaxi.com/v1 / MiniMax-M2.7`。
- 服务端新增独立的文案配置、凭据读取、模型测试和 OpenAI 兼容生成通道。生产标题、正文和中控助手均读取已保存文案配置，不再绑定当前生图平台。
- 兼容旧运行数据：独立文案密钥为空时，先复用同平台旧凭据；MiniMax 无可用凭据时可降级到已有本地兼容文本通道。密钥不回传界面、不进入普通 JSON 导出、坚果云或仓库。
- 工作台主生产区的文案模型列表通过 `/api/text-api/test` 单独刷新；设置页保存和测试会同时但分别调用图片、文案路由。切换图片平台不会改写文案平台。
- 2026-08-01 验证：`node --check public/app.js`、`node --check server.js` 通过；聚焦测试 43/43、完整测试 138/138 通过；4337 开发页确认 ByteCat/MiniMax 文案切换会更新对应地址和模型，页面无控制台错误。
- 当前本机凭据事实：本地图片兼容密钥和 ByteCat 图片密钥存在；没有可用的独立 MiniMax/本地/ByteCat 文案密钥。正式 0.14.1 调用 MiniMax `/models` 返回 HTTP 401，不得宣称 MiniMax 文案真实连接已通过。
- 0.14.1 便携包已构建并真实启动：96,165,146 字节，SHA-256 `099B8E073974799740145DE386E1E8406A60F9C7573C6B41133412A8BED03965`；4327 API 返回 `version=0.14.1`、`channel=便携版`、`desktop=true`。
- “测试自动生产”已从原素材库复制 5 套副本，共 27 张图和 5 份 TXT，原素材未移动。首次启动出现 Windows 防火墙公共/专用网络访问授权窗口；未经用户确认不得点击允许。确认后继续真实 Plus 单窗口、多窗口、暂停/继续和重启恢复验收。0.14.0 必须保留为回退包。

## 0.14.0 正式版：GPT 多窗口自动生产与后台运行（2026-07-31）

- 正式版关闭窗口默认退到托盘，只有托盘“彻底退出”才结束本地服务和 GPT 网页；有生产任务时必须二次确认。
- GPT 登录状态位于 Electron 固定持久化分区。浏览器档案 ID 和分区名称一旦发布不得重命名；升级时不得删除 Electron `userData`。
- 浏览器档案元数据由 `src/desktop/main.js` 写入 `gpt-browser-profiles.json`。隐藏或移除记录不会清登录数据；危险清理接口必须由用户明确确认。
- 自动任务保存批次、窗口、阶段和暂停状态；重启后运行中任务按暂停恢复。无法从会话和本地结果可靠判断时，不允许重复发送。
- 多窗口轮询只在等待 GPT 时切换其他窗口，不并发操作同一浏览器。验证码、登录失效、网页结构无法识别或真实限流必须暂停。
- `work-package` 仍是唯一正式打包入口。扩展必须传递本轮批次号、图片数量、TXT、下载暂存和成品目录；服务端再次校验后才归档素材。
- 生产默认路径：`D:\Download` 与 `D:\AICode\项目推进\projects\江湖有旅人\主项目\成品库（GPT+本地脚本制作）`。
- 发布前先运行 `scripts/sync-gpt-extension.ps1`，再跑工作台和扩展测试，最后构建便携版。0.13.0 保留为回退包。

## 0.13.0 正式版：内置 GPT 自动生产调度器（2026-07-30）

- 文件树已改为“父目录三态选择 + 帖子单位 + 附件懒加载”。素材与模板使用相同交互，可同时展开多个父目录；图片和 TXT 只预览，不单独勾选。
- 任务数按素材和模板的笛卡尔积计算。选择模板时按模板建立会话，同一模板会话串行处理本批素材；不选模板则直接沿用当前 GPT 会话。
- 扩展 0.2.11 是网页执行唯一真源：附件上传、原生发送、计划检测、`1` 确认、图片检测、文案请求、精确批次下载与打包均复用同一实现。工作台不另写第二套 DOM 自动化。
- 自动任务按阶段持久化并支持断点重试。完整成功后才归档素材；失败任务、缺图、缺 TXT 或打包错误不会移动原目录。
- 账号滚动三小时账本、等待队列、预计恢复时间和机器人提醒已经接入；默认 80 张上传、50 张生成，可按账号修改。账本不是 ChatGPT 官方实时额度。
- 普通设置备份不包含网页登录状态。本机登录恢复点通过重启前/后处理完整浏览器分区，解决 Windows 正在使用 Cookie 数据库时无法复制的问题；恢复点仅允许同一台可信 Windows 设备使用。
- 2026-07-30 自动验收：工作台 130/130、扩展 9/9 测试通过，全部受影响 JavaScript 通过语法检查；隔离桌面烟测确认 ChatGPT 与工作台各只有一个页面，登录恢复点包含 137 个文件且待办标记已清理。
- 0.13.0 重启后的独立 4338 服务完成非破坏性工作包预演，返回 `mode=workbench-direct`、`fallback=false`，中文 PowerShell 输出可正常解析；预演命中已有图片组时正确返回 `PREVIEW_DUPLICATE`，未创建或移动文件。
- 2026-07-30 真实 Plus 验收：模板 3 个附件和素材 3 个附件真实进入官网；模型完成模板确认、迁移计划与自动 `1`；官网真实生成并下载 `chatgpt-workpkg-20260730-221356-l1f9-1-of-1.png`，尺寸 1086×1448、大小 2,145,936 字节。随后本地工作包真实创建，复制测试素材真实移动到素材库 `已上传 1 次`，原始素材未改动。
- 已知限制：同一真实会话的小红书文案连续只返回短句片段。这是官网当前回复内容本身，不是本地提取截断。扩展 0.2.11 会自动补问一次，仍不足 80 字就安全暂停，不写 TXT、不打包、不归档。为验证本地尾段而创建的工作包使用测试素材原 TXT 作为人工回退证据，因此不能把这一次描述为“全自动文案闭环已通过”。
- 真实验收成品目录：`D:\AICode\项目推进\projects\江湖有旅人\主项目\成品库（GPT+本地脚本制作）\20260730_222303_千岛湖团建两天一夜，秋季行程安排来啦！推荐喜欢接触大自然的团队参考。第一天，乘船到湖心小岛森屿湖营地，在湖光山色中玩拓展（多项目拼图总览封面 × 白底手写信息条 × 无白边黑描边大字项目拼图模板）`。
- 正式发布前发现自动生产素材父目录把未加载状态错误显示为 0。根因是懒加载接口已经返回 `countKnown`，前端却强制将未知值转为 0；服务端也没有复用现有全局索引。修复后首次打开直接显示后台索引的真实数量，未知值显示“…”；搜索状态下父级三态选择仍按整个目录计算。
- 正式桌面烟测：16 个素材父目录、1941 个帖子数量可见；两个父目录可同时展开，单独折叠不影响另一个；父目录全选、帖子行“发送”、模板展开、模板图片与 TXT 预览均可用。
- 升级前完整备份 `C:\Users\z\AppData\Roaming\teambuilding-workflow-dashboard` 到 `D:\AICode\运行数据\江湖有旅人\图文生产控制台\备份\GPT登录档案\pre-0.13.0-20260730-230744`，核对为 2409 个文件、371,863,617 字节。正式包继续使用 `persist:teambuilding-gpt-production-*` 和固定工作台分区，重启后真实 ChatGPT 会话仍有输入框并停留在原会话。
- 0.13.0 正式包已在 4327 启动，大小 96,157,448 字节，SHA-256 `D5C7CBACEBF546CC386E0585D72B1A85F31F9A2BEB670A6D654ED1A28F975F1E`；桌面快捷方式指向 `releases/0.13.0/团建工作台-0.13.0-便携版.exe`。

## 0.12.2 正式版候选：GPT 网页自动生产（2026-07-30）

- 侧栏入口统一为“内容生产（自动）”。一套母版对应左侧一个或多个素材文件夹，队列按素材文件夹串行执行，不再显示“做一套 / 做一批”。
- 每组自动执行真实附件上传、迁移计划检测、可选发送 `1`、新图片检测、文案请求与检测、精确批次下载、本地 TXT 和工作包校验；只有图片张数与 TXT 都符合预期才算完整打包成功。
- 每组生成独立 `YYYYMMDD-HHMMSS-xxxx` 批次号；下载名为 `chatgpt-workpkg-<batch>-<index>-of-<total>.<ext>`。服务端拒绝非法批次、错误张数、缺 TXT 和不存在的输出目录，不再扫描 `D:\Download` 中的其他图片。
- GPT 区支持后退、前进、刷新、首页和多账号标签。每个账号使用 `persist:teambuilding-gpt-production-<id>` 独立分区；账号的 Cookie、登录状态和浏览历史互不共享，源码和日志不保存凭据。
- 页面设置包括自动确认、自动复制文案、自动打包、失败暂停、生产间隔随机范围、单帖超时和每轮最大套数。随机范围只调节常规队列节奏；登录、平台验证和限流必须暂停。
- 2026-07-30 验收：121/121 项 Node 自动测试通过；精确批次预演在下载目录存在无关旧图时仍只打包指定 1 张；桌面隔离烟测确认 ChatGPT 登录页、扩展 0.2.4、账号标签、四个网页控制按钮和页面设置可用。
- 已知限制：隔离烟测账号未登录，不能把“登录后的真实附件、付费生成、文案和下载整链”写成已通过。正式使用前需用户在账号 1 登录一次，再以一组真实素材完成首套验收。
- 0.12.2 正式包已在 4327 启动，API 返回 `version=0.12.2`、`channel=便携版`；桌面快捷方式指向 `releases/0.12.2/团建工作台-0.12.2-便携版.exe`。
- 正式包大小 96,144,357 字节（91.69 MB），SHA-256 `42D6B01596788C67D19B7DEC2B398BECC2B974D3C654B2CC513DD6F17C7BC82F`。旧 0.12.0、0.12.1 发布目录已按用户要求移入回收站，发布目录只保留 0.12.2。

## 0.12.1 开发版：GPT 内置测试（2026-07-30）

- “内容制作（GPT内置测试）”已加入工作台侧栏，真实 GPT 使用持久 `WebContentsView`，与原工作台同窗显示。
- 已验证页面加载到 ChatGPT 登录页、独立扩展 0.2.3 加载、工作台 API 地址注入、任务进入扩展及错误回传；隔离测试分区未登录，登录后的原生附件成功和实际生成必须在用户首次登录后补一次人工验收。
- 一个母版可映射多个素材文件夹，按素材逐组串行；没有“做一套 / 做一批”控件。
- 旧 VBS、外部 Chrome/Edge 扩展继续保留为兼容和回退路径；用户确认打通后已构建 0.12.1 正式便携版并更新 4327 桌面入口。
- Node 自动化测试 119/119；扩展自动化测试 9/9；双方 JavaScript 语法检查通过。
- 0.12.1 便携包大小 96,137,980 字节（91.68 MB），SHA-256 `AFAF237BA25D29E140465C017B3D57F23F79337982542C3E37D2998D9630E0FB`；正式包隔离烟测确认内置扩展 0.2.3 已加载。

## 0.12.1 开发版：首图校准费用刹车（2026-07-30）

- 生产确认后不再直接跑完整批。系统只允许第一套作品的 P1 发起 1 次生图请求，成功后进入 `calibration-ready` 并暂停；用户明确点击“首图确认无误，继续生成剩余 N 张”后才恢复整套。
- 首图失败不会继续其他页，也不会自动重试付费生成；再次点击只重试 P1。完整续跑跳过已经落盘的 P1。
- `生产记录.json` 和任务记录新增请求审计：实际付费请求数、尝试次数、参考图数量、供应商 request id、响应状态/耗时和 usage（供应商返回时）。失败首图也保留尝试记录。
- ByteCat 多参考图仍先在本地合成一张无留白轻量参考板；校准阶段当前只使用“封面母版 + 对应素材”的 2 格参考板。对话框级隐藏编排无法从 API 复刻，质量目标是用首图人工闸门先验证，而不是承诺一次整套都与 ChatGPT 对话框相同。
- 2026-07-30 验收：服务端、前端和生图适配层语法检查通过；119/119 项自动化测试通过；4337 开发页无控制台错误、1280 宽度无横向溢出。测试全部使用模拟响应，没有再次调用真实付费生图接口。
- 本轮只更新源码开发版 0.12.1；4327 正式便携版、桌面正式快捷方式、GitHub Release 均保持 0.12.0，等待用户明确要求更新正式版。

## 0.12.0 正式版（2026-07-29）

- 2026-07-30 复核发现：四套 ByteCat 测试任务没有带入本地 `chatgpt-history.json` 中完整的「模板迁移3.6」规则；两次任务的额外提示词分别只有 55 字和 33 字，实际主要依赖 `production-recipes.js` 的压缩配方。模板文件夹内的 TXT 是参考帖子文案，不是母版规则。
- 视觉抽检确认多张内页出现母版禁止的亮白十字分隔线；中心白线启发式检测对 20 张成品中的 8 张发出高置信提醒。素材图自身带白线时，当前“一张母版内页＋一张素材页”的参考方式仍可能把 B 类素材排版带入成品。
- 当前 `production-quality.js` 只自动检查张数、可读性、3:4、分辨率、文件指纹重复及文案风险词；母版骨架、白边/白缝、完全换位、换人换物和 AI 味仍只是人工检查文字，不会阻断或自动重试。因此下述“技术质检 20/20 通过”不能解释为视觉母版验收通过，这四套仅可作为接口连通与成本测试，不可直接作为合格母版迁移成品。
- 2026-07-30 成本链路复核：正式套图任务逐页执行，每个成品页固定取 1 张对应母版页和 1 张对应素材页；ByteCat 通道先把两张图无留白合成 1 张参考板，再向 `/images/generations` 发起 `n=1`、1024×1536 的单图请求。两次 ByteCat 任务合计记录 20 张成功成品、4 份文案、0 个最终失败；没有证据表明正式任务曾在单页上传 5—6 张素材。
- 当前成本审计不完整：生图请求对 408/429/5xx/网络错误默认最多尝试 3 次，但任务记录只保存最终页面，不保存每次上游尝试、响应 request id、`usage`、平台扣费或是否发生可计费重试。因此 20 张成品不能直接等同于 20 次实际计费，也无法从本地日志复原约 5.98 元扣费明细。
- 后续生产架构应把“模板理解”和“逐页出图”分开：每套模板只用封面＋代表内页校准一次并沉淀结构化配方；每套素材只做一次多模态内容分析；逐页最多使用“对应模板角色＋对应素材页”生成内容，并优先由本地确定性排版器锁定拼图、标题、键帽和无白缝。正式批量前必须显示预计调用数和预算上限，并记录每次尝试、用量与费用；计费不透明时默认不自动重试生图。
- 内容生产改为持久化后台任务。任务、逐页结果、失败页、输出目录、接口/模型和确认计划写入运行数据，应用异常退出后显示“可继续”，已经存在的页面不会重复生成。
- 单页失败不再中止整套；支持继续缺页、停止任务、逐页耗时和最近日志。停止功能使用请求中断信号，不再必须等待一次最长生图请求自然超时。
- 恢复任务前会按“作品 + 页码”清理重复结果记录；质量检查也按页码去重，修复进度超过 100% 和实际 5 张被误判成 7 张的问题。
- 自动质量报告核对计划张数、文件可读性、3:4 比例、最低分辨率、完全重复图片、文案存在性和高风险短语；同时把每张成品与本次母版图、素材图做 SHA-256 指纹比对，原图复制件会直接判定失败。视觉风格不作为自动阻断条件，最终随机风格由使用者自行比较。
- ByteCat `gpt-image-2` 使用 `/images/generations` 的 JSON `images[].image_url` 参考图协议。母版和素材在本机合成无留白轻量参考板，避免网关不识别 multipart 模型、请求体断线以及参考板白边被模型复制。
- 本机 `gpt-image-2` 双参考图单张实测曾成功返回，但整套生产连续出现 HTTP 502；MiniMax 当前凭据被官方接口拒绝。三路结果不混用，失败平台不会使用 ByteCat 成品冒充。
- 生产右侧支持打开作品目录、生产记录和质量报告；每个作品输出独立 `P1…Px` PNG、`小红书文案.txt`、`出图计划.json`、`生产记录.json` 和 `质量报告.json/txt`。
- Windows 目录选择改为现代系统文件夹窗口；所有路径输入继续支持回车/失焦自动保存、自动扫描与立即读取。
- 坚果云支持 Windows 当前账户加密保存凭据、连接检测、小数据备份/恢复和方案目录增量备份。隐藏临时文件会跳过，单文件失败记录后继续，连续失败才停止。
- Node 自动化测试 115/115 通过。四套真实 AI 作品共 20 页，均为 1200×1600 PNG，技术质检 20/20 通过，文件指纹确认没有使用母版或素材原图冒充成品。
- 三路生图结果严格隔离：ByteCat `gpt-image-2` 完成四套作品；本机兼容接口单张连通曾成功，但整套生产连续返回 HTTP 502；MiniMax `image-01` 因当前官方密钥认证失败未出图。失败接口没有借用 ByteCat 成品冒充。
- 坚果云工作台小数据备份已成功上传 10 份本地记录。方案大文件清单已累计上传约 986.7MB；本轮续传被 WebDAV 403 拒绝，清单继续保留，界面会提示可能为云端上传流量/空间额度或目录写入权限，额度恢复后可继续。
- 正式便携版已完成 4327 启动、五个主界面、目录选择器、流量转化同层融合和 1180 像素紧凑布局烟测；旧发布包、旧桌面快捷方式和本轮烟测临时文件已移入回收站，业务素材、成品和运行数据未删除。
- 0.12.0 便携包大小 96,070,987 字节（91.62MB），SHA-256 为 `00CDBE9278A0A572729F268026687D32D450E02AD626F095D5391957EAF635D1`；桌面“团建工作台”快捷方式已核对并指向该文件。

## 0.11.7 正式版（2026-07-29）

- 流量转化页已拆除外层 `10px` 留白、18px 圆角、阴影和独立灰底；嵌入文档的 `html / body / app` 背景在工作台模式下透明，与工作台共享同一块大背景。
- 原顶部四个功能标签、转化 SOP、话术搜索、按需求找方案、用户旅程、帮助与手机入口均保留，没有删减业务功能。
- 1280 像素宽度实测页面宽度与视口一致，无横向溢出；正式便携版已在 4327 启动并完成截图核对。
- 自动化测试 105/105 通过。

## 0.11.6 正式版（2026-07-29）

- 内容制作右侧改为真实成品文件夹浏览器：未打包读取待审与成品库，已打包读取设置中的作品集目录，打包记录读取工作流操作历史；文件夹展开后并排显示图片与 TXT。
- 内容分发作品集选择提升为整卡激活态，移除“可发送到手机”冗余字样；完成/失败任务在当前操作区保留 3 分钟并支持手动清除，历史审计不删除。
- 原 Codex 底栏被全局右下角“团建中控助手”替代。确定性命令直接解析；其他自然语言通过当前文本模型归类为能力说明、状态、页面、设置、设备、发送、补货、生产或备份等白名单意图，再交给既有安全动作执行。模型不能传入任意命令，发送继续校验设备白名单、防重复与接收确认。
- 助手已补齐“你能干嘛”和当前状态说明，不再对能力询问返回笼统拒绝。当前安全命令覆盖设备检测、页面切换、分发设置、指定作品发送、按精准/泛流量随机补货；生产命令只在素材与模板已明确时进入生产页，不凭空选择业务素材。
- 设置页新增自动备份频率、方案来源、每月大文件上限和真实进度。小数据备份上传 JSON；大文件按相对路径、大小与修改时间增量上传，跳过软链接，默认每月 2560MB，超额文件下月继续。
- 方案增量备份源已统一为企微微盘公司目录 `D:\AICode\AI\data\01-团建策划-江湖有旅人\01-知识库\WXWork\1688855280811743\WeDrive\江湖有旅人\杭州江湖有旅人户外运动策划有限公司`；“流量转化 → 按需求找方案”也使用同一目录，当前识别 730 个来源文件、729 份可搜索方案。
- 2026-07-29 正式坚果云测试仍返回 HTTP 401。已修复便携应用没有使用本机代理环境、只返回 `fetch failed` 的问题；现在网络可以到达坚果云并返回真实鉴权结果，但云端写入仍被失效的坚果云应用密码阻塞，不能标记为备份成功。
- 修复同源流量转化的嵌入 fetch 包装器被通用改写二次加前缀的问题。真实开发页面复测为 0 个 404；1280×720、1024×700 均无横向溢出。
- 自动化测试 104 项通过（含助手白名单意图、重复代理前缀回归）；语法检查通过。正式包、桌面入口、Git 推送和发布结果见本节后续发布记录。
- 0.11.6 便携包大小 96,062,601 字节，SHA-256 `10B3FCC6E3B155B4B5FF3D5B11B270F4029FC249CC348C962358A7993CBF13EA`；打包应用已真实启动 4327，正式界面复测“你能干嘛”返回完整能力清单，1280 像素宽度无横向溢出。
- 当前坚果云大文件任务已用上述企微目录真实触发，进度进入检查阶段后在 0% 明确失败为“坚果云连接失败（401）”。本地路径、增量清单、进度和代理链路有效；更换坚果云应用密码后才可继续真实上传。

## 0.11.5 正式版（2026-07-29）

- 自动分发失败根因已用正式任务记录确认：随机补货时 Python 脚本实际选中了 `作品集_050[转]`，但 Node 任务记录的 `collection` 仍为空；手机接收成功后，空名称进入移动函数，触发“作品集名称无效”。
- `transfer-progress` 现在从“补货完成”JSON 回执读取 `asset` 并回填任务作品集名称，同时兼容回执跨输出分段；随后按真实名称把原文件夹移动到“微信公众号”。
- 本次真实失败记录已恢复：`作品集_050[转]` 已从“抖音小红书”补移至“微信公众号”，未再次发送手机。
- 用户界面的 `[转]` 统一命名为“精准流量（业务类）”；内部 `conversion` 值、底层 `[转]` 后缀与脚本旧别名保持兼容。
- 内容分发与设置抽屉已在 1280×720、1024×700 检查，无横向溢出；完整 Node 自动化测试 93/93 通过。
- 正式便携包首次烟测发现 `undici` 只存在于开发依赖树、未进入发布包；现已提升为生产依赖并重新构建，打包后的独立程序已成功启动 4327，`appInfo.version=0.11.5`、`channel=便携版`。
- 正式版版本号为 0.11.5；桌面正式应用已重启到 4327，桌面快捷方式继续指向正式源码启动器，便携包 SHA-256 为 `4AC6BA8CEF4066CFB5A3CC49521E45C76ED21B75E7E8023D278B9C836024BA8D`。

## 0.11.5-dev 全界面细节巡检（2026-07-29）

- 本轮仅更新 4337 开发测试版；正式便携版、4327 正式服务、桌面正式快捷方式与发布包均未改动。
- 素材生产、内容分发、插件市场和设置页的帮助/设置入口统一为内联 SVG，固定 36×36 正圆；嵌入的流量转化帮助入口保持原应用 34×34 正圆。
- 设置页 API、工作目录、云备份、防重复与版本卡片明确占据 12 栏网格中的完整跨度，窄窗口统一回落单栏，不再出现文字竖排和按钮被压窄。
- 生产页在 1280、1024、900 像素宽度下均完成布局检查；筛选栏和模型工具栏允许换行，三栏在中等窗口收缩、窄窗口转单栏，页面不再出现可见横向溢出。
- “流量转化”代理现在同时改写 HTML 与外部 JavaScript 中的绝对 `/api/*` 请求；`正式SOP增强.js`、正式 SOP 数据与用户状态均已通过 4337 同源地址验证。
- 自动化测试 91/91 通过，`public/app.js` 与 `server.js` 语法检查通过。

## 手机准话助手开发版修复（2026-07-29）

- 手机空白的根因是服务只监听电脑回环地址，且完整转化知识库页面体积约 10MB，不适合手机直接加载。
- 开发启动器现将测试服务开放到局域网的 4337 端口，但只放行带随机令牌的 `/mobile-conversion/*`；从手机访问普通工作台仍返回 403。
- 工作台“流量转化”右下角新增圆形手机入口按钮。点击后复制安全访问地址，手机与电脑连接同一 Wi-Fi 后粘贴打开即可。
- 手机页是独立轻量交互层，继续调用电脑内现有转化能力；支持前端运营/后端转化、首选回复、备用回复、下一步、待确认信息、提醒和复制。
- 手机访问令牌只保存在开发运行目录的 `secrets` 中，不进入仓库、文档、截图或发布包。
- 已验证：未授权局域网访问被拒绝；持令牌首次访问写入安全 Cookie；手机页面正常显示；真实 MiniMax-M2.7 回复成功；完整测试 85/85 通过。
- 正式使用版、桌面快捷方式、便携包和发布仓库均未更新。只有用户明确要求“更新正式版”后才执行正式交付。

## 0.11.4 当前事实（2026-07-29）

- “开始生产 → 等待确认”不可操作的问题已定位为中栏底部绝对定位遮挡，而不是接口或用户操作问题；现已改为可见的底部操作坞。
- 首次点击生成计划后，计划自动滚入视野，主按钮变为“确认并开始生成”，旁边显示“返回调整”；真实页面 1280×720 与 1024×700 均已检查。
- 自动化检查为 79/79 通过；真实页面验证了计划生成、确认入口可见及返回调整复位。验收中没有再次点击确认，因此没有重复生成第二套。
- 本地 GPT `gpt-image-2` 已完成真实 9 页套图，全部为 1200×1600 PNG；图片累计生成约 899.2 秒，整套墙钟约 15 分 20 秒，文案约 20.5 秒。
- 质检结论：9 页视觉语言、手写标题、黄底强调、页码和纸张底色保持一致；P1 封面与 P2-P9 内页角色清楚。当前仍需人工复核封面营销词、景点小字和来源事实，不自动进入正式成品库。
- 当前链路是逐页串行请求，时间超过用户常用的 3–10 分钟。下一阶段应优先实现“整套生产会话”：模板资产只装载一次，页间共享结构化母版指纹与已生成页面摘要；并在供应商允许时对独立页面做受控并发。
- 本轮为 0.11.4 本地源码热更新，不构建便携包。桌面“团建工作台”快捷方式继续指向 `src/launch.ps1`，重新打开即可加载最新版本。

## 0.11.3 本地热更新补充（2026-07-29）

- 所有关键状态位置统一增加小圆点提醒；运行中的生产状态使用轻量呼吸反馈，不新增占空间的大卡片。
- 素材生产中栏固定显示“当前阶段、完成百分比、进度条、当前状态、最近日志”。开始生产后按计划生成、任务启动、逐页生图和完成阶段从 8% 推进到 100%。
- 生产控制区固定在中栏底部；提示词与本次要求输入框改为紧凑可滚动高度，1200×799 实页检查时无需滚动即可看到开始按钮、0% 进度条、状态和日志。
- 目录动作统一使用“选择 / 应用”等短标签；素材、模板、成品及状态文字已提高可读字号。
- 当前源码自动化测试为 74/74 通过；江湖转化助手自检为 11 通过、2 个结构警告、0 错误。系统级路由质检通过。
- 真实生图仍受外部接口阻塞：本地 OpenAI 兼容通道返回 502，ByteCat 端点连接超时，现有 MiniMax 凭据返回 401。代码、进度展示和失败反馈已通过，但在提供至少一条可用生图接口前，不能宣称真实生产验收通过，也不进行发布推送。
- 桌面“团建工作台”快捷方式继续指向 `src/launch.ps1`，因此本地源码热更新后直接从桌面图标启动即可读取最新代码，无需重新打包。

## 0.11.3 当前事实

- 流量转化通过 `/conversion-integrated/` 同源接入，页面快照可直接读取内层结构；用户侧不再出现 `8765` 地址。
- 转化顶部保留搜索、SOP、找方案、用户旅程；聊天来源、知识入库、隐私显示位于搜索页，方案来源位于方案页。
- 工作台设置新增全局 API/模型卡；生产页不再重复展示接口设置。
- 本轮源码热更新，未构建便携包；Node 语法检查和 71 项自动化测试通过。

## 0.11.2 当前事实

- 侧栏主题入口已改为单按钮循环切换“拟态 / 玻璃”，两套主题均来自江湖团建转化助手官方实现。
- 素材生产、分发、插件市场和设置已复用同一套视觉令牌；流量转化 iframe 不接受外层 CSS 覆盖，只接收限定来源的主题消息。
- 转化助手嵌入态隐藏重复主题入口，独立运行不受影响。
- 本轮版本为 0.11.2，本地源码热更新，不构建便携包。
- Node 语法检查通过，自动化测试 71 项全部通过；1280×720 实页验证主题按钮、五个主入口和 iframe 主题同步正常。

## 0.11.1 当前事实

- 工作台自身设置页已删去主题重复卡、GPT 扩展重复卡、旧一键工作包、自动打包/成组控件、发布包目录和历史工具入口。
- 当前仅保留工作目录、备份与恢复、生产与分发数据、软件与诊断四块；外层侧栏和原版流量转化界面未改动。
- 保存目录会保留服务端现有的成组与压缩参数；素材目录和成品目录均支持桌面文件夹选择。
- 本轮版本为 0.11.1，本地源码热更新，不构建便携包。
- Node 语法检查通过，自动化测试 71 项全部通过；1280×720 实页已核对四块设置按两列完整组织，侧栏主题切换仍保留。

## 0.11.0 当前事实

- 产品名和桌面入口已更新为“团建工作台”；桌面快捷方式指向 `src/launch.ps1`，本轮只做本地源码热更新，未打包便携版。
- 顶层“总览”和“作品集”均已移除；库存与发布阶段仍在“分发功能”。主界面保留团建工作台自己的 168px 文字侧栏、无顶部横栏、冷灰蓝拟态/玻璃双主题和底部服务状态。
- “流量转化”右侧不再使用工作台自制的删减版页面，而是完整 iframe 嵌入原版江湖团建转化助手；原版的搜问题和话术、转化 SOP、按需求找方案、知识库完善、设置和用户旅程均保留，只按右侧可用空间自适应缩小。
- 1280×720 实页已核对：外层侧栏宽 168px，右侧原版助手占 1092×700；不存在 `conversionContent` 或转化模块复制 DOM。
- `docs/PRODUCT-BRIEF.md` 是后续功能开发前的轻量产品初稿，先锁定任务、数据、骨架和验收再编码。
- 已增加“外层侧栏＋原版助手 iframe、禁止删减版 DOM”的回归测试；Node 语法检查通过，自动化测试 70 项全部通过。浏览器逐项打开原版六个栏目成功。

## 0.10.2 当前事实

- 本地开发源码已热更新；桌面图标继续指向 `src/launch.ps1`，本轮未打包便携版。
- 素材和模板均在各自文件夹条目下方横向展开无标题缩略图，点击缩略图显示遮罩大图；中间不再显示模板图片。
- 素材根目录和上次分类自动扫描；切换分类或根目录时直接读取，不再需要额外点击“读取”。
- 中栏可分别选择生图平台、生图模型和文案模型。当前 ByteCat `gpt-image-2` 凭据及模型发现可用；图片与文案模型不绑死。
- 最近两次失败任务的直接原因是上游 504 Gateway Timeout；0.10.2 已对临时状态与网络错误增加三次重试，并清理 HTML 网关报错。
- Node 语法检查通过，自动化测试 68 项通过；1600×1000 本地实页已核对文件夹内联缩略图、独立模型控件和无模板中栏预览。

## 0.10.1 当前事实

- 素材生产页已重构为三栏：左侧素材与模板、中间模板专属对话生产、右侧成品与待发。
- 素材支持多选、常用标签筛选、当前结果全选，并显示图片数、TXT、母标签和使用次数；仍保持分类懒加载，不在启动时递归全库。
- 模板分为转化模板和游戏模板，具体模板显示缩略图、用途、图片数量、默认页数和长期规则。
- 每个模板的规则与对话历史按模板 ID 保存在本机浏览器状态；真实生成仍调用既有计划确认、逐页生图、文案和生产记录链路。
- 新增 `/api/production/workspace` 与 `/api/production/pack`；后者复制完整作品文件夹到 `抖音小红书`，同名跳过、不覆盖、不删除源文件，并写入操作历史。
- 开发态 1600×1000 实机截图确认三栏无横向溢出，素材、模板参考图、规则、成品缩略图和打包入口可见。
- ByteCat Image 2.0 密钥仅写入运行数据的 `secrets/image-api.local.env`，不进入仓库或发布包。2026-07-28 已将渠道切换为 `image` 生图分组，并用 `gpt-image-2` 完成真实参考图编辑：输出 1024×1536 PNG 到 `API生产待审`。
- ByteCat 图片分组不提供文本模型通道；0.10.1 已把标题和小红书文案自动路由到本机已验证的 OpenAI 兼容文本接口，图片仍走 ByteCat。
- Node 语法检查通过，自动化测试 63 项通过；最终便携版桌面烟测通过三栏工作台、素材分类切换、游戏/转化模板、成品打包入口、作品集、设备分发、插件市场、帮助弹层和拟态/玻璃主题。
- 0.10.1 便携版大小 89,564,723 字节，SHA-256 `7FD1CB0B42F5023009677655D1C7908FA1CF1C29BC713E75CFD7DF981B99A949`；打包应用实测主页返回 200，ByteCat 模型检查返回 `gpt-image-2` 可用；桌面快捷方式已切换到 0.10.1。
- GitHub `v0.10.1` Release 已发布：<https://github.com/zwmopen/teambuilding-workflow-dashboard/releases/tag/v0.10.1>；源码和发布记录已推送到 `master`。
- 0.10.0 便携版大小 89,564,837 字节，SHA-256 `706187A7789D2F7B675A9C91CFFEE783EB1772E93194BD6BDA4ABFE7CA9732D7`，保留作回退版本。
- 2026-07-28 分发踩坑：应用升级/重启时若直接终止正在运行的分发子进程，手机可能停在 `receiving`，并留下 `state=sending` 的独占 claim；后续重试会在 0% 被防重复规则拦截。发布或重启应用前必须先检查 `/api/distribution/tasks`，不得停止仍在运行的任务。恢复时按精确远端任务 ID 取消接收，确认没有成功使用日志后，将旧 claim 可恢复归档，再重新发送；禁止直接删除 claim 后盲目重发。
- GitHub `v0.10.0` Release 已发布：<https://github.com/zwmopen/teambuilding-workflow-dashboard/releases/tag/v0.10.0>；源码和发布记录已推送到 `master`。

## 0.9.9 当前事实

- 主界面只保留“拟态 / 玻璃”两套视觉语言，默认拟态；侧边栏激活态不再使用黑底。
- 插件市场已登记 7 个已核对的本地/开源资产，支持分类、搜索、本地启动、打开安装包和访问仓库。
- 帮助入口统一为带书页图标的“说明”，弹层提供开发背景、方法论、步骤、数据真源和风险边界。
- 设置页新增坚果云备份，配置文件位于运行数据 `secrets/webdav-config.dpapi.json`，使用 Windows CurrentUser DPAPI 加密，不进入源码或发布包。
- 云端只备份工作台 JSON 状态和操作记录，不备份素材、模板或成品大文件；目标目录固定为人生游戏备份根目录下的 `团建内容工作台` 子目录。
- 已成功从人生游戏系统导入现存配置；2026-07-28 真实连接测试返回 HTTP 401，说明现存坚果云应用密码已失效。用户在人生游戏系统更新密码后，可在工作台点击“沿用人生游戏配置”重新导入。
- Node 自动化测试 62 项通过；桌面实机烟测通过 5 个主导航、插件市场、两套主题、帮助弹层、作品与设备分发，检测到 2 台在线设备。
- 0.9.9 便携版大小 89,558,368 字节，SHA-256 `6AB5C52C8EAF2A06816C08CEB2586290464B40967F553D812149E688301949C7`；桌面快捷方式已切换到 0.9.9。

## 0.9.8 当前事实

- 分发页五个顶部标签为设备、抖音小红书、微信公众号、已发送、操作记录。
- 抖音小红书与微信公众号均有泛流量帖、精准流量帖、未分类三个子标签；分类下拉会直接同步修改物理文件夹名中的 `[泛]` / `[转]`。
- 分类改名只允许三个工作流目录内的真实作品文件夹，同名目标拒绝覆盖，并同步重建旧发布空间兼容 Junction。
- 工作流操作日志存放于成品库 `_portfolio_move_logs/operation-history.jsonl`，界面与设备、公众号历史一起显示。
- 五个分发标签、三个分类标签、页面帮助、设备传送确认和核心 API 已通过桌面实机烟测；Node 自动化测试 62 项通过。
- 0.9.8 便携版大小 89,551,100 字节，SHA-256 `948ABDE1F1B60EB414EA4D39EB6D8E87D655415C80AEF1B50B833F187C5C7EFF`；桌面快捷方式已切换到 0.9.8。

## 0.9.7 当前事实

- 三阶段业务真源为成品库下的 `抖音小红书`、`微信公众号`、`已发送`。
- 手机确认接收后原作品文件夹移动到微信公众号；确认三端发布后生成 ZIP，校验成功再删除原文件夹。
- 2026-07-28 已按历史账本整理现有库存：13 个待手机作品文件夹、6 个待公众号文件夹、44 个已发送压缩包；3 个已有压缩包的重复原文件夹已清理。
- 发布空间下的小红书、抖音、公众号 Junction 仅作为旧传送脚本兼容入口，当前无断链。
- 界面“按历史记录整理”可重复执行；同名冲突不覆盖，空目录不能归档。
- 0.9.7 自动化测试 61 项通过，桌面实机烟测通过三阶段路径、作品展开、设备连接标签和核心 API。
- 0.9.7 便携版大小 89,546,407 字节，SHA-256 `3EED310A1E8122696276E84F1798269BEA8B9C5722C2CFDDA08835A2A1226AA2`。

## 0.9.6 当前事实

- 素材生产主入口已从“封面/内页校准＋接口参数”改为“做一张 / 做一套 / 批量做”；接口设置默认收起。
- 点击主按钮先调用 `/api/production/plan`，按有效素材生成可读的逐页计划；用户确认后调用 `/api/production/run` 启动后台任务，前台通过 `/api/production/jobs/:id` 轮询真实进度。
- 模板规则不再只存在聊天记录或一段通用提示词中：`src/lib/production-recipes.js` 当前结构化识别米字格黄底引号、湖景绿底键帽、森林路线节点中缝标题、备忘录手写四类配方，其他模板降级为严格母版迁移。
- “做一张”输出一张待审 3:4 图片；“做一套”与“批量做”按每套素材生成 `P1…Px`、`小红书文案.txt`、`出图计划.json` 和 `生产记录.json`。全部先写入 `API生产待审`，不自动进入正式成品库。
- 图片逐页单独调用，页面角色和素材参考不同，不再把 1/2/6 理解成同一提示词的多个变体。
- 文案由本地 OpenAI 兼容接口的 `gpt-5.6-terra` 生成；2026-07-28 已真实验证该模型返回正文。
- 后台任务目前在应用进程内执行；应用退出会中断正在生成的任务，但已落盘页面保留，并在任务 JSON 中标记失败。这是当前已知限制。
- 0.9.6 自动化测试 56 项通过；桌面实机烟测通过三类生产入口、默认收起接口设置、主生产按钮、原有作品集和设备分发回归。
- “做一张”真实端到端任务已生成 1086×1448 PNG 并进入待审区；标题计划通过本地文案模型提炼为短标题，避免把 `@HR`、emoji 和“快收藏”等文件夹噪音带入正式计划。
- 0.9.6 便携版大小 89,543,147 字节，SHA-256 `6D6FC7EE72BD6B46EC052A622DE51267ED78CF657AD948CDE3E7CE31BD288D7D`；功能提交 `92c463b`。
- GitHub `v0.9.6` Release 已发布，桌面快捷方式已切换到 0.9.6；0.9.5 和更早包保留回退。

## 0.9.5 当前事实

- 素材生产页已经是同屏生产控制台：素材库、模板库、现有成品库保持原结构，顶部新增 API 平台、接口、模型、密钥、数量、质量、提示词、计划确认和待审预览。
- 当前验证可用平台是本地 OpenAI 兼容网关 `gpt-image-2`；MiniMax 已接入但现有本机密钥被官方端拒绝，界面会返回真实错误。
- 密钥真源为运行数据下 `secrets/image-api.local.env`，只允许返回 `credentialConfigured`，禁止把内容写入源码、日志、诊断或发布包。
- 生成状态从“计划确认”进入封面或内页校准；结果落在 `API生产待审`，只有人工质检后才允许进入后续正式成品流程。
- 真实端到端测试使用杭州径山素材和备忘录手写母版，成功产出 1086×1448 PNG；首次测试因虚构露营被判事实不通过，加入事实锁后复测通过。
- 后续优先项：把模板规则进一步结构化为可编辑配方；增加逐页质检与“批准进入正式成品库”；接入文案模型后让每个作品文件夹稳定包含多张独立图片和一份文案。
- 0.9.5 便携版大小 89,534,841 字节，SHA-256 `898FC55FB6B2C9271D7E03423DB805FF16493BA04C37DF174B834BC7ED7385F2`；包内 `package.json` 为 0.9.5，且包含 `/api/image-api/generate`；功能提交 `d5d5667`。

- 当前开发版本：0.9.4
- 当前稳定入口：桌面快捷方式“团建内容工作台”（发布后指向 0.9.4 便携版）
- 浏览器调试地址：`http://127.0.0.1:4327`
- 业务真源：`D:\AICode\项目推进\projects\江湖有旅人\主项目`
- 运行数据：`D:\AICode\运行数据\江湖有旅人\图文生产控制台`

## 0.9.4 当前事实

- Electron 已移除右侧 ChatGPT webview、多账号 profile 和网页注入链路，主窗口直接承载本地工作台；Google/ChatGPT 登录只在系统 Chrome/Edge 中完成，由独立浏览器扩展协作。
- 素材生产页按模板、素材、生图 API、作品与文案四步重排；接口地址/模型可保存，密钥仅读环境变量，真实供应商调用尚未接入。
- 设备发现会合并本轮与最近 10 分钟状态并写入运行数据，前台区分“Wi‑Fi 在线”和“Wi‑Fi 最近在线”；实际发送仍重新发现，过期缓存不绕过底层连接校验。
- 设备卡片和两个作品包设备选择窗口均显示紧凑的 Wi‑Fi、USB、远程连接方式标签。
- 局域网发现设备映射为 Wi‑Fi 在线；USB 只有底层实际连接才显示在线，兼容旧 iPhone 的大文件 USB 路径显示为备用；远程能力未接入前显示待接入。
- 底层完成 JSON 中的真实传输协议进入前台任务结果；本地 `device-usage-log.csv` 继续保存设备、时间、协议和接收确认。
- 分发闭环保持 claim、手机接收确认、本地 CSV 记录和物理阶段文件夹联动；已有手机发送记录会覆盖误建平台入口，阻止作品集重复发送。
- Node 自动化测试 49/49 通过；桌面实机烟测通过。
- 0.9.4 便携版：89,529,505 字节，SHA-256 `480FD9F3192B8EF1A6F73F7188778248A486691AE80C588DA955C8CAFB367EE7`，源码提交 `80fce76`。

## 0.9.3 当前事实

- 作品集主界面已改为三个顶部阶段标签：抖音、小红书、公众号、已使用。
- 阶段由磁盘目录直接决定，允许工作台与资源管理器双向修改；同名目标存在时拒绝覆盖。
- 未分类作品归入第一阶段并禁用发送，当前真实数据为第一阶段 14、公众号 2、已使用 7，合计 23，无隐藏作品。
- 手机确认接收后原始目录进入公众号，公众号标记后原始目录进入已使用。
- 语法检查通过，Node 自动化测试 47/47 通过；桌面逐项验收通过三阶段标签、12/12 首阶段作品展开、预览、设备选择、系统内确认和核心 API。
- 0.9.3 便携版已构建并实际启动，API 返回 0.9.3；桌面快捷方式已切换到新版。SHA-256：`AF5296155BB1D3F106F32BB30A71B442AF4E120A1D59766B8CA043E1003255D4`。
- 真实业务作品未用于移动测试；移动与双向刷新使用临时目录验证。

## 0.9.2 当前事实

- 后台全库索引不阻塞工作台启动，扩展可获得真实全局母标签、次数和搜索统计。
- 历史使用次数从素材链接记录和制作日志自动回填，事件键保证重复刷新不重复计数。
- 真实数据隔离预演识别 1953 个素材文件夹；15 条可靠历史生产证据全部唯一匹配到 12 个文件夹，使用 1 次 10 个、2 次 1 个、3 次 1 个，待核对 0 条。
- 自动化验收：工作台 45/45、扩展 8/8，双方 JavaScript 语法检查通过。

## 0.9.1 当前事实

- 工作台不再启动时全库递归：初始只扫描状态中当前分类，其他 10 个分类保持未加载；扩展 `/api/materials` 初始只返回分类索引，展开后才读取对应分类。
- 修复前实测 `/api/dashboard` 约 21 MB / 42 秒；修复后同机冷请求约 0.37 MB / 1.1 秒，扩展初始素材索引约 3.7 KB / 22 毫秒。
- 设置页已改成同页紧凑分组，视觉选项精简为清晰工作台、纸墨编辑部、深色模式。
- 2026-07-27 验收：服务端与前端语法检查通过，44 项 Node 测试通过。
- 0.9.1 便携包已构建并实际启动；包内 API 返回版本 `0.9.1`、11 个分类且只加载当前 1 个分类，实测 746 毫秒 / 367346 字节。
- 桌面快捷方式已切换到 `releases/0.9.1/团建内容工作台-0.9.1-便携版.exe`；0.9.0 与更早发布包均保留回退。
- 0.9.1 SHA-256：`D6C3FF47EED18EB935D816F630ED5855CCD2D8D99D0F8F5BB082FA0D78BDD324`；源码提交 `dc538fe`。

## 0.9.0 当前事实
- 每个有效素材文件夹都有独有的目录身份哈希；同卷改名或移动后保持不变，两份相同内容仍拥有不同身份。
- 元数据台账以哈希绑定唯一母标签和使用次数；素材改名或移动后仍能恢复。
- 母标签界面口径限定为团建游戏、精准流量（业务类）和合集攻略；内部继续兼容旧标签“团建转化”，聚会游戏、破冰游戏、团队游戏等统一归入团建游戏。
- 自动母标签只提供默认值，扩展通过 `/api/extension/material-metadata` 写入的人工选择优先。
- 文件夹身份哈希与图片/文案聚合内容指纹分离：前者绑定标签，后者只负责查重。
- 2026-07-27 验收：服务端语法检查通过，43 项 Node 测试通过。
- 0.9.0 便携版已构建并实际启动，桌面快捷方式已切换；`/api/materials` 实测返回 9 个分类，选定分类样本返回 64 位文件夹哈希、单一母标签、自动/人工来源和使用次数。

## 0.8.9 当前事实

- 新增 `POST /api/extension/move-entry`，供扩展在用户确认后真实移动文件夹。
- 仅允许在同一授权素材库或成品库内部移动真实目录；拒绝根目录、文件、软链接、跨库、自身子目录和同名覆盖。
- 移动通过同卷 `rename` 完成，成功后清理素材缓存；测试使用临时目录，不改动真实素材和成品。
- 2026-07-27 验收：服务端语法检查通过，40 项 Node 测试通过。
- 0.8.9 便携包已构建；解包后的服务端通过独立端口启动，`/api/extension/info` 返回 200，新增移动接口对空请求按设计返回 400 且未改动任何真实目录。
- 桌面快捷方式“团建内容工作台”已指向 0.8.9 便携版；0.8.8 包和原目录保留，可直接回滚。

## 0.8.8 当前事实

- `/api/extension/material-usage-check` 同时按路径和素材内容指纹检查历史使用记录。
- 内容指纹只在选定上传时计算，避免对整个素材库持续做高开销哈希。
- 2026-07-27 联动验收：38 项 Node 测试通过；真实素材库帖子经 HTTP 调用 `material-usage-check` 返回内容指纹与 `unused` 判定；记录→改名→指纹命中链路由 `path-security.test.js` 用临时账本覆盖，未写入真实台账。
- 素材使用台账文件在第一次真实记录时才会创建；扩展侧对应版本为 0.1.8。

## 0.8.7 当前事实

- `POST /api/extension/material-use` 持久化素材的 `prepared` / `used` 状态。
- 台账路径为运行数据目录下的 `防重复账本/material-usage-ledger.json`。
- 记录操作不会移动、重命名或删除用户素材；素材接口会把状态返回给拓展。

## 0.8.6 当前事实

- `GET /api/extension/product-tree` 返回设置目录中的完整本地目录树，不使用分发库存过滤条件。
- `POST /api/extension/work-package` 直接执行 `D:\Download\make_work_package.ps1`，供浏览器拓展调用。
- 新版默认链路不调用 VBS；旧脚本和旧协议仍保留，互不破坏。
- 已用真实成品目录验证 28 个顶层条目可见，包含“已发送”“发布空间”和近期打包目录。
- 已用预演请求确认返回 `mode=workbench-direct`、`fallback=false`。

## 已实现

- 素材生产默认打开 `01-素材库`，递归识别同时包含图片和文案的帖子文件夹。
- 素材树支持紧凑列表和小图标视图；每个有效帖子可“传 GPT”。
- 工作台不再把内嵌网页作为主要 GPT 登录方案；“生产数字作品”使用正常浏览器里的独立扩展，避免 Google 将自研内嵌浏览器判为不安全。
- 工作台新增独立扩展协作接口，提供授权路径、作品集/作品、素材树、文件流和生产历史只读状态。
- 扩展协作接口支持浏览器私有网络预检，修复真实 ChatGPT 页面访问 `127.0.0.1` 时显示 `Failed to fetch`、路径正确但无法扫描的问题。
- 设置页可查看版本、检查当前版本、打开发布目录/运行数据目录/独立扩展目录和复制诊断信息。
- 作品集直接读取用户选择的本地目录；按 `[泛]`、`[转]`、无标签分类，可切换紧凑列表/网格、展开每个合集并预览图片和文本。
- 作品集前台已删除搜索、单独“小红书可用”“全部已使用”和重复台账详情；手机组只展示整组可用包。
- “发布复盘”入口与页面已删除；聚光和生产日志仍保留在设置的高级入口。
- 分发页后台自动刷新设备状态，显示在线/登记总数，在线设备置顶，离线操作禁用。
- 支持随机补泛流量/精准流量（业务类）、指定作品包选择在线设备、普通文件或文件夹上传、拖放、进度和取消。
- 作品包分发与普通文件传输都使用可恢复任务；页面显示真实阶段、百分比、停止入口和最终结果，刷新页面后可恢复当前及最近任务。
- 全站确认、错误、输入和完成反馈已改为系统内部弹层；前台不显示软链接、Junction 或原始命令行输出。
- 设备名称可保存本机备注，不改手机端原名。
- 手机分发成功后删除已校验的活动平台 Junction，不再创建抖音归档入口；源作品集始终保留。
- 作品包使用独占 claim + `device-usage-log.csv` + Junction 删除三层防重复；中断时宁可保留 claim 等待核对，也不会自动重发。
- 手机组只显示并抽取“小红书 + 抖音”两个入口同时有效的作品包；任一手机平台用过后整组不可再用，公众号组保持独立。
- 公众号流程为“公众号可用 → 已打开过 → 人工确认 → 上传已完成”。

## 执行真源

- 本地界面和任务编排：`src/`
- 手机发现、传输、接收确认与平台 Junction 安全处理：`D:\AICode\AI\skills\技能包\技能\device-folder-transfer`
- 现有网页模块、VBS、PowerShell、下载历史和工作包协议继续并行保留，作为回退路径；同步只做机械副本校验，不得覆盖或搬走原文件。
- 独立扩展源码：`D:\AICode\工具开发\projects\teambuilding-gpt-production-extension`。工作台仓库不维护第二份扩展源码。
- 生产历史真源：`D:\AICode\项目推进\projects\江湖有旅人\主项目\成品库（GPT+本地脚本制作）\_作品历史数据\作品历史数据库.json`，当前 193 组；只读汇总，不进入源码和公开包。
- 生产重复判定：整组图片 SHA-256 确定重复；64 位 dHash 及汉明距离阈值只做近似预警。旧文本哈希仅作兼容，不得冒充图片历史。

## 验收状态

- Node 自动化测试：38 项通过（2026-07-27，含素材使用台账与内容指纹链路）。
- `device-folder-transfer` Python 测试：23 项通过。
- 桌面实际验证：11/11 个素材分类和 21/21 个作品集逐项展开；图片/文本预览、三套分发子页、在线置顶、离线禁用、上传其他、选择设备、系统内二次确认和六主题全部通过。
- 发布级桌面测试已于 2026-07-26 通过；曾出现的“上传其他”失败是测试停留在泛流量筛选后误查设备行，测试现已显式切回设备列表并重新通过。
- 本机扩展协作接口实测：21 个作品集、11 个素材分类、193 组生产历史；CORS 与私有网络访问许可限定 ChatGPT 与扩展来源。
- 性能：主数据只读取当前分类，约 1.1 秒 / 0.37 MB；扩展初始素材索引约 22 毫秒 / 3.7 KB。切换或展开分类时只递归对应目录。
- 真实传输：通过应用接口向在线 1 号设备发送 165 字节验收文件，接收端提交成功，最终为 100% / completed。
- 0.8.8 便携版已于 2026-07-27 构建并真机验收：6/6 核心 API 返回 200，`material-usage-check` 在打包应用上返回与 dev 版一致的内容指纹；SHA-256 见 `releases/manifest.json`。
- 0.8.2 / 0.8.3 便携包仍保留作回退。
- 桌面快捷方式“团建内容工作台”已指向 0.8.8。
- 已发布 GitHub `v0.8.8` 标签与 Release（zwmopen/teambuilding-workflow-dashboard）。

## 仍需后续完成

- 扩展 0.1.8 在真实 ChatGPT 页面的整链（记录 prepared/used → 改名 → 指纹拦截）尚未在真实会话中走完一次；下次真实生产时顺带验收即可，API 与单测已覆盖。
- 作品包进度来自底层传输输出；在极短的小文件任务中，阶段可能快速跳过，但最终状态和使用记录仍会保留。
- 手机向电脑接收文件仍由原独立接收端承担；迁入工作台前不得删除旧软件。
- 平台发布自动点击、网络隔离和账号调度不在本版本实现范围内（侧边保留功能入口位）。

最后核对：2026-07-27，对应开发版本 0.9.1。

## 0.14.3 开发热更新补充

## 0.14.5 单帖子输入框边界（2026-08-01）

- 工作台与内置网页执行器双重校验每个任务只包含一个帖子文件夹内的附件；队列可以有多个帖子，但当前帖完整结束前不会上传下一帖。
- GPT composer 中的未发送附件和未发送文字都会跨刷新保留。两者现在分别以 `COMPOSER_ATTACHMENTS_PENDING`、`COMPOSER_DRAFT_PENDING` 暂停整批，保留当前索引并跨重启阻止自动越过。
- 历史聊天内容不是 composer 草稿，不参与拦截；升级、热重启和故障恢复不得清理 Chromium/GPT 登录分区。
- 真实 Plus 桌面证据：两个帖子排队时，第一帖解析为 6 页计划并只发送一次 `1`；生成仅 1 张图后按触顶征兆暂停，第二帖未上传，第一帖未下载、打包、归档或移动。
- 热重启验证：开发端已加载工作台 `0.14.5` 和扩展 `0.2.17`；原 ChatGPT 会话 URL 与登录态保持，composer 草稿为空、未发送附件为 0。永不停歇意图仍为 armed，队列可预排多帖但当前只有第一帖进入运行阶段。
- 自动化基线：工作台 167/167，网页执行扩展 18/18，扩展语法检查通过。真实完整成功链路仍受本次生图额度触顶影响，需在下一次额度周期继续观察。

### 0.14.4 额度边界补充

- `ensureGptTaskQuota` 现在永远只做本地估算提醒，不再抛出额度不足异常；这保证单窗口可以长期运行。
- GPT 网页真实返回限额时，`recordActualGptLimit` 才记录首次触发时间和最早检查时间，当前任务暂停且不盲目重试。
- 额度恢复基准以实际消耗起点为准：`uploadCycleStartAt` 与 `generationCycleStartAt` 分开记录，各自加配置的滚动窗口；撞限时间只记录为事件，不作为恢复起点。
- 0.14.4 回归验证：主工作台测试 `161/161`，GPT 生产扩展测试 `15/15`；开发服务已热更新到 0.14.4，未清理 Chromium/GPT 登录分区或用户数据。

- 单窗口模式允许长期连续尝试；本地额度账本只显示提醒，不能阻止用户点击继续。多窗口仍保留保守的本地并发/额度门槛。
- 任务在桥接调用前被暂停时，队列会持久化 `_submittedToGpt=false`。继续时工作台会从当前素材树恢复附件并发送 `forceUpload=true`；扩展因此不会走“只恢复阶段”的分支。
- 任务已经送达桥接后才使用 `retryFromStage`，继续时不会重复上传附件。
- 更新只改源码和扩展运行副本，不清理 Chromium/GPT 登录分区、缓存、设置和生产数据。

## 0.14.4 后续热更新：低图片数触顶保护

- 自动生产设置保留“最低完整图片数（低于此数停止）”，默认值为 4。
- 本轮只检测到 1–3 张图片时，视为 GPT 生图触顶/降级征兆：停止当前批次，不请求小红书文案、不下载、不打包、不归档，也不继续把后续素材送入同一账号。
- 该任务保留在当前队列索引，可在下一次额度探测后从检查点恢复；不会把不完整结果冒充成成品。
- 本轮回归测试：工作台 163/163 通过；未清理 Chromium/GPT 登录分区和用户数据。
- 低图数任务现在直接标记失败并跳过，不在下一周期重复检查同一组残缺图片；队列保留下一条未处理素材。
- 下一次探测使用 `generationCycleStartAt + windowHours`（默认 3 小时），到点自动调用剩余队列。`uploadCycleStartAt` 继续独立记录，但低图数信号以生图周期为准。

## 0.14.4 后续热更新：全天自动工作时段

- “单窗口自动（永不停歇）”是默认模式，使用 `teambuilding-gpt-continuous-run-v1` 保存运行意图；打开工作台后按设置自动恢复，不依赖当前页面是否可见。
- Windows 登录启动通过 Electron `setLoginItemSettings` 配置，默认开启；开发版使用 Electron 可执行文件加源码应用目录，正式版使用自身可执行文件。
- 默认工作时段为北京时间 07:00 至次日 02:00，支持跨午夜。休息时段不启动新作品；正在执行的作品完成后进入等待。
- 应用启动、网络恢复、页面重新可见和每分钟守护检查都会尝试恢复，但同一时间只保留一个启动定时器，不重复发送任务。
- 上传首次消耗与进入生图阶段分别写入本机周期账本；低图数触顶仍以首次进入生图阶段加滚动窗口作为下一次探测点。
- 右侧模板判定保持现有边界：未选本地模板时使用当前浏览器档案正在打开的已调教会话；选择实体模板时，每个模板新建一个会话后再处理所选素材。链接模板仍是后续项，不能宣称已完成。
- 本轮自动化回归 `165/165` 通过；开发服务 4327 已返回新版设置与调度脚本。未清理 Chromium/GPT 登录分区、缓存、目录和生产记录。
- 探测定时器会从 `teambuilding-gpt-web-limit-v1:<quotaGroup>` 恢复；工作台重启后不会丢失。普通自动模式没有剩余素材时只提醒，`all-day/scheduled` 才会按原规则补充素材。
