# 交接

- 当前开发版本：0.9.3
- 当前稳定入口：桌面快捷方式“团建内容工作台”（发布后指向 0.9.3 便携版）
- 浏览器调试地址：`http://127.0.0.1:4327`
- 业务真源：`D:\AICode\项目推进\projects\江湖有旅人\主项目`
- 运行数据：`D:\AICode\运行数据\江湖有旅人\图文生产控制台`

## 0.9.3 当前事实

- 作品集主界面已改为三个顶部阶段标签：抖音、小红书、公众号、已使用。
- 阶段由磁盘目录直接决定，允许工作台与资源管理器双向修改；同名目标存在时拒绝覆盖。
- 未分类作品归入第一阶段并禁用发送，当前真实数据为第一阶段 14、公众号 2、已使用 7，合计 23，无隐藏作品。
- 手机确认接收后原始目录进入公众号，公众号标记后原始目录进入已使用。
- 语法检查通过，Node 自动化测试 47/47 通过；桌面逐项验收通过三阶段标签、12/12 首阶段作品展开、预览、设备选择、系统内确认和核心 API。
- 0.9.3 便携版已构建并实际启动，API 返回 0.9.3；桌面快捷方式已切换到新版。SHA-256：`23BA638EE9065EF03BAAC0129240EFC2AF8CC845AE472DFB4CEC9F8CADC8992C`。
- 真实业务作品未用于移动测试；移动与双向刷新使用临时目录验证。

## 0.9.2 当前事实

- 后台全库索引不阻塞工作台启动，扩展可获得真实全局母标签、次数和搜索统计。
- 历史使用次数从素材链接记录和制作日志自动回填，事件键保证重复刷新不重复计数。
- 真实数据隔离预演识别 1953 个素材文件夹；15 条可靠历史生产证据全部唯一匹配到 12 个文件夹，使用 1 次 10 个、2 次 1 个、3 次 1 个，待核对 0 条。
- 自动化验收：工作台 45/45、扩展 8/8，双方 JavaScript 语法检查通过。

## 0.9.1 当前事实

- 工作台不再启动时全库递归：初始只扫描状态中当前分类，其他 10 个分类保持未加载；扩展 `/api/materials` 初始只返回分类索引，展开后才读取对应分类。
- 修复前实测 `/api/dashboard` 约 21 MB / 42 秒；修复后同机冷请求约 0.37 MB / 1.1 秒，扩展初始素材索引约 3.7 KB / 22 毫秒。
- 设置页已改成同页紧凑分组，视觉选项精简为清晰工作台、纸墨编辑部、深色模式。
- 2026-07-27 验收：服务端与前端语法检查通过，44 项 Node 测试通过。
- 0.9.1 便携包已构建并实际启动；包内 API 返回版本 `0.9.1`、11 个分类且只加载当前 1 个分类，实测 746 毫秒 / 367346 字节。
- 桌面快捷方式已切换到 `releases/0.9.1/团建内容工作台-0.9.1-便携版.exe`；0.9.0 与更早发布包均保留回退。
- 0.9.1 SHA-256：`D6C3FF47EED18EB935D816F630ED5855CCD2D8D99D0F8F5BB082FA0D78BDD324`；源码提交 `dc538fe`。

## 0.9.0 当前事实
- 每个有效素材文件夹都有独有的目录身份哈希；同卷改名或移动后保持不变，两份相同内容仍拥有不同身份。
- 元数据台账以哈希绑定唯一母标签和使用次数；素材改名或移动后仍能恢复。
- 母标签限定为团建游戏、团建转化和合集攻略；聚会游戏、破冰游戏、团队游戏等统一归入团建游戏。
- 自动母标签只提供默认值，扩展通过 `/api/extension/material-metadata` 写入的人工选择优先。
- 文件夹身份哈希与图片/文案聚合内容指纹分离：前者绑定标签，后者只负责查重。
- 2026-07-27 验收：服务端语法检查通过，43 项 Node 测试通过。
- 0.9.0 便携版已构建并实际启动，桌面快捷方式已切换；`/api/materials` 实测返回 9 个分类，选定分类样本返回 64 位文件夹哈希、单一母标签、自动/人工来源和使用次数。

## 0.8.9 当前事实

- 新增 `POST /api/extension/move-entry`，供扩展在用户确认后真实移动文件夹。
- 仅允许在同一授权素材库或成品库内部移动真实目录；拒绝根目录、文件、软链接、跨库、自身子目录和同名覆盖。
- 移动通过同卷 `rename` 完成，成功后清理素材缓存；测试使用临时目录，不改动真实素材和成品。
- 2026-07-27 验收：服务端语法检查通过，40 项 Node 测试通过。
- 0.8.9 便携包已构建；解包后的服务端通过独立端口启动，`/api/extension/info` 返回 200，新增移动接口对空请求按设计返回 400 且未改动任何真实目录。
- 桌面快捷方式“团建内容工作台”已指向 0.8.9 便携版；0.8.8 包和原目录保留，可直接回滚。

## 0.8.8 当前事实

- `/api/extension/material-usage-check` 同时按路径和素材内容指纹检查历史使用记录。
- 内容指纹只在选定上传时计算，避免对整个素材库持续做高开销哈希。
- 2026-07-27 联动验收：38 项 Node 测试通过；真实素材库帖子经 HTTP 调用 `material-usage-check` 返回内容指纹与 `unused` 判定；记录→改名→指纹命中链路由 `path-security.test.js` 用临时账本覆盖，未写入真实台账。
- 素材使用台账文件在第一次真实记录时才会创建；扩展侧对应版本为 0.1.8。

## 0.8.7 当前事实

- `POST /api/extension/material-use` 持久化素材的 `prepared` / `used` 状态。
- 台账路径为运行数据目录下的 `防重复账本/material-usage-ledger.json`。
- 记录操作不会移动、重命名或删除用户素材；素材接口会把状态返回给拓展。

## 0.8.6 当前事实

- `GET /api/extension/product-tree` 返回设置目录中的完整本地目录树，不使用分发库存过滤条件。
- `POST /api/extension/work-package` 直接执行 `D:\Download\make_work_package.ps1`，供浏览器拓展调用。
- 新版默认链路不调用 VBS；旧脚本和旧协议仍保留，互不破坏。
- 已用真实成品目录验证 28 个顶层条目可见，包含“已发送”“发布空间”和近期打包目录。
- 已用预演请求确认返回 `mode=workbench-direct`、`fallback=false`。

## 已实现

- 素材生产默认打开 `01-素材库`，递归识别同时包含图片和文案的帖子文件夹。
- 素材树支持紧凑列表和小图标视图；每个有效帖子可“传 GPT”。
- 工作台不再把内嵌网页作为主要 GPT 登录方案；“生产数字作品”使用正常浏览器里的独立扩展，避免 Google 将自研内嵌浏览器判为不安全。
- 工作台新增独立扩展协作接口，提供授权路径、作品集/作品、素材树、文件流和生产历史只读状态。
- 扩展协作接口支持浏览器私有网络预检，修复真实 ChatGPT 页面访问 `127.0.0.1` 时显示 `Failed to fetch`、路径正确但无法扫描的问题。
- 设置页可查看版本、检查当前版本、打开发布目录/运行数据目录/独立扩展目录和复制诊断信息。
- 作品集直接读取用户选择的本地目录；按 `[泛]`、`[转]`、无标签分类，可切换紧凑列表/网格、展开每个合集并预览图片和文本。
- 作品集前台已删除搜索、单独“小红书可用”“全部已使用”和重复台账详情；手机组只展示整组可用包。
- “发布复盘”入口与页面已删除；聚光和生产日志仍保留在设置的高级入口。
- 分发页后台自动刷新设备状态，显示在线/登记总数，在线设备置顶，离线操作禁用。
- 支持随机补泛流量/团建转化、指定作品包选择在线设备、普通文件或文件夹上传、拖放、进度和取消。
- 作品包分发与普通文件传输都使用可恢复任务；页面显示真实阶段、百分比、停止入口和最终结果，刷新页面后可恢复当前及最近任务。
- 全站确认、错误、输入和完成反馈已改为系统内部弹层；前台不显示软链接、Junction 或原始命令行输出。
- 设备名称可保存本机备注，不改手机端原名。
- 手机分发成功后删除已校验的活动平台 Junction，不再创建抖音归档入口；源作品集始终保留。
- 作品包使用独占 claim + `device-usage-log.csv` + Junction 删除三层防重复；中断时宁可保留 claim 等待核对，也不会自动重发。
- 手机组只显示并抽取“小红书 + 抖音”两个入口同时有效的作品包；任一手机平台用过后整组不可再用，公众号组保持独立。
- 公众号流程为“公众号可用 → 已打开过 → 人工确认 → 上传已完成”。

## 执行真源

- 本地界面和任务编排：`src/`
- 手机发现、传输、接收确认与平台 Junction 安全处理：`D:\AICode\AI\skills\技能包\技能\device-folder-transfer`
- 现有网页模块、VBS、PowerShell、下载历史和工作包协议继续并行保留，作为回退路径；同步只做机械副本校验，不得覆盖或搬走原文件。
- 独立扩展源码：`D:\AICode\工具开发\projects\teambuilding-gpt-production-extension`。工作台仓库不维护第二份扩展源码。
- 生产历史真源：`D:\AICode\项目推进\projects\江湖有旅人\主项目\成品库（GPT+本地脚本制作）\_作品历史数据\作品历史数据库.json`，当前 193 组；只读汇总，不进入源码和公开包。
- 生产重复判定：整组图片 SHA-256 确定重复；64 位 dHash 及汉明距离阈值只做近似预警。旧文本哈希仅作兼容，不得冒充图片历史。

## 验收状态

- Node 自动化测试：38 项通过（2026-07-27，含素材使用台账与内容指纹链路）。
- `device-folder-transfer` Python 测试：23 项通过。
- 桌面实际验证：11/11 个素材分类和 21/21 个作品集逐项展开；图片/文本预览、三套分发子页、在线置顶、离线禁用、上传其他、选择设备、系统内二次确认和六主题全部通过。
- 发布级桌面测试已于 2026-07-26 通过；曾出现的“上传其他”失败是测试停留在泛流量筛选后误查设备行，测试现已显式切回设备列表并重新通过。
- 本机扩展协作接口实测：21 个作品集、11 个素材分类、193 组生产历史；CORS 与私有网络访问许可限定 ChatGPT 与扩展来源。
- 性能：主数据只读取当前分类，约 1.1 秒 / 0.37 MB；扩展初始素材索引约 22 毫秒 / 3.7 KB。切换或展开分类时只递归对应目录。
- 真实传输：通过应用接口向在线 1 号设备发送 165 字节验收文件，接收端提交成功，最终为 100% / completed。
- 0.8.8 便携版已于 2026-07-27 构建并真机验收：6/6 核心 API 返回 200，`material-usage-check` 在打包应用上返回与 dev 版一致的内容指纹；SHA-256 见 `releases/manifest.json`。
- 0.8.2 / 0.8.3 便携包仍保留作回退。
- 桌面快捷方式“团建内容工作台”已指向 0.8.8。
- 已发布 GitHub `v0.8.8` 标签与 Release（zwmopen/teambuilding-workflow-dashboard）。

## 仍需后续完成

- 扩展 0.1.8 在真实 ChatGPT 页面的整链（记录 prepared/used → 改名 → 指纹拦截）尚未在真实会话中走完一次；下次真实生产时顺带验收即可，API 与单测已覆盖。
- 作品包进度来自底层传输输出；在极短的小文件任务中，阶段可能快速跳过，但最终状态和使用记录仍会保留。
- 手机向电脑接收文件仍由原独立接收端承担；迁入工作台前不得删除旧软件。
- 平台发布自动点击、网络隔离和账号调度不在本版本实现范围内（侧边保留功能入口位）。

最后核对：2026-07-27，对应开发版本 0.9.1。
