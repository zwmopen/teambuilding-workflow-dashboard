# 0.8.2 发布验收

发布日期：2026-07-25

## 成品

- 文件：`releases\0.8.2\团建内容工作台-0.8.2-便携版.exe`
- 大小：89,558,348 字节
- SHA-256：`FC697C2ED6953A35F783D1D4A330E8D3226C2502AFBAF12069407B8A292FE1B7`
- 桌面快捷方式：`C:\Users\z\Desktop\团建内容工作台.lnk`
- 公开版本：<https://github.com/zwmopen/teambuilding-workflow-dashboard/releases/tag/v0.8.2>

## 自动化验收

- Node 测试：31/31 通过。
- `device-folder-transfer` Python 测试：23/23 通过。
- JavaScript 语法检查：`server.js`、`public/app.js`、`desktop/main.js` 和桌面实测脚本通过。
- 桌面逐项实测：11/11 个素材分类、21/21 个作品集均可展开；图片/文本预览正常。
- 四个工作流页面、三套分发子页、在线/离线按钮联动、上传其他、设备选择、系统确认弹窗、六套主题均通过。
- 核心 API：主数据、设备状态、聚光数据、作品集 CSV 导出均返回 200。
- ChatGPT：真实网页、登录入口、对话输入框、现有用户脚本和历史状态均已确认。

## 真实运行验收

- 实际识别：1990 个帖子、21 个作品集、2 个团建转化包。
- 工作台库存与原 `device-folder-transfer` Skill 的检查结果一致。
- 使用应用接口向在线 1 号设备发送 165 字节验收文件；接收端 `committed: true`。
- 最终任务状态：`completed`，100%，显示“发送完成并确认接收”。
- 日常主数据读取：约 1.2–1.4 秒；完整素材递归扫描保留为人工刷新动作。
- 便携版独立启动成功；连续启动两次仍只有一个主窗口。
- 设置中的发布目录指向真实 `releases\0.8.2`，不是临时解压目录。

## 数据与兼容性

- 原 VBS、PowerShell、油猴脚本及独立分发 Skill 保持不动、继续可用。
- 可公开脚本在 `src\integrations` 保存平行副本并随便携包发布。
- 个人 ChatGPT 历史只进入 `D:\AICode\运行数据\江湖有旅人\图文生产控制台\chatgpt-history.json`，不提交仓库、不打入公开包。
- 作品使用记录继续以真实平台入口、claim 和 `device-usage-log.csv` 联合防重复。

## 保留边界

- 平台发布自动点击、网络隔离和账号调度仍不在本版本实现范围。
- 正式作品包的破坏性分发没有作为发布测试执行；运输层使用独立验收文件实发，正式作品选择与使用标记由单元测试、计划模式和现有 Skill 测试覆盖。
