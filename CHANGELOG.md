# 变更记录

## 0.15.26 - 2026-08-09（十页计划否定语义与断点续跑）

- 修复计划正文写有“禁止 P11 / 不做 P11 / 不会开启第二批”时被误判为真的要继续第二批的问题；只有肯定提出 P11 或第二批才会触发安全纠正。
- 全局轮换真实运行中，账号 1 的旧对话停在“计划完成，待回复 1”时会保留当前素材并切换账号 2，不覆盖旧对话、不跳过素材。
- 账号 2 的 13 个附件任务从 `PLAN_NOT_READY` 安全检查点恢复，向前找到已完成的 P1-P10 计划，只补发一次 `1`，不重复上传附件。
- 完整自动回归 364/364 通过；本轮实机最终落盘 10 PNG + 1 TXT + 1 JSON，重启后队列为空且未自动领取下一套。

## 0.15.25 - 2026-08-09（生产熔断与小猫稳定性）

- 修复全局轮换遇到 `Failed to fetch` 等临时故障时连续跳过素材的问题。
- 增加账号就绪门禁、同任务跨账号保留和未知故障安全暂停。
- 增加成功归档素材稳定 ID 去重。
- 修复小猫拖动失焦消失；气泡默认常显并增加对应设置。
- 完整自动测试 364 项通过。

## 0.15.24 - 2026-08-08（原生账号模式持久化）

- 真实配置验证发现 renderer 已调用 `saveProfile({...account, mode})`，但 Electron 的 profile 字段白名单没有 `mode`，原生再水合仍会丢失账号 2、4 的轮换资格。
- 主进程 profile schema 新增安全生产模式字段，覆盖读取、默认档案和保存；仅允许 8 个已知模式并迁移旧 `multi`。
- 新增主进程静态回归，确保前端保存与原生读取形成完整闭环。

## 0.15.23 - 2026-08-08（巡检归档双账本闭环）

- 真实 9 图恢复已创建有效作品包，但发现巡检只回传工作台可见历史，服务端检查点仍停在 66%，导致自动选材仍可能把已完成素材视为在途。
- `reportPatrolPackageCompletion()` 改为异步合并写回权威检查点，再同步可见历史；保存完成阶段、图片文件、文案、下载目录和成品路径。
- 旧任务 `gpt-1786200853543-ixc8dm` 已校正为 100% / 9 图 / 1 TXT / `packageValid=true`，成品路径为 `20260808_233904_上海室内团建TOP8｜下雨降温也不怕没地方玩`。

## 0.15.22 - 2026-08-08（巡检恢复图片完整性硬校验）

- 真实恢复发现 ChatGPT 页面只临时水合 1 张图，而检查点已有 9 个完整 URL；旧逻辑只在页面 0 图时才回退，错误创建了 1 图作品包。
- 恢复链现在比较页面与检查点图片数量并采用更完整集合；下载前必须达到检查点计划数/检测数，否则返回 `recovery-image-set-incomplete` 且不产生任何文件或归档动作。
- 错误 1 图包已完整移动到成品库 `_待检查`，原文件可恢复；素材保留在已使用 2 次目录，正确 9 图重打包时不再次增加使用次数。

## 0.15.21 - 2026-08-08（巡检跨重启图文证据恢复）

- 修复当前默认上传词“请完整读取全部附件”未命中旧巡检边界正则的问题；实机账号 2 已有完整计划、9 图和文案时不再误报“待上传素材”。
- 服务端检查点新增 `generatedImageUrls`，普通工作流保存与恢复该证据；巡检打包在 ChatGPT 长对话虚拟化隐藏图片轮次时回退到检查点图片 URL。
- 调试脚本支持可配置等待时间，便于实机长步骤验收；针对性测试通过，完整回归与账号 2 半成品恢复结果待补充。

## 0.15.20 - 2026-08-08（账号模式持久化与跨账号在途去重）

- 修复账号模式只写浏览器本地状态、未同步到 Electron 原生账号配置的问题；重新加载账号配置后不再丢失 `rotate`，账号 1、2、4 可以共同进入全局轮换候选集。
- 自动选材接入服务端生产检查点：近 24 小时已有 20% 以上实质进度且尚未形成有效成品的素材被视为“在途”，不会被另一个账号或重启后的新队列重复领取。
- 新增纯逻辑回归，覆盖近期未闭环、已归档、过期检查点和未开始任务；针对性回归 **160/160**，完整回归与三账号实机验收仍以最终结果为准。

## 0.15.19 - 2026-08-08（多账号轮换额度预检）

- 修正全局多账号轮换的额度检查顺序：先核验本套能否完整进入剩余额度，再记录本地上传周期起点。
- 上传阶段改为如实显示“正在上传、等待网页确认”，避免真实附件预览出现前误报成功。
- 新增额度预检顺序回归测试；实机三账号轮换结果待本轮真实生产补充。

## 0.15.18 - 2026-08-08（巡检成品前端历史闭环）

- 工作台现在消费巡检续接返回的权威成品结果，把同一任务的旧失败/暂停行合并成一条已完成记录，避免文件已交付但“查看生产记录”仍显示失败。
- 巡检结果显式携带原生产任务 ID；用户可见记录按该 ID 幂等更新，成品路径、图片数和文案长度保持可追踪。
- 新增失败优先回归；全量自动回归 **355/355**。桌面实机加载 0.15.18 / 扩展 0.2.70 后，同任务历史只剩一条 completed，幂等重放前后成品保持 1 个文件夹、8 个文件。

## 0.15.17 - 2026-08-08（巡检闭环记录同步）

- 单账号多对话巡检完成下载、打包和素材归档后，现在会同步写入生产记录与对话日志，不再出现文件已完成但面板仍停在 66% 的统计失真。
- 巡检账本保存成品路径、图片数、文案和归档路径；再次打开已闭环对话时只重放权威完成记录，不重复下载、不重复打包、不重复归档。
- 新增失败优先回归，针对性回归 **146/146**；完整回归与桌面重放验收见最终记录。

## 0.15.16 - 2026-08-08（长对话文案识别与队列完整性）

- 修复 ChatGPT 长对话中助手正文位于无障碍渲染子树时，清理克隆节点误删全部 928 字文案的问题；清理结果异常过短时回退到当前可见正文。
- 文案检测失败统一标记为 `COPY_REQUIRED` 完整性边界：当前作品未生成 TXT、未下载和未归档时必须停在原帖，禁止跳到下一套或清空队列。
- 第 7 套真实运行暴露并复现上述问题；全量回归 **353/353**。桌面已加载 0.15.16 / 扩展 0.2.68，并从原对话恢复打包 6 图 + 1 TXT，素材使用次数从 2 归档到 3。

## 0.15.15 - 2026-08-08（跨重启旧队列提示词迁移）

- 真实验收发现：重启前已经创建的待执行任务仍携带旧短提示词，0.15.14 只迁移了模式设置，没有迁移持久化队列快照。
- 恢复队列时现在只识别并替换精确的旧默认前缀，保留素材文件夹名称和补充要求；用户自定义提示词不受影响。
- 新增回归先复现失败、再完成修复；针对性回归 **144/144**。第 7 套已停在上传后的安全检查点，加载本版后从当前帖子继续核验，不会重复上传附件。

## 0.15.14 - 2026-08-08（提示词真源与最多 10 张硬约束）

- 修复工作台任务记录与 ChatGPT 实际发送内容不一致：扩展现在按“环节自定义提示词 → 任务提示词 → 安全兜底”的顺序取值，真实发送和对话日志使用同一份任务快照。
- 上传素材的默认提示词明确要求完整读取全部附件和 TXT；素材超过 10 张时先筛选、聚类、合并与取舍，整套计划及成品始终最多 P1-P10，禁止第 11 页、分批和第二批。
- 回复 `1` 前增加计划硬校验：超过 10 页或提出第二批时先要求 GPT 重写；重试后仍不合格则安全暂停，不消耗生图额度，也不上传下一套素材。
- 设置中心的工作流环节改为简洁的“查看 / 编辑”入口；运行中也可查看和修改提示词，修改只从下一套任务生效，当前任务继续使用启动时快照。
- 修复设置页编辑对象错位：保存、删除和拖拽现在绑定“设置中选中的模式”，不会误改当前正在运行的另一种模式。
- 自动回归 **352/352**；桌面实机已加载工作台 `0.15.14` / 扩展 `0.2.67`，现场确认提示词可编辑且包含最多 10 张硬规则。

## 0.15.12 - 2026-08-08（单账号多对话真实闭环）

- 对话准入最终固定为标题规则：包含“模板”或“母版”自动参与，包含“游戏”强制排除；对话正文中的计划、图片和文案特征只用于识别阶段，不能反向授予生产资格。
- 兼容 ChatGPT 重启后先恢复文字、后水合生图轮播的时序，等待真实图片与“下载本组”入口出现后再判断，避免把已完成的 10 张图误判为仍在生成。
- 巡检续接补齐最后一段主链：图片完成后只请求一次发布文案，随后下载本批图片、保存 TXT、创建成品文件夹并归档原素材；完成账本跨重启恢复为“作品已闭环”。
- 清理 ChatGPT 无障碍朗读控件泄漏的“说：”前缀，避免污染文案、图片名和作品文件夹名。
- 历史对话切换后等待标题与语义轮次真正恢复，再执行阶段判断，消除重启后首次续接短暂误报“未命中生产标题”的竞态。
- 真实验收：千岛湖素材完成 10 张图片 + 1 份 TXT 打包，原素材使用次数从 2 归档到 3；全量自动回归 **345/345**。

## 0.15.5 - 2026-08-08（真实生图轮播容器兼容）

- 生图卡计数同时读取缓存图片组、当前下载容器和当前助手轮次，修复 ChatGPT 轮播重渲染后缓存容器失效、真实 10 张仍被识别为 0 张的问题。
- 使用新的桌面/扩展版本生成独立运行资源副本，避免开发中后续修复被同版本耐久副本缓存遮蔽。

## 0.15.4 - 2026-08-08（多对话自动参与与新版生图卡识别）

- 单账号多对话巡检改为标题含“模板”或“母版”自动参与，标题含“游戏”强制排除，并保留按账号手动排除名单；对话内容特征只判断生产阶段，不反向授予生产资格。
- 兼容 ChatGPT 新版 `data-turn="assistant"` 生图卡，按本组下载入口和真实生成文件识别完成状态；修复把“下载 0/10”误当成“生成 0/10”、导致图片已完成却一直等待的真实问题。
- 当前真实验收素材已成功生成第一批 10 张，待加载扩展 `0.2.57` 后继续验证文案请求与闭环归档。

## 0.15.3 - 2026-08-08（单账号多对话安全单步续接）

- “单账号多对话巡检”从纯只读识别推进到显式单步续接：仍先扫描左侧历史，并要求“标题包含模板 + 当前账号人工准入”双重通过。
- 每次点击只打开一个已准入对话、重新读取当前阶段，最多执行一个确定动作；首版仅放行“计划完成后回复 1”和“图片完成后请求完整小红书文案”。生成中、输入框有草稿、额度/脚本风险、阶段不明、下载归档和整批重图均保持只读。
- 对话链接限定为 ChatGPT `/c/` 页面；主进程和网页扩展各自再次校验目标与准入名单，并增加 60 秒重复动作保护、每个对话最多 5 次生图请求账本。
- 测试驱动新增准入、生成中、草稿、上限和不支持动作边界；全量自动回归 **344/344**，四个关键 JavaScript 入口语法检查通过。真实 ZWMRPG 单步测试仍需重启桌面加载工作台 `0.15.3` / 扩展 `0.2.56` 后执行。

## 0.15.2 - 2026-08-08（小猫应用边界与双击交互）

- 根因修复：独立小猫子窗此前无条件启用 Electron 系统级 `floating` 置顶，主窗口仅失焦但仍可见时不会触发隐藏，因此会盖在其他软件上。
- 默认改为应用内模式：工作台与小猫都失焦时隐藏，工作台重新获得焦点时恢复；只有用户显式开启“脱离工作台显示”后，才允许在其他软件上继续显示。
- 单击小猫不再打开助手，改为双击打开；拖动后的双击仍受位移抑制保护。右键菜单和设置中心同步新增“脱离工作台显示”“脱离后始终置顶”，置顶必须以脱离开关为前提。
- 新增设置归一化、原生窗口行为和安全重启桥接回归；全量自动回归 340/340，主进程和前端语法检查通过。原生窗口修改需要重启桌面应用后生效。

## 0.15.1 - 2026-08-08（小猫页面通知与真实头部跟随）

- 小猫通知新增页面归属策略：当前界面默认稳定显示 9 秒并逐条播放；其他界面默认显示 3 秒、同批最多 1 条，相同提醒自动去重，不再因数据重绘连续闪现。
- “电脑作品集储备不足”明确归属内容分发页；在内容制作等其他页面只作一次短提醒，不再永久覆盖本界面状态。
- 桌面小猫拆分“猫是否显示”和“气泡是否显示”，关闭通知不再连猫一起消失。设置中心新增显示小猫、显示通知、动态跟随、两类时长和跨页条数上限。
- 右键小猫直接打开快捷设置；完整设置仍可从左侧底部“设置中心”恢复。头颈层随屏幕鼠标左右轻歪，完整六边形项链归入同一层，身体保持稳定，避免项链断裂和背后残影。
- 新增独立通知策略模块与回归测试；生产队列、账号分区和 GPT 自动化逻辑未改动。
- 验收：全量自动回归 337/337；桌面实机为 0.15.1，右键快捷设置和显示开关均真实生效，重启前后生产队列保持暂停在 2/8，未触发生产或额度消耗。

## 0.15.0 - 2026-08-08（渐进式架构升级基线）

- 采用保留现有入口的渐进式拆分，不重写已跑通的生产主链。后端 8 个业务路由模块继续作为稳定边界，前端开始把可独立测试的运行服务从 `app.js` 抽离。
- 新增 `gpt-runtime-recovery.js` 控制器，接管暂停队列的二次就绪确认；修复旧内联逻辑没有 `await` 二次状态 Promise、导致条件可能永远不成立的问题。
- 新控制器使用依赖注入，不直接持有账号、队列或 DOM 数据；`app.js` 只负责提供当前状态与动作回调，便于后续继续拆分而不改变持久化格式。
- 架构升级前源码备份通过 7-Zip 完整性校验；Git 基线提交 `fa21c4c`，恢复标签 `pre-architecture-0.14.92` 已推送。全量自动回归 **330 pass / 0 fail / 0 skip**；桌面实机返回 `0.15.0`，恢复控制器存在，生产仍暂停在 2/8 且连续调度未武装。

## 0.14.92 - 2026-08-08（小猫连续轻摆修复）

- 实测确认小猫窗口在 120Hz 下没有丢帧；卡顿感来自旧动画把全部左右转向挤在周期最后 12%，长期静止后会突然甩动。
- 原生小猫悬浮窗与工作台内回退小猫统一改为 4.8 秒全周期连续轻摆：上下最多 2px、左右最多 0.6°，并补充 `will-change`、稳定变换原点和背面隐藏提示。
- 当前原生小猫窗口已加载新动画；桌面已安全重启并实际返回 `0.14.92`，重启后队列仍暂停、任务未运行、连续生产未武装，未消耗额度。新增回归测试后全量自动测试 **327 pass / 0 fail / 0 skip**。

## 0.14.91 - 2026-08-08（单账号多对话只读阶段识别）

- 巡检模式新增独立、纯函数式阶段分类器，将当前对话证据映射为：待上传素材、等待计划、计划完成待回复 `1`、正在出图、图片不足待整批重做、图片完成待文案、等待文案、待下载归档、已闭环或证据不足。
- 图片不足只在生成已经停止、实际张数大于 0 且小于计划张数时提示“整批重做”，生成中不会误报；疑似触顶、脚本兜底和证据不足保持只读。
- 只读扫描结果会为“当前正在打开的模板对话”显示阶段标签；其他候选仍只做标题与准入发现，不切换对话、不发送消息、不上传附件。
- 验证：全量自动测试 **326 pass / 0 fail / 0 skip**；桌面已实际加载工作台 `0.14.91` / 内嵌扩展 `0.2.55`，ZWMRPG 当前页只读识别为“待上传素材”。完整自动接管仍未开放。

## 0.14.90 - 2026-08-08（启动素材树按需加载）

- 修复工作台启动时误把 `.暂时不制作` 隐藏分类作为默认分类全量扫描的问题；点开头的素材目录不再参与工作台默认选择。
- 启动阶段只返回轻量分类索引，用户明确点开某个可用分类后才读取帖子、图片和 TXT，不删除登录数据或作品文件。
- 冷启动实测：`/api/dashboard` 从约 11.8 秒 / 11.4 MB 降至约 1.3 秒 / 0.52 MB；`/api/materials` 降至约 0.14 秒 / 3.2 KB。
- 手动分发精确源路径修复已在重启后的真实服务验证：不存在作品集立即拒绝，未生成传输任务；当前设备离线，真实手机签收留待设备在线。
- 验证：全量自动测试 **322 pass / 0 fail / 0 skip**。版本：工作台 `0.14.90`，内嵌扩展 `0.2.54`。

## 0.14.89 - 2026-08-08（真实提示词设置中心）

- 设置中心直接展示当前真正发送给 GPT 的 V4.5 初始化提示词，支持展开、复制、粘贴修改、恢复默认和保存；不再使用只显示版本号的占位区域。
- 每个工作流环节的提示词可以在右侧展开编辑；“上传当前帖子”默认使用“请读取全部附件，不要省略 TXT……”并由程序动态追加当前素材文件夹名称。
- 自定义初始化提示词、模式专属环节提示词和全局补充规则均进入实际任务快照，运行中的作品保持冻结，下一套起使用新值。
- 修复失败任务重试成功后仍残留旧错误文字的问题；成功完成时清理错误码和错误详情。
- 修复移除 Junction 后手动分发仍调用旧发布入口扫描器的问题：工作台现在以自身库存快照为真源，校验作品集阶段和真实源目录后直接发送，接收端提交成功后才移动到公众号阶段。
- 设备传输技能的库存检查不再因 Windows GBK 控制台无法输出警告符号而崩溃；无在线设备时可稳定返回诊断结果。
- 真实生产验收完成 1 套：7 张图、完整小红书文案、7 个下载文件和成品文件夹均闭环；模式切换现场验证人工/选材后/单账号不会回弹。
- 验证：全量自动测试 **321 pass / 0 fail / 0 skip**。版本：工作台 `0.14.89`，内嵌扩展 `0.2.54`。

## 0.14.88 - 2026-08-08（模式切换与重启调度器隔离）

- 修复曾经运行过连续/轮换模式后，切回单账号或人工模式并重启，旧的“连续生产已武装”标志仍可能在后台恢复队列的问题。
- 启动完成后以当前窗口模式为最终真源：只要不是连续模式，就清除旧调度标志和待启动计时器；切换窗口、重连和重启都不能把它重新带回来。
- 增加真实故障对应的回归测试；现有登录、账号禁用状态和队列完整性保护保持不变。
- 版本：工作台 `0.14.88`，内嵌扩展 `0.2.54`。

## 0.14.87 - 2026-08-08（V4.5 初始化提示词单一真源）

- 将用户提供的 V4.5 完整初始化提示词原样纳入独立注册表，记录版本号与 SHA256，避免自动生产仍漂移到旧 V3.6 常量。
- 新账号与需要初始化的新对话统一读取 V4.5；已经调教好的在线对话继续沿用短指令，不重复注入大提示词。
- 设置界面显式显示当前初始化提示词版本；静态资源加载顺序保证注册表先于工作台主逻辑可用。
- 本轮真实作品已完成 6 图、成稿、下载与归档；安全暂停后队列停止，没有继续上传下一套。
- 版本：工作台 `0.14.87`，内嵌扩展 `0.2.54`。

## 0.14.86 - 2026-08-08（安全暂停不再被账号切换抢占）

- 真实只读巡检完成：ZWMRPG 扫描 62 个历史对话，识别 22 个“模板”标题；空准入名单 0 条可接管，单独准入当前 URL 后恰好 1 条。
- 巡检前后当前对话 URL 保持不变，没有点击历史对话、发送消息或上传附件。
- 修复账号窗口被重新附着、巡检或切换时，旧的连续生产标志可能越过已保存的安全暂停并重新启动队列的问题。
- 显式“启动/继续”仍可清除暂停后恢复；普通切换、重连和只读检查只能保持暂停。
- 增加 CDP `accept-dialog` 诊断动作，方便自动验收停止确认框。
- 全量自动测试 `317 pass / 0 fail / 0 skip`。
- 版本：工作台 `0.14.86`，内嵌扩展 `0.2.54`。

## 0.14.85 - 2026-08-08（单账号多对话只读巡检 P0）

- “单账号多对话巡检”增加独立安全准入面板，只在该模式设置中显示，不改变现有生产模式。
- 扫描会在当前账号的 ChatGPT 左侧历史中向下加载更多对话，完成后恢复原滚动位置。
- 对话必须同时满足“标题包含模板”与“工作台显式准入”才能成为后续续接候选；日常聊天不会仅凭位置或最近更新时间入选。
- 首版严格只读：不点击候选对话、不发送消息、不上传附件、不消耗额度；后续阶段识别和单步续接在真实扫描通过后再开放。
- 修复当前对话是否为模板的判断：只读取当前 URL 对应标题，不再因为侧栏其他对话含“模板”而误判。
- 全量自动测试 `316 pass / 0 fail / 0 skip`。
- 版本：工作台 `0.14.85`，内嵌扩展 `0.2.54`。

## 0.14.84 - 2026-08-08（小红书成稿提示词收口）

- 文案请求改为只返回可直接发布的完整成稿：首行真实标题、随后正文、末尾话题标签。
- 明确禁止解释、开场白、总结，以及“标题 / 正文 / 话题 / 标签”等栏目名和 Markdown 标题。
- 已保存的旧默认短提示词会自动迁移；用户自行编辑的个性化提示词保持不变。
- 归档标题解析兜底继续保留，兼容历史对话和偶发不规范回复。
- 版本：工作台 `0.14.84`，内嵌扩展 `0.2.53`。

## 0.14.83 - 2026-08-08（跨账号旧帖边界与作品标题收口）

- 全局多账号自动轮换遇到目标账号的旧未归档帖子时，改为完整性暂停并保留当前素材，不再把素材误标为跳过后继续烧队列。
- 素材归档确认增加 90 秒硬超时；超时停在当前作品边界，恢复时复用已下载图片和文案，不重新生图。
- 作品命名跳过文案中的独立“标题 / 正文 / 话题”栏目标签，使用真实标题创建成品文件夹。
- 真实双账号验收：账号 1 在 `41/45` 时于作品边界让位，账号 4 接管并完成 8 附件、7 页计划、7 图、文案、下载和归档。
- 版本：工作台 `0.14.83`，内嵌扩展 `0.2.52`。

## 0.14.82 - 2026-08-08（归档后刷新超时保护）

- 作品归档完成后的 ChatGPT 页面维护增加 20 秒超时，不再因单个网页刷新卡住而永久阻塞队列推进和账号轮换。
- 刷新超时只记录窗口待恢复状态；已经闭环的作品和队列进度保持有效，调度器继续选择下一个可用账号。
- 新增工作台回归测试，锁定维护请求必须通过 `Promise.race` 受超时保护。
- 版本：工作台 `0.14.82`，内嵌扩展 `0.2.51`。

## 0.14.81 - 2026-08-08（超过十页的首批选择闭环）

- 当 ChatGPT 提示单轮最多 10 张时，不再回复含糊的 `1`，改为明确回复 `先出 P1-P10`。
- 同步把本轮期望图片数收口为 10，避免计划 12 页、实际首批 10 张时产生错误差异。
- 新增首批选择与期望张数测试。
- 版本：工作台 `0.14.81`，内嵌扩展 `0.2.51`。

## 0.14.80 - 2026-08-08（连续作品归档边界闭环）

- 修复同一模板对话中，上一套已经下载并归档后，下一套仍被误判为“上一帖未完成”的问题。
- 扩展会持久记录当前对话最近一次成功归档的素材边界；刷新或重启后也不会重复阻塞下一帖。
- 新增归档边界单元测试，并继续保留未归档作品的硬阻断。
- 版本：工作台 `0.14.80`，内嵌扩展 `0.2.50`。

## 0.14.79 - 2026-08-08（模式切换与暂停队列保护）

- 修复用户主动从“全局多账号自动轮换”切换到人工控制、单账号全自动等模式后，旧的暂停轮换运行态立即把界面弹回轮换模式的问题。
- 主动切出轮换时只挂起轮换运行态，保留轮次、账号游标和恢复信息；再次切回轮换时可以继续，不删除现场。
- 设置或模式变更不再清空暂停中的素材队列、当前索引和手工任务，避免切换界面造成可恢复任务丢失。
- 自动化回归：**308 pass / 0 fail / 0 skip**；桌面模式切换与队列保持仍需重启后的真实验收。
- 版本：工作台 `0.14.79`，内嵌扩展 `0.2.49`。

## 0.14.78 - 2026-08-08（网页静默与原生重试自愈）
- 修复跨重启模式回落：只要持久化的全局轮换仍处于运行、暂停、完整性暂停或额度等待状态，运行态就优先于旧的 `single` 配置；界面与“继续”入口都会保持“多账号全自动”，任务真正完成后才退回用户配置。
- 新增重复输出看门狗：ChatGPT 连续输出 8 次以上同一占位行（例如反复“文案”）时，自动停止异常流并恢复当前素材，不推进队列、不重传下一套。

- 计划素材消息发送成功但 ChatGPT 没有产生任何助手回复时，不再固定等待完整 8 分钟；网页稳定空闲 60 秒后自动续接同一素材，禁止重传附件或推进下一套。
- ChatGPT 显示“出了点问题，请重试”时，即使页面同时残留“停止回答”按钮，也会在 15 秒后只点击一次原生重试，再继续执行计划完整性门禁。
- 模板初始化继续允许没有计划页数的短确认；普通素材计划仍必须得到可解析页数，半截计划不能触发 `1`。
- 第 14/676 套已在账号 2 真实闭环：6 个附件、5 页计划、5/5 生图、528 字文案、5 张下载、TXT 与 JSON 正式归档；账号 1 低产出后由账号 2 安全接管，队列只前进一项。
- 全局轮换设置设计补齐运行轮数、工作时段、额度、恢复参数、会话策略、本地/链接模板、单/多模板分配和 V4.5 候选提示词边界。
- 版本：工作台 `0.14.78`，内嵌扩展 `0.2.48`。
- 修复轮换断点二次初始化与重复上传：已提交到 GPT 的当前素材跨重启时直接恢复原网页检查点，不再新建母版会话、不重复计算上传额度；被旧初始化进度污染的“等待附件”会恢复为“等待迁移计划”。
- 新增生图无首包自愈：发送确认后，若网页结束生成但没有新增助手消息且 0 张图，自动在同一作品内续接图片生成，最多两次；仍失败则以 `IMAGE_COUNT_UNCERTAIN` 完整性边界暂停，不跳下一套。
- 真实验收：第 15/676 套在账号 2 完成 10 图、622 字文案、下载、TXT 和正式归档，队列准确推进到 16/676；第 16 套已完成 6 页计划并自动发送一次 `1`。
- 验证：全量自动测试 **306 pass / 0 fail / 0 skip**。
- 真实耐久补充：账号 2 连续完成第 16、17、18 套（6 / 5 / 10 图）并正式归档；滚动额度到 `42/45` 后，下一套预计 7 图、剩余 3 图，调度器在作品上传前安全等待并切换账号 4。账号 4 已完成母版初始化、素材上传、7 页计划和一次自动确认，首次真实跨账号接管成立。

## 0.14.76 - 2026-08-07（作品边界额度硬保护）

- 新增账号窗口会自动补入账号额度设置，并继承当前默认的 `80 个附件 / 45 张生图 / 3 小时`，修复后来新增的 `account-6` 继续沿用旧 50 张安全线的问题。
- 上传滚动账本改为统计图片与 TXT 在内的全部附件，避免每套 TXT 被漏记后误判还有上传额度。
- 自动模式会在下一套作品开始前同时核对“整套附件数”和“整套预计生图数”；任一无法完整容纳就进入等待/轮换，已经开始的作品仍完整完成文案、下载和归档。
- 新增账号的额度设置会同时热同步到当前页面内存，账号自己的设置面板无需刷新即可出现。
- 额度边界等待会持久化 `nextProbeAt`、附件总数、预计图数和“未提交”标志；重启后自动恢复探测定时器，不会误传当前素材。
- 人工模式不受自动额度边界拦截；真实网页限流和低产出检测继续保留最高优先级。
- 版本：工作台 `0.14.76`，内嵌扩展 `0.2.46`。
- 验证：全量自动测试 **296 pass / 0 fail / 0 skip**。

## 0.14.75 - 2026-08-07（真实计划断点禁止重传）

- 修复真实桌面调用链遗漏 `planSubmitted` 恢复凭据的问题：工作台主进程现在把可信计划断点传给扩展，扩展直接复用当前网页计划并进入确认流程，不再误走附件上传。
- 修正队列 `_submittedToGpt` 标记：计划解析失败重试保持“已提交”，只有真正的发送/附件边界失败才强制干净重传。
- 版本：工作台 `0.14.75`，内嵌扩展 `0.2.45`。
- 验证：全量自动测试 **293 pass / 0 fail / 0 skip**。

## 0.14.74 - 2026-08-07（全局账号轮换与计划断点恢复）

- 新增“全局自动切换账号生产”：只有账号自身选择“全局轮换”的窗口参与；人工控制、单账号全自动、暂时禁用、人工暂停/停止的窗口全部跳过，隐藏窗口不等于禁用，仍可参与。
- 每个账号独立使用 3 小时生图安全线，默认从 50 调整为 45；达到或超过安全线时，必须先完成当前作品的生图、文案、下载和归档，再切换下一个账号，真实网页限额信号仍拥有最高优先级。
- 所有参与账号都在冷却时，持久化等待到最早恢复时间；队列、账号游标和冷却状态支持跨重启续跑。
- 修复不完整计划只出现标题或前两行时被误判完成的问题；计划页数无法解析时保留当前帖子并在同一会话续接，禁止重复上传素材。
- 账号标签右键新增“账号生产与额度设置”，可直接设置该账号的生产模式、安全线与额度参数。
- 版本：工作台 `0.14.74`，内嵌扩展 `0.2.44`。
- 验证：全量自动测试 **293 pass / 0 fail / 0 skip**。

## 0.14.73 - 2026-08-07（账号禁用持久化与7套自动作品集）

- 账号窗口的“暂时禁用”进入 Electron 持久档案，并从旧网页本地状态自动迁移。
- 自动作品集阈值改为 7 套，新作品集直接输出到“抖音小红书”待发阶段。
- 操作脚本改用 `disabled` 真实字段，自动跳过未充值或未登录窗口。

## 0.14.72 - 2026-08-07（运行中热更新保护）

- 扩展源码变化不再立即刷新正在生产的 ChatGPT 窗口；有任务运行时先记录待刷新，任务结束后再统一加载更新，避免开发维护打断上传、生图或归档。
- 单账号连续生产遇到 `PLAN_NOT_READY` / `PLAN_NOT_COMPLETE` 时，会清理网页并对同一素材完整重传两次；仍无完整计划则保留素材、顺延数个队列位后重试，流水线继续处理下一套，不再停整批或丢素材。
- 保留 0.14.71 的当前消息锚点、计划无响应自动续接和 `PLAN_NOT_READY` 队列保护；内嵌扩展版本仍为 `0.2.43`。
- 全量自动测试 **283 pass / 0 fail / 0 skip**。

## 0.14.71 - 2026-08-07（迁移计划无响应自动续接）

- 真实会话确认两条失败任务均为“附件和用户消息已提交，但 ChatGPT 没有生成助手回复”，不是已有计划被关键词漏识别。
- 计划检测严格绑定当前素材消息之后的助手节点，不再依赖可能随网页重排变化的全局会话节点编号，避免把上一套旧产物误当成本轮回复。
- 当前附件消息无回复或回复不是完整计划时，系统会在同一帖子上下文自动补发最多两次续接指令；恢复成功后继续发送 `1`、生图、文案、下载和归档。
- 连续恢复仍失败时才使用 `PLAN_NOT_READY` / `PLAN_NOT_COMPLETE` 暂停当前帖子并保留队列索引，绝不上传下一套覆盖现场。
- 版本：工作台 `0.14.71`，内嵌扩展 `0.2.43`；全量自动测试 **281 pass / 0 fail / 0 skip**。

## 0.14.70 - 2026-08-07（单账号全链路真实验收）

- 真实跑通第一账号窗口：自动上传 6 张图片 + 1 份 TXT，识别 6 页计划，自动发送 `1`，检测 6/6 张成图，请求并接收 792 字文案，下载 6 张图片，写入 TXT 并创建正式作品文件夹。
- 成品核验：6 张 PNG、1 份非空 TXT、1 份 `GPT作品记录.json`；记录状态 `completed`，图片集合哈希和文案哈希完整。
- 下载根目录现在同时授权工作台默认目录和一键作品包配置的 `image_inbox_path`，修复 `D:\Download` 被错误拒绝。
- 修复成功归档后的完成记账引用块级变量 `uploadImages`，避免成品已经生成但队列仍误报暂停。
- 单账号模式支持手动窗口接力：运行中的帖子不跨窗口搬运，完成后下一帖自动交给用户新选中的窗口。
- 修复停止后点击“启动”只清除账号停止标记、未解除队列暂停，导致界面显示启动但不续跑的问题。
- 版本：工作台 `0.14.70`，内嵌扩展 `0.2.42`。

## 0.14.68 - 2026-08-07（自动生产主链阻断修复）

- 修复 `logConversationEvent()` 读取作用域外 `task` 导致自动生产在上传后抛出 `task is not defined` 的阻断错误；当前任务 ID 和素材名改为由工作流显式传入。
- 恢复部分附件上传的安全分类：只有部分文件进入 GPT 输入框时返回 `UPLOAD_LIMIT_SIGNAL` 并暂停，禁止带着残缺素材继续发送；一个附件都没有时返回 `ATTACHMENT_UPLOAD_NOT_READY`。
- 保留 0.14.67 的两项关键行为：空素材文件夹报 `NO_ATTACHMENTS`；新版 ChatGPT 编辑器清空提示词时自动补注入并等待发送按钮。
- 版本：工作台 `0.14.68`，内嵌扩展 `0.2.41`。
- 验证：全量 `npm test` → **272 pass / 0 fail / 0 skip**。

## 0.14.57 - 2026-08-06（移除文件分发 junction link 兼容逻辑）

- **背景**：文件分发现已由软件内直接控制（手机分发→自动移动到公众号→点击使用→移动到已使用），不再需要 junction link（硬链接/软链接）作为平台目录的兼容入口。手势软件直接操作，junction link 是遗留兼容机制。
- **移除的函数**（`distribution-data.js`）：
  - `replaceDirectoryLink`：创建/替换/删除 junction link 的核心函数
  - `syncLegacyLinksForStage`：按阶段同步平台目录 junction
  - `removeMatchingLink`：清理匹配的 junction
  - `ensureWorkflowCompatibilityLinks`：分发前创建兼容 junction（同时从 `server.js` 移除导入和调用）
- **移除的调用点**：
  - `moveCollectionSourceToStage`：不再调用 `syncLegacyLinksForStage`
  - `renameCollectionType`：不再调用 `syncLegacyLinksForStage`
  - `reconcileWorkflowFolders`：不再调用 `syncLegacyLinksForStage`
  - `server.js` 的 `startDistributionTask`：不再调用 `ensureWorkflowCompatibilityLinks`
- **保留的函数**：`getDistributionSnapshot` 仍能读取已存在的遗留 junction（向后兼容已有数据），但不再创建新的 junction。
- **测试更新**：
  - 删除 `lib/replace-directory-link.test.js`（被测函数已移除）
  - 新增 `lib/junction-removal.test.js`（7 个测试 T1-T7）：验证 `moveCollectionSourceToStage`、`renameCollectionType`、`reconcileWorkflowFolders`、`getDistributionSnapshot`、`markOfficialUsed` 在无 junction 环境下正常工作且不创建新 junction
  - `package.json` test 脚本更新：移除旧测试文件，添加新测试文件
- **验证**：全量 `npm test` **268 pass / 0 fail / 0 skip**。
- **测试计划**：详见 `docs/JUNCTION_REMOVAL_TEST_PLAN.md`。

## 0.14.55 - 2026-08-06（GPT 生产测试断言修复 + 测试并发竞态修复）

- **3 个测试断言同步更新**：0.14.50 工作流重构后变量名变更，但 `workbench-ui.test.js` 中 3 个断言未同步：
  - `select.id === "gptProductionMode"` 移除不存在的 `gptProductionModeSetting` OR 条件
  - `expectedImageCount: downloadedImages` → `expectedImageCount: downloadResult.count`
  - `recordWorkbenchQuota(task.entry, "generated", imageUrls.length)` → `detected.length`
- **测试并发竞态修复**：`package.json` test 脚本添加 `--test-concurrency=1`，消除 `node --test` spec reporter 在并发模式下的错误输出溢出问题（从 9 假失败降到 0）。
- **验证**：全量 `npm test` 262 pass / 0 fail / 4 skip。

## 0.14.54 - 2026-08-06（文件分发 EEXIST 修复 + 托盘重启菜单）

- **文件分发 EEXIST 崩溃修复**：`distribution-data.js` 的 `replaceDirectoryLink` 在创建 junction 时遇到 EEXIST（悬空 junction 导致 `lstatSync` 误报 ENOENT，清理逻辑被跳过），现在捕获 EEXIST 后先清除旧 junction 再重试。用 `fsutil reparsepoint delete` 即时清除了发布空间中 2 个悬空 junction。
- **缓存版本不同步修复**：`index.html` 中 `distribution-ui.js` 和 `material-workspace.js` 缓存版本仍停留在 0.14.44，已统一同步到当前版本。
- **托盘右键菜单增加"重启工作台"**：`main.js` 新增 `restartApp()` 函数，自动生产运行时弹确认框防止误触中断。托盘菜单：打开团建工作台 → 暂停自动生产 → 重启工作台 → 彻底退出。
- **新增测试** `replace-directory-link.test.js`：覆盖正常创建、幂等、替换、冲突、删除 5 个场景（沙箱环境 4 个跳过）。
- **导出 `replaceDirectoryLink`**：加入模块导出以支持单元测试。
- **验证**：`node -c desktop/main.js` 通过；全量 `npm test` 258 pass / 4 fail（均为预已存在）/ 4 skip。版本同步测试已修复通过。

## 0.14.44 - 2026-08-05（GPT 生产就绪检测与素材加载性能修复）

- **GPT 生产就绪检测修复**：`main.js` 的 `chatConversation` 检测和 `safeGptUrl` 验证只认 `chatgpt.com`，不认 `www.chatgpt.com`。当 ChatGPT 重定向到带 www 的域名时，`productionReady` 永远为 false，点击"开始自动生产"静默退出不触发。已同时加入 `www.chatgpt.com` 判断。
- **素材加载性能优化**：新增 `/api/materials` 轻量路由和 `getMaterialLibrary` 的 `loadAll` 选项；`app.js` 新增 `loadMaterialCategory()` 函数，GPT 素材区展开/勾选和 `prepareAutoGptQueue` 改用轻量接口，不再每次走完整 `loadDashboard` 重算全量分发/成品/日志数据。
- **好状态保持**：`prepareAutoGptQueue` 的自动选素材逻辑（按使用次数从低到高、跳过隐藏文件夹、只选同时含图片和 TXT 的帖子）不变，只优化了加载路径。
- **回归门禁**：新增 `GPT-PRODUCTION-READY-001` 和 `GPT-MATERIAL-LOAD-002` 两条回归项，记录复现条件、根因、排查备忘，防止后续修改把检测机制和加载路径修坏。
- **验证**：`app.js` 版本号升至 0.14.45；应用已完全重启。桌面实测待用户确认。

## 0.14.41 - 2026-08-05（GPT 设置区紧凑化与账号窗口同步）

- **工作流参数继续压缩**：随机等待模块不再重复显示“随机”，主行直接显示 `最小~最大 秒`；失败重试合并为 `失败重试 最小~最大 秒`，去掉“延迟/秒后”等重复文字。
- **设置区宽屏一行化**：`settings-two-columns` 改为自动多列布局，模式运行参数、工作时段与额度、目录路径在电脑宽屏下优先横向排布，减少大片空白；窄屏仍回落单列。
- **添加环节按钮克制化**：工作流“＋ 添加环节”改为左侧单加号按钮，保留 `aria-label/title`，避免按钮文字占据整行。
- **小猫大小自适应**：小猫主体从固定大尺寸改为 `clamp()` 响应式尺寸，上限收窄，避免当前工作台界面里显得过大。
- **账号窗口管理同步**：GPT 设置里的“账号窗口”管理区打开时会主动刷新桌面真实账号档案，并显示“当前同步 N 个账号窗口”，手动新增/减少账号后不再只显示旧缓存的 1 个账号。
- **分发页视觉修复**：分发统计卡改为居中紧凑；页面标题区右侧动作从两列网格改为横向 flex，避免刷新/设置/帮助按钮错位。
- **深色模式逐项修复**：GPT 工具栏按钮、账号标签、添加账号、左侧导航、服务状态、主题切换和安卓设备图标在 `midnight/midnight-glass` 下使用浅色半透明小边框，不再出现一片黑框或对比不足。
- **流量转化原生骨架**：桌面端 `conversionView` 移除 `conversionAppFrame` iframe，改为原生 `conversion-native-shell`、模块导航、服务状态和 `conversionContent` 挂载点；切到流量转化页时直接调用 `loadConversionHub()` 复用现有搜索、SOP、方案匹配和旅程渲染。
- **小猫透明渲染**：小猫图片元素强制 `background: transparent`，降低深浅主题切换时误判为图片底板的风险。
- **回归测试**：`workbench-ui.test.js` 新增“GPT 设置和助手控件在宽屏下紧凑自适应”“GPT 账号窗口设置跟随真实手动账号列表刷新”“分发页标题动作和统计卡保持居中紧凑”“流量转化桌面页使用原生骨架而不是 iframe 壳”等断言，并增强工作流等待/重试文案断言。

## 0.14.40 - 2026-08-05（纯脚本触顶 mock 工作流与日志增强）

- **mock 场景验证暂停分支**：新增 `mock工作流:纯脚本/沙盒输出会返回触顶暂停决策` 测试，用模拟数据验证 `scriptOutputLimitSignal=true` 会被 `classifyAutomationBoundaryPause` 判定为 `GENERATION_LIMIT_SIGNAL`。
- **主工作流边界接入**：`sidebar.js` 在 `stateSnapshot` 检查阶段调用 `classifyAutomationBoundaryPause`，命中 `script-output-limit` 后直接抛出 `GENERATION_LIMIT_SIGNAL`，暂停当前帖子/窗口，不进入下载、文案、打包、归档。
- **日志增强**：触顶相关关键分支统一输出 `[TB_GPT_LIMIT]`，记录 `nativeImages`、`artifactCount`、脚本/压缩包信号、低图信号、阶段、错误码、风险原因和最近回复片段，方便后续排查真实页面误判。
- **工作流编辑器 UI 修复**：提示词输入改为可点击放大的多行编辑框，并保留“默认/自定义”来源切换；随机等待秒数合并到主行；失败重试参数改为独立参数组；重试和随机等待数字框放宽，避免显示不完整。
- **顶部界面文案与视觉修复**：侧栏改名为“生产（暂停）”“内容生产”“设置中心”；GPT 账号工具栏改为轻量扁平样式，去掉一排黑框感。
- **小猫气泡定位修复**：小猫拖到左侧后，气泡会在内容显示后按真实高度重新贴到小猫旁边，并保持箭头指向正确，不再跑到小猫底部。
- **版本同步**：`VERSION` 与 `src/package.json` 已同步为 `0.14.40`。
- **验证**：`node --check` 三文件通过；`gpt-automation-core.test.js` 19/19；`workbench-ui.test.js` 97/97；全量 `npm test` 245/245 通过。

## 0.14.39 - 2026-08-05（纯脚本/沙盒输出归入生图触顶）

- **纯脚本/沙盒输出也算触顶**：用户确认“没有原生生图,但出现代码解释器、脚本文件、压缩包、批量下载等产物”就是 GPT 生图额度触顶的一种表现。
- **新增检测函数**：`detectScriptOutputLimitSignal` 识别 `nativeImages=0` 且存在脚本/沙盒/压缩包产物的情况,返回 `script-output-limit`。
- **错误码调整**：`script-output-limit` 现在抛出 `GENERATION_LIMIT_SIGNAL`,不再作为普通 `SCRIPT_GENERATED_OUTPUT` 处理;仍然暂停当前帖子/窗口,不把脚本产物当正常图片。
- **状态快照增强**：`generatedOutputRisk` 和 `conversationStateSnapshot` 新增 `scriptOutputLimitSignal`。
- **自动测试**：`gpt-automation-core.test.js` 新增 4 个纯脚本/沙盒触顶测试。
- **验证**：`node --check` 三文件通过；`gpt-automation-core.test.js` 18/18；全量 `npm test` 235/235 通过。

## 0.14.38 - 2026-08-05（PY脚本兜底拼图与低图触顶补充特征）

- **PY脚本兜底拼图检测**：GPT Plus 账号撞到 50 张生图上限后，会用 py/代码解释器拼接垃圾图而非 DALL-E 原生出图。新增 `detectPyScriptFallbackSignal`（有图+脚本特征=PY兜底拼图），在 `generatedOutputRisk` 中标记为 `pyScriptFallbackSignal`，`hardFailure` 包含此信号，`riskReason` 新增 `"py-script-fallback"` 分类。
- **低图触顶检测**：只出 4 张及以下也是撞上限的补充特征。新增 `detectLowImageLimit`（≤4张）。图片检测完成后，如果本轮只出 4 张及以下且计划要求更多，判定为 `GENERATION_LIMIT_SIGNAL` 触顶，不再只当"生成结果不足"跳过。
- **错误消息明确化**：`waitForGeneratedImageGrowth` 中 PY 脚本兜底拼图的错误消息明确指出"PY代码兜底拼接垃圾图而非大模型原生生图，疑似撞到生图上限"。
- **状态快照增强**：`conversationStateSnapshot` 返回 `pyScriptFallbackSignal` 和 `lowImageLimit`，`stateSnapshot` 检查加入 `pyScriptFallbackSignal`。
- **自动测试**：`gpt-automation-core.test.js` 新增 10 个测试用例（5个PY脚本兜底拼图 + 5个低图触顶）。
- **验证**：`node --check` 三文件通过；`gpt-automation-core.test.js` 14/14；全量 `npm test` 235/235 通过。

## 0.14.37 - 2026-08-05（GPT 等待关键词检测）

- **GPT 等待关键词检测**：`wait-plan`、`wait-images`、`wait-copy` 新增可编辑“完成关键词正则”，默认分别为 `计划完成`、`出图完毕`、`文案完成` 相关表达；执行层读取 UI 参数，关键词命中会作为完成信号之一，同时保留原有计划结构、图片数量、文案质量检测。`detect-plan` 的 `pattern` 参数也已从硬编码改为实际生效。
- **关键词模拟测试**：新增 `integrations/gpt-production-extension/gpt-automation-core.test.js`，构造计划、图片、文案三段模拟回复，验证自定义关键词可触发完成检测。
- **版本同步**：`VERSION`、`src/package.json`、`src/public/index.html` 和扩展 `manifest.json` 已同步更新。
- **验证**：新增模拟测试先红后绿；`node --check integrations/gpt-production-extension/gpt-automation-core.js; node --check integrations/gpt-production-extension/sidebar.js; node --check public/app.js` 通过；全量 `npm test` 225/225 通过。

## 0.14.36 - 2026-08-05（作品集空文件夹扫描修复）

- **空作品集过滤**：修复内容分发扫描中“只有空子目录 / 只有 TXT 文案 / 没有图片”的作品集被误判为可发的问题。`inspectSource()` 现在按含图片的子作品目录计数，空目录不会再进入抖音小红书可发库存。
- **全量统计与有限预览分离**：作品集有效性会扫描全部子目录；界面预览仍只返回前 50 个条目，避免大目录因为第 51 个之后才有图片而被误判为空。
- **回归测试**：新增 `lib/distribution-scan-validity.test.js`，覆盖空作品集、空子目录、只有文案、有效图片目录和“有效目录排在 50 个预览项之后”的边界。
- **验证**：`node --test lib/distribution-scan-validity.test.js` 2/2 通过；`node --check lib/distribution-data.js`、`node --check public/distribution-ui.js` 通过；全量 `npm test` 221/221 通过。

## 0.14.35 - 2026-08-05（分发统计布局修复 + 视觉规则沉淀 + Tab 重命名）

- **移除冗余统计卡**：分发设备页面的"待手机分发"统计卡与抖音小红书 tab 内容重复，已移除。保留 3 个统计卡：可信设备在线、陌生设备、进行中任务。
- **统计卡紧凑化**：分发面板内的统计卡从主页面的大尺寸（min-height: 92px, font-size: 28px）覆盖为紧凑尺寸（min-height: 0, padding: 10px 12px, font-size: 20px），减少垂直空间占用。
- **Tab 重命名**：分发页面顶部 tab "抖音小红书" → "抖音小红书可发"，"微信公众号" → "微信公众号可发"，"已发送" → "已全部发送"。Stage header 和 collection filter 同步更新。
- **视觉布局防回归规则**：在 `D:\AICode\AI\memory\开发项目默认交付标准.md` 新增 §1.6，包含 6 条强制检测清单：网格列数与子元素数量匹配、信息不重复、统计卡紧凑性、通知统一、响应式断点一致性、移除元素后清理。

## 0.14.33 - 2026-08-05（储备不足通知改为小猫气泡）

- **通知统一到小猫**：电脑作品集储备不足的提醒从独立的浮动通知条改为通过小猫助手气泡展示，不再占据页面顶部空间。所有通知类信息统一由小猫负责展示。
- **通知内容**：小猫气泡显示"电脑作品集储备不足：精准流量（业务类）只有 X 个，低于安全线 Y 个，请继续批量制作作品集。"，以 warning 语气持续展示。
- **旧通知条隐藏**：`#distributionReserveAlert` 元素保留但始终隐藏，避免影响已有逻辑。

## 0.14.32 - 2026-08-05（微信公众号草稿发布器 UI 重构）

- **批量操作栏分离**：将批量操作栏（全选、模式切换、批量创建）从帖子列表内部移到「02 帖子列表」卡片的独立子头部区域，不再随列表滚动，操作始终可见。
- **按钮尺寸统一**：参考「内容生产」模块的按钮规范，统一了微信公众号草稿发布器内所有按钮的尺寸（min-height: 30px、font-size: 11px、font-weight: 750、border-radius: 9px），包括批量操作栏按钮、账号设置按钮、测试连接按钮、创建草稿按钮。
- **布局比例优化**：侧边栏宽度从 300-380px 调整为 340-420px，最小高度从 400px 提升到 520px，侧边栏网格行高重新分配（作品集 180-240px、帖子列表 280px+），确保两个卡片在同一视口内完整可见。
- **空状态隐藏**：批量操作栏在没有帖子时自动隐藏（`:empty { display: none }`），避免空白区域干扰视觉。
- **按钮文本修复**：创建草稿完成后按钮文本恢复为包含模式标签（"创建草稿（测试模式）"或"创建草稿（正式模式）"），之前错误地恢复为"创建公众号草稿"。
- **回归测试**：102 个单元测试全部通过，浏览器验证三卡片布局正常显示，API 接口正常响应。

## 0.14.28 - 2026-08-04（微信公众号批量草稿队列 + 图片素材复用）

- **批量草稿队列**：新增帖子多选、全选、批量创建草稿功能。用户勾选多篇帖子后一键启动串行处理，实时显示进度面板（百分比、成功/失败/跳过计数、逐项状态）。支持测试模式（dry-run）和正式模式切换。队列持久化到本地 JSON 文件，重启后可恢复。支持取消正在进行的批量任务和清空已完成记录。
- **图片素材复用**：新增 SHA-256 哈希匹配已上传永久素材的函数（`recordMaterialMapping`、`findReusableMediaId`），按账号隔离，避免相同图片重复上传。映射记录持久化到 `wechat-material-mapping.json`。（函数已实现并测试，尚未集成到上传流程，属于下一步任务。）
- **后端 API**：新增 5 个批量队列路由：`batch/create`、`batch/status`、`batch/process-next`、`batch/cancel`、`batch/clear`。
- **前端 UI**：帖子列表新增 checkbox 和批量操作栏；新增进度面板组件含进度条和逐项状态；新增 CSS 样式。
- **测试覆盖**：新增 8 个单元测试覆盖批量队列和素材复用逻辑，全量回归测试 219 个通过。

## 0.14.27 - 2026-08-04（微信公众号草稿接手与账号设置修复）

- **交接存档**：`docs/wechat-draft-publisher-handoff.md` 已补充 TRAE 接手记录，区分已实际验证、仅代码推断和尚未验证，避免把真实公众号接口验收前的能力标记为正式可用。
- **PR #18 本地同步**：已将 `zwmopen/team-video-workflow` 的 `agent/wechat-draft-publisher-v1` 头提交中 `tools/wechat-draft-publisher` 发布器代码和仓库级测试同步到本地源码仓库，作为后续继续开发基础。
- **内容分发入口核对**：当前团建工作台已存在“内容分发 → 微信公众号”界面，可选择公众号阶段作品集，在右侧检查帖子图片、标题、正文并创建公众号草稿。
- **账号设置修复**：`saveWechatSettings()` 改为公众号账号级合并，新增或编辑一个账号不再覆盖其他已配置账号；仍会删除任何传入的明文 `AppSecret`，只保存环境变量名。
- **测试补充**：新增 `lib/wechat-draft.test.js` 并加入 `npm test`，覆盖账号合并和密钥不落盘规则。
- **尚未验收**：真实 3 图 newspic 草稿、999/1000/1001 字正文、微信错误码和后台展示仍需用户提供测试公众号权限后验证。

## 0.14.26 - 2026-08-03（账号窗口暂时禁用功能）

- **右键暂时禁用**：右侧账号窗口标签支持右键菜单"暂时禁用"/"启用账号窗口"切换。禁用后标签变灰（grayscale + line-through），自动化模式（多账号轮换、永不停歇等）自动跳过此窗口，窗口仍可手动点击查看。
- **自动化守卫**：`availableMultiWindowAccounts()` 过滤 `disabled` 账号；`sendNextGptTestTask()` 和 `reconcileGptWindow()` 在连续自动化模式下跳过禁用账号；手动模式和用户主动发起的半自动不受影响。
- **浏览器管理面板**：设置页的账号窗口管理区域新增"禁用/启用"按钮，禁用行以虚线边框+灰度显示。
- **状态持久化**：`disabled` 字段保存在 localStorage 的账号列表中，跨重启保持状态。

## 0.14.23 - 2026-08-03（内容分发系统 6 问题修复）

### P0 - 架构断裂（已修复）
- **P0-1 Junction/文件夹双模式兼容**：`restock_device.py` 的 `find_assets()` 不再强制要求 `all(is_junction(link))`。新增双模式检测：全部 Junction 时走旧模式解析源目录，全部普通文件夹时直接以文件夹为源，混合类型跳过并告警。工作台"移动文件夹"模式与脚本层完全对齐。
- **P0-2 进度解析器适配结构化日志**：`transfer-progress.js` 新增 `parseStructuredLog()` 函数，解析 `[时间] [OP-xxx] [INFO] module: message | key=value` 格式。同时兼容旧版 stdout 明文输出和新版 stderr 结构化日志，确保分发进度条不再卡住。新增 15 个测试用例覆盖所有阶段。

### P1 - 孤岛和空仓（已修复）
- **P1-3 双日志系统关联打通**：`server.js` 在 spawn 脚本时注入 `TRAE_TASK_ID` 环境变量，`logger.py` 将其作为 `wbTask` 追加到每条日志。工作台 `distribution-automation.jsonl` 和脚本 `distribute-YYYY-MM-DD.log` 通过 `wbTask` 关联 ID 打通，排查问题不再需要在两个文件间跳转。
- **P1-4 空仓行动建议**：`--check` 命令在转化类库存为 0 时输出详细摘要（源目录总数、已分发数、可用数、已分发列表、下一个编号建议），直接给出 `作品集_052[转]` 的生产建议。

### P2 - 失衡和遗留（已修复）
- **P2-5 内容比例倒挂分析**：`--check` 新增 `print_content_ratio()` 函数，按渠道（小红书/抖音/公众号）统计泛流量 vs 转化类比例，转化类低于阈值或占比低于 30% 时告警并给出生产建议。当前状态：30 泛 + 0 转 = 0% 转化类，已断供。
- **P2-6 公众号作品集状态核实**：新增 `--verify-official` 命令，交叉比对 `device-usage-log.csv` 和 `official-account-usage-log.csv`，标记每个公众号作品集状态（已上传/手机已用未确认/未使用）。发现 10 个作品集（7 转 + 3 泛）已分发到手机但公众号上传状态未确认。新增 `--mark-official-uploaded` 命令用于标记已上传作品集。

### 其他
- `logger.py` 新增 `get_task_id()`/`set_task_id()` 接口支持工作台任务 ID 注入。
- `transfer-progress.js` 新增 `wbTaskId` 字段提取，与 `remoteTaskId` 独立存储。

## 0.14.22 - 2026-08-03（开发热更新）

- **设置面板合并**：将原"模式与归档"和"自动生产闭环"两个 section 合并为统一的"生产工作流" section。删除了 5 个重复字段（`gptModeConfirmText`、`gptModeCopyPrompt`、`gptAutoConfirmEnabled`、`gptAutoCopyEnabled`、`gptAutoPackageEnabled`）和 2 个隐藏 legacy 字段（`gptConfirmText`、`gptCopyPrompt`）。母版迁移器规则和自定义额外规则移入"生产工作流" section。
- **修复"修改不生效"根因**：`saveGptAutoSettings` 不再用独立字段值覆盖工作流步骤；改为从工作流步骤派生 `confirmText`/`copyPrompt`/`autoConfirm`/`autoCopy`/`autoPackage`，工作流步骤成为唯一真源。
- **实时保存**：删除"保存当前模式"按钮；工作流编辑器的任何修改（文字、启用、超时、动作）通过 `input` 和 `change` 事件实时保存到 localStorage 并同步到 `gptAutoSettings`；模式名称和第一环节也加入实时保存。
- **拖拽排序**：工作流步骤行支持 HTML5 拖拽排序（`draggable="true"` + 拖拽手柄 ⠿）；拖拽时目标行高亮，松手后顺序立即保存；输入框内拖拽不触发排序，可正常选择文字。
- 验证：`node --check src/public/app.js` 通过、全量测试 `183/183` 通过。

## 0.14.21 - 2026-08-03（开发热更新）

- **半自动模式**：新增 `semi-auto` 生产模式——自动上传并发送素材到 GPT，生成迁移计划后暂停等待人工确认。用户审核计划后点"确认继续出图"，自动完成出图→文案→打包归档。适合需要审核计划再出图的场景。
- **半自动模式状态隔离**：发送按钮在等待确认期间禁用并显示"等待确认计划"；切换到其他模式自动清除 pending 状态；暂停/跳过/停止按钮均正确清除 pending，避免"确认继续出图"按钮残留或被绕过。
- **半自动暂停不触发失败告警**：`catch` 路径新增 `isSemiAutoPause` 判断，半自动暂停不弹出"自动生产已暂停"告警、不显示重试按钮。
- **V3.6 母版规则可视化**：设置面板"自动生产闭环"区新增只读 `gptMasterPromptRules` 文本框，显示自动注入到新会话的 V3.6 母版规则全文。旁边新增可编辑的 `gptExtraPromptRules` 字段，用户可追加自定义约束，与母版规则一起注入。
- **连续模式状态增强**：`updateGptAssistantBubble` 增加连续模式运行状态显示——等待额度恢复（显示预计等待时间）、工作时段外（显示时段范围）、永不停歇已就绪/运行中/未启动。
- 验证：`node --check src/public/app.js` 通过、全量测试 `183/183` 通过。

## 0.14.20 - 2026-08-03（开发热更新）

- **模式名称简化**：`单账号单模板，等待循环+手动窗口` → `单账号·永不停歇`；`单账号单模板，等待循环+自动窗口` → `多账号轮换·永不停歇`。下拉框不再截断，右上角模式选择器完整可读。
- **模式动态提示行**：模式选择器下方新增 `gptModeHint` 提示行，实时显示当前模式的简短用途说明，帮助操作员快速理解每个模式适合什么场景。
- **设置面板模式说明按钮**：设置面板的默认生产模式选择器旁新增 `?` 圆形按钮（`gptModeInfoBtn`），点击展开/收起当前模式的详细说明面板（`gptModeInfoPopover`），包含适用场景和操作要点。
- **GPT 视图层级修复**：`showEmbeddedGptView` 新增 6 项前置守卫——非 GPT 标签页、设置面板打开、系统对话框打开、图片大图打开、助手面板打开、历史面板打开、右键菜单打开时均不显示原生 GPT 视图。GPT 网页不再"浮"到分发、转化、工作流等其他标签页之上。
- **延迟回调标签页检查**：`restoreEmbeddedGptView` 的 3 个延迟回调（rAF / 180ms / 700ms）均增加当前标签页检查，防止切换标签后延迟回调把 GPT 视图显示到非 GPT 页面上。
- **设置面板打开消除残影**：`openPageSettings` 改为 `async` 并 `await` GPT 视图隐藏，消除设置面板打开瞬间的 GPT 残影闪烁。
- `GPT_MODE_DEFINITIONS` 新增 `shortName` 和 `description` 字段，供账号标签和说明面板使用。
- 验证：`node --check src/public/app.js` 通过、全量测试 `183/183` 通过。

## 0.14.19 - 2026-08-03（开发热更新）

- **每窗口独立生产模式**：每个账号窗口现在记住自己的生产模式（手动/自动/永不停歇/轮换）。窗口1可设为手动、窗口2设为自动，切换标签时各自恢复——"一个手动一个自动"的架构基础。
- **账号标签模式指示**：顶部账号窗口标签显示当前模式简称（手/自/循/轮），悬停可看完整模式名。
- **暂停后可选素材**（P0-1）：暂停生产时不再锁定素材和模板选择，操作员可以在当前帖安全停在检查点后直接队列下一批。
- **右键菜单不再被 GPT 遮挡**（P0-2）：GPT 原生视图活动时，右键菜单会先隐藏 WebContentsView，关闭后 60ms 延迟恢复，避免对话框过渡闪烁。
- **DPAPI 安全存储修复**：`powershell.exe` 改为 System32 绝对路径，解决测试环境和 Electron 中 PATH 不含 PowerShell 的问题。
- **手动模式防重复上传**：手动任务等待用户发送期间，发送按钮禁用并提示"等待手动发送"；点击事件加 guard 防竞态触发重复上传。
- **账号切换清状态**：切换账号窗口时清除上一个窗口的 `gptCurrentManualTask`，防止"完成当前，上传下一套"按钮串到新窗口。
- 分发快照（junction 硬链接分发）已确认为废弃功能，不再纳入回归测试。当前分发方式以移动文件夹为主。
- 验证：全量测试 `183/183` 通过（0 失败）、`node --check src/public/app.js` 通过、热更新服务器验证代码已更新。

## 0.14.18 - 2026-08-02（开发热更新）

- 切换账号窗口会先恢复该账号自己的额度快照和生产检查点，不再短暂沿用上一窗口的数据；共享额度组按 `quotaGroup` 查询。
- 连续模式遇到网页尚未就绪、桥接或网络暂时失败时，会自动回到当前阶段重试；附件冲突、低产出、解析不确定和真实限额仍安全停住。
- 暂停/停止会清理对应窗口的自动重试计时器，但不会删除队列、设置或 GPT/Google 登录分区。
- 验证：工作台 `194/194`、GPT 扩展 `41/41`、`node --check src/public/app.js`。

## 0.14.17 - 2026-08-02（开发热更新）

- 单账号窗口改为按当前账号窗口恢复：切换顶部账号标签后，继续读取该窗口自己的任务阶段、上传附件数、生图数和额度状态，不再把它当成新窗口，也不会把任务注入到正在查看的其他窗口。
- 新增窗口级“暂停/继续”和“停止/启动”：暂停只停在当前安全检查点，停止终止该账号窗口本轮自动运行但保留队列、设置和登录分区。
- 额度触顶后的首次探测继续按本轮第一次真实消耗时间计算；探测仍失败时改为随机 10–20 分钟自动重试，不需要用户手动点“重试”。
- 轮换模式在切换到下一个账号前显式激活对应浏览器档案，保留一帖一窗口顺序；所有窗口触顶时等待最早探测时间。
- 小猫助手现在显示当前活动账号的近 3 小时上传/生图快照，并在附件真实确认后更新 `已确认收到 x/y 张图片`。
- 工作台回归测试：`75/75` 通过；`node --check src/public/app.js` 通过。未清理 Chromium 缓存，未修改 GPT/Google 登录分区。

## 0.14.16 - 2026-08-02（开发热更新）

- 修复账号窗口保存时丢失当前外部网址的问题；每个浏览器档案现在独立持久化 `lastBrowserUrl`，切换窗口或重启后地址栏恢复各自最近访问页面。
- 保持每个账号窗口独立 Chromium 登录分区；地址栏只允许安全的 `http/https` 页面，顶部主页按钮始终回到 ChatGPT 首页。
- 地址栏提交按钮改为紧凑的向右箭头，保留悬停提示、无障碍名称和回车提交，不再占用“访问”文字空间。
- 永不停歇模式触顶后不再只显示“暂停”：状态栏和小猫助手会显示触发的是上传/生图额度、当前批次已停住、最早探测时间和预计等待时长；队列按钮明确显示“继续（等额度）”，人工继续时再清除该提示。
- 上传任务如果只让部分附件进入 GPT 输入框（例如 1/8），会记录为 `UPLOAD_LIMIT_SIGNAL`，按上传上限处理并显示下一次探测时间，不再误报普通失败或继续塞下一帖。
- 点击小猫打开对话时先隐藏原生 GPT 视图，原生助手浮层在对话期间保持隐藏；对话框和小猫统一使用最高层级，关闭后再恢复 GPT 视图。
- 小猫对话框收紧为约原来一半高度，保留消息滚动与输入执行区，减少对 GPT 工作区的遮挡。
- 统一素材归档目录规则：新归档只写入素材库根目录下的 `1`、`2`、`3`、`4` 等纯数字目录；旧中文目录仍可识别，避免历史素材次数丢失。
- 增加账号窗口地址栏、独立分区、上传部分附件和小猫层级的回归测试，工作台自动化测试 `194/194` 通过；生产扩展 `41/41` 通过。

## 0.14.14 - 2026-08-02（开发热更新）
- 成品路径核对：生产记录继续以实际存在的成品目录为准，暂存目录不再冒充成品路径。
- GPT 作品记录新增 `sourceMaterialPath`、`sourceMaterialName`，保存原始素材绝对路径；有归档证据时为历史记录安全补全，不根据标题猜测。
- 生产历史返回并展示原素材地址，便于从成品反查素材；任务检查点也保存该地址，重启恢复不丢失关联。
- 回归验证：工作台 192/192、GPT 生产扩展 41/41，Node 语法检查通过。

## 0.14.13 - 2026-08-02（开发热更新）

- 新增 GPT 生产完成后的网页维护：一套作品完成并落盘后刷新当前账号窗口；不清理登录状态。
- 新增按账号保存的临时缓存维护计时器，默认每 3 小时调用 Chromium `session.clearCache()` 后刷新页面；明确不调用 `clearStorageData()`，Cookie、localStorage、Google/ChatGPT 登录分区和生产数据保持不变。
- 增加“出了点问题，请重试”等出图阶段通用失败提示的保守触顶识别；网络、离线、超时、登录和验证码错误不误判为额度触顶。触顶/降级信号会暂停当前批次并记录下一次探测时间。
- 生产中到期的缓存维护会延后到当前任务结束，避免刷新造成附件、回复、下载或打包中断。
- 回归验证：工作台 `192/192`、生产扩展 `41/41`，主进程、预加载和扩展 `node --check` 通过。

## 0.14.12 - 2026-08-02（开发热更新）

- 账号窗口地址栏补齐安全导航：支持 `http/https`、无协议域名、localhost/局域网地址，拒绝危险协议和网址凭据；每个账号窗口保存独立的最后访问地址，GPT 登录分区不变。
- 生产模式右键菜单现在可从可见模式选择框直接打开“修改模式设置”；每个模式拥有独立的可编辑环节、发送文字、等待上限和启用状态，并校验上传→计划→确认→出图→文案→打包的安全依赖。
- 工作流编辑器在运行批次期间锁定当前快照，修改只作用于下一批；删除模式配置会恢复默认环节。
- 回归验证：工作台 `191/191`、生产扩展 `41/41`，桌面主进程与扩展副本 `node --check` 通过。本轮未清理 Chromium/GPT 登录状态，也未启动真实生产消耗额度。
- 层级修复：为 GPT 原生视图、页面固定层、小猫助手、右键菜单、系统弹窗、历史记录和大图预览建立统一层级契约；原生小猫设置为 `floating` 子窗口层，避免被 Electron `WebContentsView` 压住。
- 手动生产修正：手动上传不再把“网页脚本”等内部控制语句注入 GPT 输入框；自动模式仅保留最小会话提示，发送、扣 1、等待、下载和打包仍由状态机负责。
- 浏览器语义补充：顶部账号窗口标签切换持久登录分区，底部复用当前账号窗口的单一网页容器；默认 ChatGPT，地址栏可访问其他普通网页，外部网址不会覆盖 GPT 会话地址。
- 新增“单窗口多浏览器轮换模式”：一个账号窗口完成当前帖子后继续使用该窗口；仅检测到网页真实限额、低图数/降级信号时才把未完成帖子切换到下一个可用账号窗口，不按普通轮询乱切账号，不会把下一帖提前塞进当前输入框。
- 轮换状态持久保存当前账号、阻塞账号、下一次探测时间和未完成 requestId；所有账号都触顶时暂停并等待，不清理登录分区或 Chromium 缓存。

## 0.14.10 - 2026-08-02（开发热更新）

- 统一当前三种生产模式与历史别名映射，修复旧 `scheduled` 分支继续影响定时生产计划解析的问题。
- 当前会话生产在未选择实体模板时只上传当前帖子，不重复注入母版提示词；明确启用模板注入时才加载 V3.6 规则。
- 网页执行层增加当前对话状态快照，识别计划、等待确认、图片、文案、脚本/Python 降级和真实限额信号；检测到降级输出时停止当前帖子，不把垃圾拼接结果当成品。
- 独立扩展升级至 `0.2.24`，并同步工作台内嵌清单，消除独立源与运行副本版本漂移。
- 新增持续维护的项目手册与检查清单，记录一帖一窗口、统一打包、状态机、助手层级和发布门禁。
- 修复当前会话复用时误拿旧素材计划、重试仍携带旧附件、ChatGPT 页面无法直接读取 localhost 文件以及附件尚未就绪就提前发送的问题；网页执行器现在通过扩展后台桥接本地文件，并等待全部原生附件卡片与真实发送按钮就绪。
- 真实单账号单帖闭环通过：12 张素材图＋TXT 进入 GPT，计划后只确认一次，首批生成并落盘 10 张 1086×1448 PNG、完整文案 TXT 和作品记录，总耗时 12分48秒，成功素材归档到“已使用一次”。
- 修复 12 页总计划被 GPT 明确拆成首批 10 页后，历史同步把已完成 10/10 工作包误判为残缺的问题；只有工作包记录、实际图片数和磁盘文件三者完全一致时才采用本批完成口径。

## 0.14.9 - 2026-08-02（开发热更新）

- 自动生产将“本轮文案/TXT 已验证”设为最终图片下载、打包和归档的硬前置；文案通过校验后先写入下载目录的 `.gpt-copy-staging/<batchId>.txt`，再下载图片，未取得本轮文案或文案少于最低可见字数时不再误报完成，也不会生成缺少 TXT 的成品文件夹。
- 手动“下载并打包”与自动闭环共用同一文案前置检查；用户可先复制文案或下载 TXT，再执行打包，缺少文案时会明确提示而不是创建半成品。
- 生产记录按真实产物显示路径：有 `packagePath` 时显示“打开成品文件夹”，只有图片暂存时显示“打开图片暂存目录”；不再把没有本地成品的记录伪装为已完成。
- 手动“下载并打包”现在也先把剪贴板文案写入 `.gpt-copy-staging/<batchId>.txt`，再下载当前回复图片；没有文案时不会启动图片下载。生产记录会实际检查成品文件夹中的图片和 TXT，缺项时显示“打开成品文件夹（待核对）”和缺项数量，不再把半成品标成完成。
- 回归验证：网页执行扩展 `32/32`、工作台 `181/181`，扩展语法检查通过；未清理 Chromium/GPT 登录分区、缓存、队列或用户配置。

## 0.14.7 - 2026-08-02（开发热更新候选）

- 手动“下载并打包”和自动闭环改为调用同一工作台打包函数、同一批次号及同一查重链；修复中文控制台输出导致“成品实际存在但界面报未找到”的问题。
- 模板区增加“本地模板 / 在线模板”：在线模板由模板库根目录 `链接模板.txt` 持久保存名称、ChatGPT 会话/分享链接和可选浏览器绑定。
- 账号窗口标签支持拖动排序与右键重命名；生产中切换仅切换查看窗口，当前任务继续绑定启动时的账号窗口，不会误注入。
- 新增“多账号单模板·永不停歇”：每个账号窗口一次只运行一个帖子，各自记录额度与失败状态；一个账号触顶只停止该工作单元，其余账号继续。
- 在线分享模板进入生产前会先续接为当前账号可编辑的对话；无法续接时明确暂停，不把素材发到只读分享页。
- 素材区与模板区统一提供“上传”按钮：本地文件夹只注入当前 GPT 输入框且不自动发送，在线模板直接在当前浏览器打开对应会话。
- 修复 ChatGPT 把完整套图折叠成 `P1…Pn` 文件按钮时只识别到少量预览图的问题；恢复任务会在当前回复外层容器读取完整文件组，不重复上传素材或发送 `1`。
- 真实 Plus 双素材串行验收通过：第一套 7 图＋TXT 完整打包后才上传第二套；第二套从误停断点恢复 7/7 图片、文案、打包与“已使用两次”归档。回归测试更新至工作台 177 项、网页执行扩展 24 项。

## 0.14.6 - 2026-08-01

- 修复 GPT 多图生成首张后的短暂停顿被误判为整组结束；统一使用当前回复完成证据和唯一图片组统计。
- 检测证据不足时暂停当前素材，不再登记额度触顶或跳过到下一帖。
- 真实会话确认本轮已有 6 张唯一图片，旧队列的“仅 1 张”提示属于误报。

## 0.14.5 - 2026-08-01（开发热更新）

- 单窗口自动队列增加单帖子硬边界：一个任务只允许包含一个帖子文件夹内的附件。
- GPT 输入框若仍有未发送附件或未发送草稿文字，整批立即暂停并保留当前任务；刷新网页后也不会重复粘贴提示词或继续叠加下一帖。
- 修复无提示词当前会话模式的变量作用域错误，并把模式名称改为“单窗口（手选自动做）”。
- 真实 Plus 验证完成 6 页计划、单次发送 `1`、仅 1 张图触顶后停止；第二帖未上传，失败素材未下载、打包、归档或移动。

## 0.14.4 - 2026-08-01（开发热更新）

- 本地额度估算彻底改为提醒层：不再在任何自动生产入口抛出“额度不足”并拦截上传；只有 GPT 网页返回真实限额时才暂停。
- 真实网页限额单独记录首次触发时间、原始提示和最早检查时间，避免把本地估算与官方限制混在一起，也避免盲目重试。
- 静态资源版本号更新为 0.14.4，确保已打开的工作台不会继续使用旧的额度拦截脚本。
- 真实限额记录改用本轮第一次实际上传和第一次进入生图阶段的时间分别计算；上传与生图各自维护滚动窗口，最晚时间只用于提醒检查，不把撞限时间当作新的起点。
- 本轮只生成 1–3 张时，按触顶/降级征兆立即停止：残缺素材记为失败并跳过，不下载、不索要文案、不打包、不归档，也不继续发送本批后续素材。
- 下一次自动探测从本轮第一次进入生图阶段起计算，默认 3 小时后用队列中的下一条新素材试跑；定时器可在工作台重启后恢复。
- “单窗口自动（永不停歇）”设为默认模式并补齐持久循环：打开工作台、联网恢复和合盖唤醒后会恢复队列；一批完成后自动选择使用次数最低的下一批素材，触顶后等待 `generationCycleStartAt + 3 小时` 再探测。
- 增加跨午夜工作时段，默认北京时间 07:00 至次日 02:00。休息时段不启动新作品，到点任务安全完成；错过的探测时间会在下次联网/启动且进入工作时段后立即执行。
- 生产模式选择框支持右键直接打开当前模式设置，避免寻找另一个配置入口。
- 内容生产设置增加 Windows 登录后启动工作台开关，默认开启；开发版和正式版分别登记自己的启动路径，不复制或清理网页登录分区。

## 0.14.3 - 2026-08-01（开发热更新）

- 修复单窗口自动生产在本地额度提醒后被错误拦截的问题：单窗口继续生产只提示，不再用本地估算额度阻止用户主动操作；网页真实限流仍由 GPT 页面返回并暂停。
- 修复“继续生成”没有重新上传素材的问题：如果任务在工作台桥接前暂停，继续时会从本地素材树补齐附件并强制重新注入；已经进入 GPT 工作流的任务仍从阶段检查点恢复，避免重复上传。
- 工作台桥接和 GPT 扩展统一传递 `forceUpload`，扩展重试任务也会清理旧工作流状态，避免把“需要重新上传”误判成“只恢复网页阶段”。
- 本次只做开发版热更新，不清理 Chromium/GPT 登录分区、缓存、用户设置或生产数据；正式便携包仍保留旧版本作为回退。

## 0.14.2 - 2026-08-01（持续开发稳定节点）

- 真实跑通 ChatGPT Plus 单窗口自动生产闭环：两套成功样本生成 9 张图片与两份文案，平均单套约 8分10秒；图片不足场景安全跳过，不误打包或归档。
- 修复计划完成后不发送 `1`、网页白屏、任务选择被打乱、生产记录不同步、提示词和下载按钮层级异常等问题。
- 增加全局透明小猫助手，状态气泡、日志和 AI 对话分层；原生子窗口可覆盖 GPT 页面并保持拖动位置。
- 增加拟态浅色、玻璃浅色、拟态深色、玻璃深色四套主题；GPT 网页与小猫助手同步同一色板，去掉重复外壳、阴影与白边，并修正深色侧栏文字对比度。
- 修复 GPT 原生视图四周残留 12px 内缩及“左侧滚动、右侧网页悬浮不动”的割裂感；自动生产页改为固定工作区，只有左侧素材/模板栏内部滚动，GPT 正文底色与唯一 16px 圆角边界跟随主题。
- 设置、图片/TXT 预览和生产记录关闭后强制恢复 GPT 原生视图；登录分区和浏览器档案在开发进程重启后保持。
- 工作台 149/149、网页执行扩展 14/14 自动测试通过，扩展语法检查通过。

## 0.14.1 - 2026-08-01（正式版候选）

- 将生图与文案 API 拆成独立配置：分别保存平台、接口地址、模型和密钥，不再让文案生成依赖当前生图平台。
- 文案平台新增本地 OpenAI 兼容、ByteCat 和 MiniMax 下拉选择，默认 MiniMax `MiniMax-M2.7`；MiniMax 文案使用 OpenAI 兼容的 `/chat/completions`。
- 设置页“保存接口/测试连接”升级为“保存全部接口/测试两路连接”，会分别调用 `/api/image-api/*` 和 `/api/text-api/*`。
- 内容制作的文案模型列表改为从当前文案接口读取；生图平台切换只影响生图，不再顺带改写文案模型。
- 保留旧本机凭据兼容：没有独立文案密钥时可回退到同平台旧密钥；配置为 MiniMax但没有有效密钥时，再安全回退到已有本地兼容文案通道。
- 密钥仍只保存在运行数据的本机凭据文件，不进入普通导出、坚果云备份、界面响应或仓库。
- JavaScript 语法检查、聚焦接口测试及完整自动化测试通过，共 138/138；开发版设置页实测平台切换、默认模型和双接口控件正确，控制台无错误。
- 0.14.1 便携包已构建并真实启动，大小 96,165,146 字节，SHA-256 `099B8E073974799740145DE386E1E8406A60F9C7573C6B41133412A8BED03965`；API 返回 `version=0.14.1`、`channel=便携版`、`desktop=true`。
- 正式包 MiniMax 文案模型列表测试返回 HTTP 401，当前没有可用 MiniMax 文案凭据，不能把真实连通标记为通过；正式 Plus 多窗口生产验收已准备 5 套复制素材，首次启动停在 Windows 网络访问授权窗口等待用户确认。

## 0.14.0 - 2026-07-31（正式版）

- 窗口右上角关闭默认改为退到系统托盘，保留 GPT 页面、登录状态和正在运行的生产任务；托盘提供打开工作台、暂停自动生产和彻底退出，运行任务时彻底退出需要二次确认。
- 删除启动时全量清理 Chromium 缓存的逻辑。GPT 页面首次创建后只做隐藏/恢复，手动刷新或异常恢复时才重新载入；素材树、成品树和其他浏览器档案继续按需加载。
- “添加浏览器”现在创建永久浏览器档案。档案名称、登录分区、额度组、隐藏状态和最后使用时间保存到本机数据文件；隐藏和移除记录默认不删除登录数据，真正清除登录分区是独立危险操作。
- 内容生产增加手动、单窗口自动和多窗口轮询三种模式。多窗口默认并行 3 个、最多 5 个，每个窗口独立记录上传、等待计划、自动确认、等待图片、请求文案、下载、查重打包、归档和完成阶段。
- 自动任务启动后锁定本批素材、模板、账号和任务数量；后续勾选只影响下一批。暂停按钮会变成“继续”，跳过只跳过当前作品，重启后从本机检查点恢复，无法可靠判断时停在人工确认。
- 上传与生图额度改为按账号额度组记录滚动三小时事件；显示逐笔释放时间，并在额度恢复时通过全局助手和系统通知提醒，默认不会自动开启下一周期任务。
- 增加定时启动、并行窗口数、自动确认文字、文案请求词、最低图片数、下载暂存、成品库、闲置网页回收时间及网页内工具开关。当前默认下载暂存为 `D:\Download`，成品库沿用本机真实配置。
- GPT 回复下方恢复提示词库、下载本组、下载并打包、复制文案和下载 TXT。下载与当前回复、当前批次绑定；图片不足或文案为空时不允许冒充打包成功。
- 本地工作包接口支持本次任务传入下载和成品目录，仍复用查重与 `work-package` 链路；只有图片、TXT、批次、数量、查重和成品目录全部核对成功后才归档素材。
- 工作台自动化测试 135/135、扩展测试 11/11、桌面隔离真机布局与核心 API 检查通过。0.13.0 正式版继续保留作为回退版本。

## 0.13.0 - 2026-07-30（正式版）

- “内容生产（自动）”改为工作台调度、浏览器扩展执行：工作台只管理本地素材、模板、账号、额度、队列、归档与备份；真实 ChatGPT 网页内的附件上传、发送、回复检测、图片下载和文案提取继续由同一套扩展能力完成。
- 素材树和模板树统一为可折叠的懒加载文件树。父目录支持三态全选，帖子文件夹是最小生产单位；图片与 TXT 只负责预览。多个父目录可同时展开，目录变化会自动刷新，未展开目录不会一次加载全部缩略图。
- 模板支持多选并按笛卡尔积计算任务：3 个素材 × 2 个模板生成 6 个作品。选择模板时，每个模板只建立一个新会话并依次处理本批素材；未选择模板时沿用当前已沉淀母版的 GPT 会话。
- 手动模式只上传当前任务并填入提示词，停在发送前；自动模式依次完成上传、等待计划、发送 `1`、等待图片、请求小红书文案、精确下载、TXT 写入、打包校验和素材归档。失败可重试当前阶段、跳过当前任务或暂停整批，已经完成的步骤不会重复执行。
- 每个账号使用独立持久登录分区和独立滚动三小时账本。默认估算上传 80 张、生成 50 张，开始前只安排能完整执行的作品，超额任务留在等待队列并显示预计恢复时间；该账本只是本地估算，网页实际限流优先。
- 只有图片与 TXT 均已落盘且批次一致，才允许打包成功并把复制素材移动到“已上传 N 次”。失败、下载不完整或打包失败均不移动；同名目录自动追加短指纹，手动移动后仍可通过内容指纹识别使用次数。
- 设置、帮助、图片和 TXT 预览打开时，原生 GPT 视图自动隐藏，关闭后恢复；修复多次快速切换账号时重复创建 ChatGPT 视图的问题。
- 本地设置导出/导入覆盖路径、开关、账号别名、额度账本、任务和归档记录，但不导出 Cookie。新增仅限本机的 GPT 登录档案恢复点，关闭网页会话并重启后复制完整登录分区，默认不上传坚果云。
- 扩展升级到 0.2.11，支持按失败阶段断点续跑、原生附件与发送确认、异常计划自动纠正、生成图片直接保存，以及小红书文案过短时自动补问并安全暂停；完整 Node 自动化测试 128/128、扩展测试 9/9 通过。
- 使用已登录的真实 ChatGPT Plus 分区完成官网链路验收：3 个真实附件进入官网、模型返回逐页计划、系统自动发送 `1`，随后真实生成并下载 1 张 1086×1448 的 3:4 PNG。图片、本地工作包和测试素材归档均已真实落盘。
- 本次官网连续返回被截断的小红书文案。0.2.11 会自动要求补齐一次；仍短于完整文案阈值时停止打包和归档，不把残缺文本冒充成品。为单独验证本地尾段，使用测试素材原 TXT 完成了一次工作包和“已上传 1 次”归档；该次不计为全自动文案闭环通过。
- 修复自动生产素材区把“尚未展开”误显示为 0 的问题。正式界面现在复用后台全局索引显示 16 个父目录、1941 个帖子；未知数量显示“…”而不是假 0，搜索不会破坏父目录三态全选。
- 正式便携版已构建并启动到 4327，登录后原 ChatGPT 会话和输入框仍存在；升级前完整备份 Electron 用户数据 2409 个文件、371,863,617 字节，更新未更换或清理 GPT 持久分区。
- 工作台自动化测试 130/130、扩展测试 9/9 通过。正式包大小 96,157,448 字节（91.70 MB），SHA-256 `D5C7CBACEBF546CC386E0585D72B1A85F31F9A2BEB670A6D654ED1A28F975F1E`。

## 0.12.2 - 2026-07-30（正式版候选）

- “内容制作（GPT内置测试）”正式更名为“内容生产（自动）”，按左侧已选素材文件夹数量串行生产；一套母版可连续对应多个素材，不再出现“做一套 / 做一批”逻辑。
- 自动流程补齐真实闭环：上传母版与当前素材、等待迁移计划、按设置发送 `1`、等待新图片、请求并复制小红书文案、逐张下载、生成 TXT、校验后打包。任一阶段失败会暂停或按设置跳过，不把计划完成冒充作品完成。
- 每个帖子使用唯一下载批次；本地只接收该批次明确命名的图片，校验图片张数、TXT 和成品目录后才返回成功，解决下载目录旧图串包风险。
- GPT 区新增前进、后退、刷新、首页、多账号标签和账号隔离。每个账号使用独立持久会话分区，互不共享 Cookie、历史和登录状态。
- 页面级设置新增自动确认、自动复制文案、自动打包、失败暂停、生产间隔随机范围、单帖超时和每轮最大套数。随机间隔只用于正常队列节奏；遇到登录、平台验证或限流会暂停，不绕过平台限制。
- 内嵌网页容器补齐圆角边界，生产区新增真实百分比、当前阶段和日志反馈；账号切换、删除和继续生产均有明确状态。
- Node 自动化测试增至 121/121；桌面隔离烟测确认 ChatGPT 登录页、内置扩展 0.2.4、账号标签、四个网页控制按钮和页面设置均可用。隔离账号未登录，因此真实付费生成仍需用户首次登录后完成一套人工验收。
- 正式便携版已构建并启动到 4327，桌面“团建工作台”快捷方式已切换到 0.12.2。旧 0.12.0、0.12.1 发布目录已移入回收站，本地发布目录只保留最新版。
- 0.12.2 便携包大小 96,144,357 字节（91.69 MB），SHA-256 `42D6B01596788C67D19B7DEC2B398BECC2B974D3C654B2CC513DD6F17C7BC82F`。

## 0.12.1 - 2026-07-30（开发版）

- 内容制作下新增“内容制作（GPT内置测试）”：左侧分别展开本地素材与母版，右侧直接承载真实 ChatGPT 网页；登录状态使用独立持久会话保存，不复制 Cookie、令牌或账号密码。
- 一个母版可同时对应多个已选素材文件夹；工作台按素材逐组排队，每组自动附带典型封面/内页母版、当前素材图片与 TXT，并注入长期母版迁移规则，不再提供“做一套 / 做一批”选择器。
- 复用现有浏览器扩展、提示词、下载与本地工作包链路；嵌入态隐藏扩展自身的重复侧栏，只保留真实附件上传、任务结果回传和原生 ChatGPT 生成能力，旧 VBS 仅保留兼容。
- 真实桌面烟测确认 ChatGPT WebContentsView、扩展加载、持久分区、任务桥接与失败反馈均可工作；隔离测试会话未登录，因此登录后的真实附件与生成链路仍需首次人工登录后验收。
- 正式便携版现在自带 GPT 扩展运行快照，不再依赖打包后不存在的相邻源码路径；开发态仍优先读取独立扩展仓库，发布前通过同步脚本更新快照。
- 生产链改为“首图校准优先”：确认计划后只生成第一个素材的 P1 封面，并在成功落盘后强制暂停；用户看图确认后才继续剩余页面与文案。
- 计划和按钮明确显示“仅 1 次调用”“失败不自动重试”和剩余张数，避免勾选多组素材后一次性烧完整批费用。
- 正式生产的付费生图请求关闭 408、429、5xx 和网络错误自动重试；失败停在当前校准图，不再悄悄追加第二、第三次请求。
- 每张成品在 `生产记录.json` 中保存实际请求数、尝试次数、参考图数量、供应商请求编号及供应商返回的 usage（若有）；失败首图也保存尝试记录。
- 首张确认后续跑会跳过已经存在的 P1，只生产剩余页面；失败重试仍只重试首张，不会误放行整套。
- 语法检查、无付费模拟接口、状态机和界面回归共 119 项自动化测试全部通过；未调用真实生图接口。用户确认打通后已构建并验收 0.12.1 正式便携版，更新 4327 桌面入口。

## 0.12.0 - 2026-07-29（正式版）

- 内容生产任务改为可恢复的后台任务：逐页落盘、逐页记录耗时与失败原因；单页失败不再拖垮整套，重启后可继续补跑缺页，也可主动停止。
- 生产区新增真实百分比、当前阶段、最近日志、失败页和结果检查报告；完成后可直接打开作品目录、生产记录与质量报告。
- 一套模板可同时对应左侧多个素材文件夹；作品数量完全跟随已选素材，不再显示“做一套 / 做一批”模式选择。
- ByteCat `gpt-image-2` 改用已验证的 JSON 参考图协议，并把母版与素材压缩为一张轻量参考板，解决标准 multipart 编辑接口无法识别模型和大图请求断线的问题。
- 本机 OpenAI 兼容生图通道与 ByteCat 均完成真实参考图请求；MiniMax 接口会显示真实认证结果，不把失败冒充为成功。
- 每套作品继续输出独立 3:4 PNG、`小红书文案.txt`、`出图计划.json`、`生产记录.json` 和新增的 `质量报告.json/txt`；测试任务支持文件夹前缀，便于与正式作品分开。
- Windows 桌面版目录选择改为现代系统文件夹窗口；路径也可直接粘贴，按回车或失焦后自动保存、扫描和读取。
- 坚果云补齐本机加密凭据、连接检测、工作台小数据备份/恢复与方案目录增量备份；跳过隐藏临时文件，单个文件失败不会立即中断整批。
- 新增生产恢复、技术质量检查、WebDAV 备份、即时停止和桌面真实交互回归；成品会与本次母版/素材原图做文件指纹比对，拒绝把输入原图冒充 AI 成品；自动化测试增至 115 项。
- 正式版仍与 4337 开发端隔离；发布成功后桌面快捷方式只指向 0.12.0，旧发布包清理，不删除源码依赖、素材、成品或运行数据。

## 0.11.7 - 2026-07-29（正式版）
- 流量转化页拆除嵌套卡片壳：取消外层留白、圆角、阴影和独立底色，嵌入内容背景改为透明，与团建工作台共享同一块大背景。
- 保留原有顶部功能标签、转化 SOP、搜索话术、按需求找方案、用户旅程和手机入口，不删减功能。
- 补充同层融合回归测试；完整自动化测试增至 105/105，并在 1280 像素宽度下确认无横向溢出。

## 0.11.6 - 2026-07-29（正式版）

- 内容分发补齐页面级设置、设备白名单、库存阈值、自动检测与自动补货配置；作品集整行点击即可选中，当前任务保留 3 分钟并支持立即清除，完整历史继续写入操作记录。
- 内容制作右侧改为真实成品目录浏览器，按“未打包 / 已打包 / 打包记录”查看帖子文件夹；图片与 TXT 可展开和预览，目录输入支持回车或失焦自动保存。
- 新增右下角“团建中控助手”：固定安全指令直接执行，其他自然语言先由已配置文本模型归类为允许动作；支持能力说明、状态查询、切换页面、检测设备、发送作品、补货、打开设置、触发生产与备份。所有发送仍受设备白名单、防重复和接收确认约束。
- 坚果云备份分为工作台小数据与方案文件增量备份；支持每天、每周或间隔运行，默认每月大文件上传上限 2560 MB，并记录进度、结果和下月续传清单。
- 方案备份源与“流量转化 → 按需求找方案”可使用同一个企微微盘同步目录；本机当前目录为 `D:\AICode\AI\data\01-团建策划-江湖有旅人\01-知识库\WXWork\1688855280811743\WeDrive\江湖有旅人\杭州江湖有旅人户外运动策划有限公司`。
- 修复打包应用访问坚果云时未沿用 Windows 代理环境、只显示 `fetch failed` 的问题；现在可通过系统代理建立连接，并把 401 明确显示为“坚果云应用密码无效”。
- 正式版与开发版继续隔离：桌面快捷方式启动 4327 便携正式版，源码开发使用 4337；正式更新不会覆盖运行数据或用户业务目录。
- 全部 Node 自动化测试增至 104/104，通过助手意图白名单、备份配置、内容分发、成品浏览、路径安全和界面回归。

## 0.11.5 - 2026-07-29（正式版）

- 修复自动分发随机选中作品集后，工作台没有记录脚本实际选中名称，导致手机已确认接收但电脑提示“自动移动失败：作品集名称无效”的问题；现在会从底层完成回执中回填真实作品集名称，再移动到“微信公众号”。
- 已核对并补齐本次真实发送记录：`作品集_050[转]` 未重复发送，原文件夹已从“抖音小红书”安全移动到“微信公众号”，操作记录可追溯。
- 内容分发统一把 `[转]` 定义为“精准流量（业务类）”；库存提醒、筛选、设备补货按钮、分类设置和任务状态不再显示“团建转化”。底层 `[转]` 文件名保持兼容，不改用户已有作品集名称。
- 内容分发设置中的开关控件修复内边距与小屏挤压；1280×720、1024×700 均无横向溢出。
- 自动化测试增至 93/93，通过自动选择名称回填、分段输出解析、真实文件夹流转与界面口径回归。
- 正式便携包补齐 `undici` 运行依赖；已用打包后的独立程序实际启动 4327 服务并确认版本、运行通道和依赖完整，不再出现“Cannot find module 'undici'”后直接退出。

- 完成工作台全界面细节巡检：素材生产、内容分发、流量转化、插件市场与设置页统一使用清晰的 SVG 图标；帮助与设置入口固定为 36×36 正圆，不再被通用按钮高度挤成椭圆。
- 修复设置页 12 栏网格中 API 卡片只占 1 栏而导致文字竖排、按钮挤压的问题；统一标题、路径、标签、设备状态和插件说明的换行与最小字号。
- 修复 1280、1024、900 像素宽度下生产工具栏、筛选按钮、三栏工作区、设置卡片、插件装饰和分发路径的横向溢出。
- 修复“流量转化”外部增强脚本仍访问工作台根 `/api` 的同源代理缺口；正式 SOP 与用户状态现在均从 `/conversion-integrated/api` 返回 JSON，不再误拿工作台 HTML。
- 新增圆形图标、响应式网格、转化脚本代理与兼容回退测试；完整自动化测试 91/91 通过。
- 修复手机端“准话助手”打开后无内容：新增轻量手机入口，不再让手机加载约 10MB 的完整知识库页面。
- 手机与电脑处于同一 Wi-Fi 时，可从工作台“流量转化”右下角的圆形手机按钮复制临时访问地址；手机端支持前端运营/后端转化切换、生成回复、备用回复和复制首选回复。
- 手机入口使用本机随机访问令牌和安全 Cookie；局域网只能访问受保护的准话助手，工作台、素材、设置和其他接口仍拒绝外部设备访问。
- 开发服务继续使用独立端口 4337 和独立运行数据，不影响 4327 正式使用版。
- 已完成真实 MiniMax 回复链路、未授权访问拦截、手机页面显示与交互验证；完整自动化测试 85/85 通过。
- 本轮更新正式使用版、桌面快捷方式、开源仓库与 0.11.5 发布包。

## 0.11.4 - 2026-07-29（本地热更新）

- 修复“开始生产”后计划已经生成、但确认入口被中栏底部绝对定位控件遮住的问题。
- 生产改为清晰的两步主按钮：首次点击生成计划并自动滚到可见区域；随后同一位置显示“返回调整 / 确认并开始生成”。
- 生图平台、生图模型、文案模型、页数和质量压缩为单行紧凑工具栏；计划、确认、进度和日志统一收进中栏底部操作坞。
- 新增确认入口位置、按钮状态切换、滚动定位与紧凑模型栏回归测试；完整自动化测试 79/79 通过。
- 使用本地 GPT 生图通道完成 1 套 9 页真实生产：全部输出 1200×1600 PNG，并附小红书文案、出图计划和生产记录；总墙钟耗时约 15 分 20 秒。
- 本轮仅更新本地源码，不重新打包；桌面源码启动器直接读取 0.11.4。

## 0.11.3 - 2026-07-29（本地热更新）

- 生产页新增阶段、百分比、进度条、当前状态和最近日志，并将生产控制区固定在中栏底部，普通窗口尺寸下始终可见。
- 所有关键状态增加统一的小圆点提醒；运行状态增加轻量呼吸动效。
- 压缩提示词和本次要求输入区，提高小字可读性；目录按钮统一简化为“选择 / 应用”。
- 新增生产状态和紧凑按钮回归测试，工作台自动化测试增至 74 项并全部通过。
- 本轮仅更新本地源码，不重新打包、不推送发布；真实生图仍等待可用的外部生图接口或凭据。

## 0.11.3 - 2026-07-28

- 流量转化改为工作台同源模块入口，不再暴露跨端口独立应用；外层可读取完整页面结构，便于直接点击、标注和维护。
- 原转化侧栏横移为内容区顶部标签，只保留搜索话术、转化 SOP、按需求找方案和用户旅程四个主任务。
- 聊天记录来源与知识入库归入搜索页，方案来源归入找方案页；两个来源目录独立保存、独立扫描，互不影响。
- 知识入库明确显示写入本机 `人工确认知识.json`，确认后才重建搜索索引。
- API 与模型设置从生产页移至工作台全局设置，供所有模块统一调用。
- 本轮继续使用源码热更新，不构建便携包；自动化测试 71 项通过。

## 0.11.2 - 2026-07-28

- 以“江湖团建转化助手”的拟态与克制玻璃视觉令牌作为全局唯一主题真源，统一素材生产、分发、插件和设置页面。
- 侧栏主题入口收敛为一个循环按钮，点击依次切换“拟态 → 玻璃 → 拟态”，不再显示两枚并排主题按钮。
- 外层工作台通过受限 `postMessage` 同步嵌入的流量转化主题；嵌入态隐藏其重复主题入口，但不覆盖或删减原应用功能。
- 本轮继续使用本地源码热更新，不重新打包。

## 0.11.1 - 2026-07-28

- 重整工作台自身的设置页，收敛为工作目录、备份与恢复、生产与分发数据、软件与诊断四块。
- 删除与侧栏重复的主题卡、与插件市场重复的 GPT 扩展卡，以及旧一键工作包、合集数量、自动压缩、发布包目录和历史工具入口。
- 设置页新增素材目录选择按钮；保存目录时保留既有后台成组参数，不再让已删除的旧控件覆盖配置。
- 桌面采用两列紧凑布局，窄窗口自动回落为单列；本轮继续使用本地源码热更新，不重新打包。

## 0.11.0 - 2026-07-28

- 产品更名为“团建工作台”，顶层导航移除重复的“作品集”，新增“流量转化”。
- 顶层同时移除“总览”；旧总览与作品集状态会迁移到素材生产。
- 流量转化保留团建工作台外层侧栏，右侧完整嵌入江湖团建转化助手原版界面；搜索话术、转化 SOP、需求方案、知识库完善、设置和用户旅程均不删减，只随内容区自适应缩小。
- 界面按江湖知识库原实现移植：168px 文字侧栏、无顶部横栏、冷灰蓝拟态面板、深灰主按钮、底部服务状态与“玻璃 / 拟态”切换。
- 素材生产、分发、转化、插件和设置均保留；生产三栏在 1280px 桌面窗口内不再横向溢出。
- 新增轻量产品初稿 `docs/PRODUCT-BRIEF.md`，后续开发先确认用户任务、数据真源、界面骨架、完成标准和验收场景。
- 桌面新增“团建工作台”源码快捷方式，继续采用本地热更新，不构建新便携包。
- 增加“原版转化助手完整嵌入、禁止渲染删减版转化 DOM”的回归测试；自动化测试 70 项通过，实页逐项验证原版六个转化栏目可用。

## 0.10.3 - 2026-07-28

- 左侧素材区明确为“素材分类文件夹 → 帖子文件夹 → 图片与 TXT/Markdown”三级结构；每一级都由用户点击展开，不再一次铺开整个分类的所有图片。
- 文件夹名称始终显示；帖子展开后，图片和文本附件在该帖子文件夹下方并排显示，图片不显示文件名，点击图片查看大图，点击 TXT/Markdown 查看完整正文。
- 选择框只保留在帖子文件夹上，图片和文本附件不再出现选择框；素材根目录继续自动扫描，无需“读取”按钮。
- 中间区彻底移除“一套/一批”模式表达，只显示当前模板规则、模型选择和已选帖子数量；实际处理对象完全由左侧帖子文件夹勾选决定。
- 生图结果统一标准化为 1200×1600 的精确 3:4 PNG，并记录模型原始尺寸；校准提示词禁止虚构页码或总页数。
- ByteCat 真实复测遇到上游连接超时；系统已完成三次自动重试，并把最终失败改成可读的 502 提示，保留当前素材与模板选择供直接重试。
- 本轮仍为本地开发热更新，不构建便携包；桌面图标继续启动源码版。

## 0.10.2 - 2026-07-28

- 素材与模板改为文件夹内联展开：点击条目后，缩略图直接在其下方横向排列，不显示图片名称；点击缩略图弹出大图。
- 素材根目录启动后自动扫描，上次使用的分类自动读取；切换分类或素材根目录时直接扫描，不再要求额外点击“读取”。
- 中间生产区移除模板图片预览和“做一套/批量做”提示，只保留母版规则、本批要求、页数、质量与模型控制。
- 主生产区新增生图平台、生图模型、文案模型三项独立选择；平台切换后读取该平台实际模型列表，图片与文案可以使用不同模型。
- 生图接口对 408、429、500、502、503、504 和短时网络故障自动重试，失败信息改为可读提示。
- 本轮只更新本地开发源码与桌面源码入口，不构建便携包。

## 0.10.1 - 2026-07-28

- ByteCat 密钥切换到 `image` 生图分组，默认模型改为 `gpt-image-2`。
- 完成真实参考图编辑校准：返回 1024×1536 PNG，并写入 `API生产待审` 和生成记录。
- 修复 ByteCat 图片分组无法调用文本模型的问题：图片继续走 ByteCat，标题与小红书文案自动走已验证的本地 OpenAI 兼容文本接口。
- 保持密钥只存于本机运行数据，不进入源码、日志、诊断信息或发布包。

## 0.10.0 - 2026-07-28

- 素材生产页改为三栏直线工作流：左侧选择素材和模板，中间对话生产，右侧查看成品并打包待发。
- 素材卡片显示图片数、文案、母标签与使用次数，支持多选、当前筛选全选和常用标签筛选。
- 模板增加“转化模板 / 游戏模板”一级分类、具体模板二级选择、封面/内页缩略预览与用途说明。
- 每个模板保存独立生产规则和对话历史；本次口述要求会自动合并进真实生产计划。
- 成品区同时读取 API 待审区和正式成品库，支持待审、待打包、已打包筛选和多选。
- 新增真实待发打包接口：复制完整作品文件夹到 `抖音小红书`，同名不覆盖并写入操作记录。
- 新增本地游戏模板资产识别；保留既有生产计划、逐页生成、文案生成、文件夹分发和防重复能力。
- 新增 ByteCat Image 2.0 配置入口和独立本机密钥存储；当前密钥分组尚未返回图片模型，因此只标记为“已配置、待校准”，不把充值或接口连通误报为生图可用。
- 自动化测试 63 项通过；最终便携版桌面烟测通过三栏生产、素材分类切换、游戏/转化模板、成品打包入口、作品集、分发、插件市场、帮助和两套主题。

## 0.9.9 - 2026-07-28

- 整体视觉收敛为“拟态 / 玻璃”两套主题，复用江湖知识库的蓝灰、低对比、柔和阴影语言；移除侧边栏黑色选中态。
- 侧边栏新增插件市场，集中展示 ChatGPT 作品助手、团建 GPT 数字作品生产助手、团建转化助手、素材下载与本地脚本，并提供本地启动、安装包、发布页和开源仓库入口。
- 所有主界面帮助入口由问号改为“说明”，内容补齐开发背景、方法论、真实数据来源、操作步骤与风险边界。
- 设置页新增坚果云备份：可安全沿用人生游戏系统的 WebDAV 配置，凭据使用 Windows 当前账户加密保存；备份设置、提示词、任务索引、设备备注、分发记录和防重复账本，不上传素材与成品大文件。
- 真实桌面冒烟覆盖 5 个主导航、插件筛选搜索、两套主题、详细帮助弹窗、目录分发与在线设备；本轮检测到 2 台在线设备。
- 本机人生游戏系统的坚果云配置已导入，但真实服务当前返回 HTTP 401；界面明确显示失败，不伪装成备份成功，更新应用密码后可重新导入。

## 0.9.8 - 2026-07-28

- 分发页顶部统一为“设备 / 抖音小红书 / 微信公众号 / 已发送 / 操作记录”，后三个库存标签直接对应真实文件夹。
- 抖音小红书和微信公众号新增“泛流量帖 / 精准流量帖 / 未分类”分类标签与实时数量。
- 每个作品集可在分发页直接修改分类；系统同步增删文件夹名中的 `[泛]`、`[转]`，同名冲突停止且不覆盖。
- 文件夹分类改名、移动到公众号、压缩归档并删除源目录都会写入本地操作记录；文件夹继续作为最终事实真源。
- 各主界面右上角新增问号帮助入口，悬浮显示简要说明，点击查看当前页面的真实数据来源和操作边界。
- 自动化测试增至 62 项，桌面实机烟测覆盖五个分发标签、三类作品标签、帮助入口、设备选择与核心 API。

## 0.9.7 - 2026-07-28

- 分发阶段改为直接读取真实目录：`抖音小红书`、`微信公众号`、`已发送`；资源管理器和工作台刷新后双向一致。
- 新增按历史记录整理：未发送作品进入抖音小红书，手机已接收作品进入微信公众号，已有有效已发送压缩包的残留原文件夹会安全清理。
- 三端发布完成后的最终态改为“已发送压缩包”：先生成并校验 ZIP，再删除原作品文件夹；压缩失败或同名冲突时保留原文件夹。
- 旧发布空间继续作为手机传送兼容入口，整理或流转后自动修复 Junction，现有设备传送逻辑不丢失。
- 界面同步显示三条真实路径、阶段数量与归档动作；空作品文件夹不允许误归档。
- 新增真实目录双向同步、历史迁移、压缩后清理、冲突保护等自动化测试。

## 0.9.5 - 2026-07-28

- 素材生产页新增同屏本地 API 生产区：直接设置生图平台、接口、模型、本机密钥、1/2/6 张数量、质量档和本批提示词，不再把生图入口藏在设置页。
- 接入本地 OpenAI 兼容图像网关与 MiniMax 双平台；本地 `gpt-image-2` 已完成真实模型发现、参考图上传和出图验证，MiniMax 当前密钥校验失败时会明确提示，不伪装成可用。
- 固化 A 类母版、B 类素材、C 类历史结果身份隔离，以及事实锁、江浙沪、10 人起接、无价格不写价格、独立 3:4、先计划确认再校准等规则。
- 校准图只写入运行数据的 `API生产待审`，不进入正式成品库、不增加完成数量；封面和典型内页分开生成与检查。
- 本机密钥迁移到运行数据的受限 secrets 文件，接口不会向前端回传密钥明文，仓库、日志和发布包均不包含密钥。
- 真实“径山素材 × 备忘录母版”端到端校准通过，返回 1086×1448 独立图片；事实锁修正了首次试验中擅自增加露营场景的问题。

## 0.9.4 - 2026-07-27

- 移除桌面应用右侧 ChatGPT 内嵌浏览器与多账号登录壳，工作台恢复为单一完整界面；生产协作统一打开系统受信任的 Chrome/Edge 并交给独立扩展。
- 素材生产页新增“选模板 → 选素材 → 生图迁移 → 作品与文案”四步 API 流程条，并提供不落盘密钥的接口/模型设置入口；实际生图执行仍待用户确定服务后接入。
- 设备发现改为合并本轮结果与 10 分钟内最近发现，并把最近状态持久化到运行数据，降低移动端后台节流导致的在线设备闪退。
- 设备卡片和选择设备窗口新增 Wi‑Fi、USB、远程连接方式图标与状态标签。
- 局域网发现结果明确标记为 Wi‑Fi；USB 备用能力、USB 实际连接和远程待接入不再混成笼统在线状态。
- 传送任务从底层完成结果读取并显示实际传输协议。
- 继续以物理阶段文件夹、独占 claim 和包含传输协议/接收确认的本地 CSV 账本三层防重；已有手机发送记录的作品集不会重新进入待发送库存。

## 0.9.3 - 2026-07-27

- 作品集页面收敛为“抖音、小红书 / 公众号 / 已使用”三个顶部阶段标签，不再要求理解平台状态筛选。
- 手机确认接收成功后，原始作品文件夹自动移动到公众号目录；公众号人工标记后真实移动到已使用目录。
- 三阶段直接读取磁盘目录：在工作台操作会改变文件夹，在资源管理器中移动文件夹后工作台刷新也会同步变化。
- 没有 `[泛]/[转]` 标签的新作品仍保留在第一阶段，并以“待补分类”禁用发送，不再形成界面外的隐藏状态。
- 每张作品卡只保留打开作品和当前阶段主动作，继续保留内容类型、设备选择、传输确认、日志与防重复规则。
- 新增真实目录双向流转、物理目录识别和同名冲突保护测试，自动化测试增至 47 项。

## 0.9.2 - 2026-07-27

- 新增后台全库素材索引，逐分类扫描并持久化紧凑标签、次数、哈希和全局统计，不恢复启动时同步全库扫描。
- 自动读取 `素材链接记录.csv` 与 `制作日志.csv`，按生产事件去重后回填素材使用次数。
- 历史路径失效时只接受唯一文件夹名称匹配；多候选和未匹配记录进入待核对，不自动猜测。
- 历史回填幂等，同一证据重复刷新不会重复加次数；扩展后续真实发送继续在同一计数上累加。
- 元数据修改会同步更新持久化全库索引，真实文件夹移动后自动后台重建索引。

## 0.9.1 - 2026-07-27

- 素材读取改为真正的分类懒加载：首页只加载当前分类，扩展初始接口只返回顶层分类；展开其他分类时才递归扫描。
- 本机实测首页数据由约 21 MB / 42 秒降到约 0.37 MB / 1.1 秒，扩展分类索引约 3.7 KB / 22 毫秒。
- 设置页改为单页紧凑分组，主题从 6 个重复选项精简为 3 个明确模式。
- 提示词入口继续由浏览器扩展独立挂载，不依赖已停用的左侧会话树。

## 0.9.0 - 2026-07-27

- 为每个有效素材文件夹生成独有且可随同卷移动保持稳定的 SHA-256 身份；两份内容相同的文件夹仍是两个身份。
- 新增素材元数据台账，按文件夹哈希保存唯一母标签和使用次数，改名或移动后仍可恢复。
- 母标签限定为“团建游戏 / 团建转化 / 合集攻略”；聚会游戏、破冰游戏等同义关键词统一归入“团建游戏”。
- `/api/materials` 向扩展返回文件夹哈希、母标签、标签来源和使用次数；新增 `/api/extension/material-metadata` 供扩展人工改标签或增加次数。
- 文件夹身份与内容查重分层：唯一文件夹哈希绑定标签，图片与文案聚合指纹继续只负责内容防重。

## 0.8.9 - 2026-07-27

- 为独立 GPT 生产扩展新增受控文件夹移动接口：只允许在同一授权素材库或成品库内部移动真实文件夹。
- 拒绝移动库根目录、普通文件、软链接、跨库目标、自身子目录和会覆盖同名项的操作。
- 移动成功后清理素材扫描缓存，扩展刷新即可看到真实新位置。
- 新增真实临时目录移动及安全拒绝回归测试；Node 测试增至 40 项。

## 0.8.8 - 2026-07-26

- 素材使用台账增加内容指纹，防止素材改名或移动后被重复生产。
- 指纹按图片和文案内容计算，不依赖文件名。
- 仅在选中上传时计算，避免后台全库哈希造成卡顿。

## 0.8.7 - 2026-07-26

- 增加素材使用台账：区分已加入 GPT 与已真正发送。
- 台账只记录路径、状态、会话与时间，不移动或改名原素材。
- 素材接口返回使用状态，供拓展阻止重复上传。

## 0.8.6 - 2026-07-26

- 增加拓展专用完整成品目录树接口，不再用手机分发筛选结果代替本地文件浏览。
- 增加拓展工作包接口，由工作台直接执行现有 PowerShell 脚本，默认不再唤起 VBS。
- 增加目录越界保护、完整目录可见性和直接工作包链路测试。

## 0.8.5 - 2026-07-26

- 修复素材库顶层分类被重命名或删除后仍复用旧递归缓存的问题；扩展不再显示已经不存在的帖子路径。
- 素材缓存增加顶层目录签名校验，分类变化会自动失效并重新扫描；新增分类重命名回归测试。
- 完成真实 Edge + ChatGPT 扩展联动验收：5 个本地附件进入真实会话、生成 5 张图片、真实下载 5/5。
- 下载事件写入本地防重复台账，包含落盘路径、来源会话、文件大小与完成时间。

## 0.8.4 - 2026-07-26

- 修复真实 ChatGPT 页面访问本机目录接口时被浏览器私有网络访问策略拦截、扩展显示 `Failed to fetch` 的问题。
- 本机扩展协作接口现在为允许的 ChatGPT 与浏览器扩展来源返回私有网络预检许可；其他来源仍然拒绝跨域读取。

## 0.8.3 - 2026-07-25

- 工作台与“团建 GPT 数字作品生产助手”拆成两个独立项目；工作台继续承载本地数据和执行接口，扩展专门服务“生产数字作品”SOP。
- 新增扩展协作接口：读取当前素材/成品路径、作品集与作品、素材分类与帖子文件，并允许保存用户粘贴的路径设置。
- 生产防重复改为只读汇总既有作品历史库：当前 193 组图片均具备整组 SHA-256 与 64 位 dHash；精确哈希用于确定重复，视觉哈希只做近似预警。
- 分发防重复仍由手机组、公众号组、claim、使用日志和入口状态独立管理，不再把旧文本哈希误称为当前生产历史。
- 设置页新增生产历史覆盖状态与独立扩展项目入口；默认主题保持用户指定的“拟态悬浮”。
- 扩展协作接口只向真实 ChatGPT 域名和已安装浏览器扩展开放，不给任意网页开放整个中控 API。
- 修正发布级实机测试在“泛流量包”筛选下误查设备行的问题；34 项 Node 测试和完整桌面交互回归均通过。

## 0.8.2 - 2026-07-25

- 修复便携桌面版主工作区只占顶部、下方大面积白屏的问题，素材树和真实 ChatGPT 网页现在始终铺满窗口。
- 设置新增当前版本、检查版本、打开发布包目录、打开运行数据和复制诊断信息；便携版会打开真实 EXE 所在目录。
- 增加应用单实例保护，连续双击只保留一个窗口，并唤醒已有窗口。
- 修复点号开头但合法的 `[转]` 作品集被漏算，工作台库存与原分发 Skill 统一为 2 个团建转化包。
- 普通文件发送终态统一为 100%、`completed` 和“发送完成并确认接收”。
- 素材库加入运行时完整缓存，日常主数据读取由约 18–21 秒降至约 1.2–1.4 秒，手动“刷新文件树”仍会执行完整递归扫描。
- 将现有 ChatGPT 用户脚本及工作包 PowerShell/VBS 同步为应用内平行副本；个人会话历史只导入本机运行数据，不进入公开仓库或发布包。
- 新增真实桌面逐项验收：11/11 个素材分类、21/21 个作品集、图片/文本预览、三套分发子页、设备联动、应用内弹窗和六套主题全部通过。
- 通过应用接口向在线 1 号设备实际发送 165 字节验收文件，接收端提交成功。

## 0.8.1 - 2026-07-25

- 正式提供 Electron 便携桌面版；应用可自行启动本地服务，不依赖浏览器调试窗口。
- 素材生产右侧使用真实 ChatGPT 网页，支持持久登录、Google 授权弹窗和 3 个隔离账号空间。
- 生成桌面快捷方式“团建内容工作台”，浏览器版继续只作为调试与兼容入口。
- 分发设备行按钮统一为单行紧凑文字，修复“补泛流量”等按钮中文被拆行溢出。
- 作品集折叠箭头改为嵌入整行背景；列表/网格改为筛选面板右下角的单一低调图标。

## 0.8.0 - 2026-07-25

- 删除暂时不用的“发布复盘”导航、总览入口与整页，旧标签会安全回到素材生产。
- 作品集改为直接读取可粘贴、可选择的本地目录，并按文件夹名 `[泛]`、`[转]`、无标签分类。
- 删除小红书单平台筛选、全部已使用、搜索框及下方重复台账详情，只保留当前真正需要的筛选和作品树。
- 作品集增加紧凑列表/网格视图、所有合集独立展开、3:4 缩略图和图片 + TXT/Markdown 双预览。
- “拟态悬浮”升级为新安装和本次默认迁移主题。
- 桌面素材生产将真实 ChatGPT 网页继续放在右侧，新增本地素材文件与生产指令投递桥接；三个账号空间仍相互隔离。
- 兼容读取现有作品集目录和旧工作包历史，不修改原 VBS、PowerShell、油猴脚本或个人历史导出。
- 自动化测试增至 31 项。

## 0.7.0 - 2026-07-25

- 作品包分发由同步等待升级为可恢复任务：显示核对、设备响应、发送百分比、接收确认、写入记录和完成状态。
- 分发任务和普通文件任务统一显示在分发页，支持安全停止；刷新页面后可恢复当前及最近任务。
- 作品包选择设备后改用系统内二次确认，明确显示作品包、目标设备、内容类型和手机组使用规则。
- 删除前台关于软链接、Junction 等内部实现的提示；兼容层继续并行工作，但不干扰日常操作。
- 全站原生确认框、警告框和重命名输入框替换为统一的系统弹层，六套主题下保持一致视觉与可读状态。
- 接口错误统一转换为可读提示，不再把原始 JSON 或脚本输出直接弹给用户。
- 新增传输阶段解析测试，自动化测试增至 30 项。

## 0.6.0 - 2026-07-25

- 分发统计统一按当前真实可分发列表计算，统计卡与下方作品包数量保持一致。
- 设备状态改为后台低频自动刷新，始终显示“当前设备在线 N/总数”；刷新时只显示小型旋转图标。
- 在线设备置顶，离线设备明确禁用补笔记与上传；设备名称可直接编辑本机备注。
- 泛流量和团建转化卡可直接切换真实作品包列表；每个作品包独立选择当前在线设备，发送前保留最终确认。
- 设备选择弹窗统一使用 Android / Apple 图标、设备名称和“当前在线”状态，删除平台与作品数等重复信息。
- 新增“上传其他”：可选择文件或文件夹、拖放到在线设备、查看实时进度并取消指定传输任务。
- 指定作品包分发继续复用 `device-folder-transfer` 的真实传输与接收确认；成功后只删除已校验的活动平台 Junction，不再创建抖音归档入口。
- 增加三层防重复：发送前独占占用记录、接收成功 CSV 日志、活动 Junction 删除；已有成功记录的作品即使入口被误建回来也拒绝再发。
- 手机库存改为组级互斥：只有小红书与抖音两个同源入口同时有效才在前端显示和参与底层抽取；任一手机平台使用后整组退出，公众号资格独立保留。
- 公众号页改为“公众号补笔记”，加入“打开公众号官网”，状态统一为“公众号可用 / 已打开过 / 上传已完成”。
- 素材文件树改为紧凑列表，删除文案摘要，增加列表/小图标视图切换；“传 GPT”改为圆润紧凑按钮。
- 浏览器调试版删除仿制任务卡；桌面版素材生产右侧继续显示可登录、可多账号隔离的真实 ChatGPT 网页。
- 本地服务仅监听 `127.0.0.1`，并拒绝把整个磁盘作为拖放传输源。

## 0.5.0 - 2026-07-24

- 增加桌面双栏应用：素材生产左侧读取本地文件树，右侧直接加载真实 ChatGPT 网页；其他工作流自动隐藏 GPT。
- 增加 3 个隔离的 GPT 登录空间，登录状态保存在本机。
- 设备在线检测改为服务端直接返回数量和在线设备，自动检测与库存扫描拆开；在线检测每 60 秒低频执行，手动刷新仅作兜底。
- 修复 Android 图标缺少头部的问题，并改用 Windows / macOS 系统字体栈。
- 作品集改成可折叠树，可展开 14 个作品并点击预览；数量移动到右侧。
- 作品集标签台账可编辑并导出 CSV，Excel 可直接打开。
- 删除抖音归档入口与归档筛选；抖音旧归档统一按“已使用”展示。
- 公众号流程改成“公众号可用 → 已打开过 → 是否已上传？ → 上传已完成”。
- 主题恢复原项目名称“拟态悬浮”和“克制玻璃”。
- 素材根目录校正为 `01-素材库`，继续递归识别图片与文案齐全的帖子文件夹。

## 0.4.0 - 2026-07-24

- 素材生产顶部新增“项目目录设置”：支持粘贴本地路径后回车、系统文件夹选择器和递归扫描。
- 帖子识别收紧为“同一文件夹内同时存在图片和 TXT/Markdown 文案”；只有有效帖子显示“传 GPT”。
- 设置页直接读取并维护现有 `D:\Download\workpkg_config.json`，支持作品集目录、14 条成组与自动压缩设置；保存前保留 `.bak`，VBS/PowerShell 继续并行可用。
- 分发页接入 `send_to_device.py --status` 的真实在线明细；在线设备置顶并显示当前作品数，离线设备明确标记并禁用补笔记按钮。
- 设备统计补齐“台 / 个”单位，设备卡使用 Android / Apple 图形标识。
- 作品集新增并行 JSON 台账，可编辑人工分类、标签和备注；不删除旧 Junction 执行链。
- 删除不可实现的“发布调度”占位入口。
- 主题从 3 套扩展为 6 套：新增来自江湖转化助手规范的“江湖悬浮”，以及“纸墨编辑部”“深海夜航”。
- 自动化测试增至 26 项，并完成 331 个真实帖子、设备在线联动和 6 套主题的浏览器验收。

## 0.3.0 - 2026-07-24

- 删除没有实际价值的“总览”，旧状态自动迁移，软件启动后直接进入“素材生产”。
- 素材生产升级为双栏工作台：左侧真实读取本地文件夹树，右侧负责当前帖子、生产指令和真实 ChatGPT 联动。
- 兼容根目录直接存放帖子、分类目录下存放帖子两种素材结构；当前可读取 331 个本地帖子文件夹。
- 每个帖子右侧增加“传 GPT”，自动准备帖子路径与母版指令，并打开已登录的真实 ChatGPT 浏览器。
- 直接复用 `cgpt-workpkg://run`、`cgpt-workpkg://configure` 和已有网页脚本，不复制原脚本源码。
- 左侧增加“发布调度”预留入口；当前只展示开发边界，不伪造设备、账号或平台发布状态。
- 外部启动入口增加白名单，仅允许 ChatGPT HTTPS 地址与现有工作包协议。
- 自动测试增加到 23 项，并完成素材树展开、搜索、页面切换和浏览器错误回归。

## 0.2.1 - 2026-07-24

- 删除作品集页重复的顶部统计卡，把实时数量合并到内容类型和平台状态筛选按钮右侧。
- 筛选数量会随另一组筛选和搜索词联动，不显示固定的过期总数。
- 将分发页“手机补货”改为“手机补笔记”，并统一“已登记设备”“泛流量合集”“团建转化（精准流量）”等用户用语。
- 删除设备扫描后重复展示的长提醒块，保留统计卡、设备列表和扫描刷新能力。
- 自动测试增至 19 项，并完成浏览器交互回归。

## 0.2.0 - 2026-07-24

- 将顶部标签导航升级为左侧工作流栏：总览、素材生产、作品集、分发功能、发布复盘和设置。
- 新增作品集实时状态扫描，按 `[泛]`、`[转]`、平台资格、公众号领取和归档状态筛选。
- 新增作品集详情，显示条数、文件数、容量、三平台状态和使用记录。
- 新增分发功能四页签，接入 8 台登记设备、真实库存、设备扫描和现有安全分发 Skill。
- 新增公众号“已领取待上传 → 已确认上传”的人工确认日志。
- 新增雾绿实体、轻透玻璃和柔和拟态三种可持久化主题。
- 新增作品集状态、界面筛选和分发参数白名单测试。
- 识别并显示当前 2 个抖音归档 Junction 的源目录异常，不把它们冒充为可恢复库存。

## 0.1.0 - 2026-07-22

- 从江湖有旅人业务项目中提取独立软件源码。
- 将项目资产、软件源码、运行状态和共享 Skill 分层。
- 增加稳定启动入口、桌面入口和路径覆盖配置。
# 0.9.6 - 2026-07-28

- 把“校准出图”的开发者界面改为普通用户能直接理解的“做一张 / 做一套 / 批量做”三个真实入口。
- 从既有生产聊天中提炼并结构化米字格、绿底键帽、森林路线节点和备忘录四类模板配方；模板管视觉、素材管事实、历史失败结果不得继承。
- 新增出图计划确认：系统按有效素材自动拆页，明确每页标题、封面/内页角色、总图片数和文案数；计划确认后才开始扣费生产。
- 新增后台生产任务与进度查询；“做一套”逐页使用不同内容与提示词生成独立 3:4 图片，并写入小红书文案、出图计划和生产记录。
- “批量做”从当前素材开始连续处理 2/3/5 套，同一模板复用但每套素材事实相互隔离。
- 接口地址、模型和密钥移入默认收起的高级区；主流程只保留用户真正需要的生产方式、页数、质量和额外要求。
- 新增模板配方、页数规则、标题清洗和本地文案接口测试；自动化测试增至 56 项。
## 0.11.6 - 2026-07-29

- 内容分发作品集选中后整张卡片上浮；名称区域不再套第二层小模块，移除“可发送到手机”冗余徽标，分类与操作控件统一高度。
- 已完成、失败和取消的任务支持直接清除，并在当前操作区保留 3 分钟后自动收起；完整审计历史继续保存在“操作记录”。
- 移除顶部“按历史记录整理”和底部 Codex 命令条，新增全局右下角“团建中控助手”。助手可检测设备、打开页面设置，并按设备白名单与防重复规则执行指定作品或指定分类分发。
- 设置页新增每天/每周/按小时的自动备份计划、方案目录选择和每月大文件流量上限。方案文件使用增量清单上传，达到月额度后留待下月继续。
- 修复流量转化嵌入脚本把 `/conversion-integrated` 重复添加两次、造成内部接口 404 的问题，并加入回归测试。
- 内容制作右侧成品区升级为与素材区一致的文件夹展开结构，图片和 TXT 可直接预览；未打包、已打包和打包记录分别读取真实本地目录。
- README 重写为面向实际使用者的完整说明，补充开发背景、最快使用方法、文件流转、备份、技巧、安全和故障排查。
# 0.14.7-dev 账号窗口口径（2026-08-02）

- 将生产界面与设置中的“浏览器窗口”统一称为“账号窗口”，明确每个 GPT 网页对应一个独立账号档案。
- 单账号持续模式统一显示为“单账号单模板·永不停歇”；多账号模式显示为“多账号单模板·永不停歇”。历史数据里的“浏览器 N”只作为迁移别名保留。
- 账号窗口仍保持独立登录分区；切换查看不会把新素材注入到另一个正在生产的账号窗口。
## 0.14.11 - 2026-08-02

- 右侧 GPT 原生浏览器视图新增底部地址栏；支持粘贴网址、回车/访问按钮、前进、后退、刷新和返回 GPT 首页。
- 地址导航复用当前账号的持久登录分区，仅允许 http/https，不接受 `javascript:`、本地文件或网址内嵌账号密码。
- 离开 GPT 访问其他网址时，不覆盖账号下次启动要恢复的 GPT 会话地址；回到 GPT 首页后继续使用原生产能力。
- 地址栏与原生网页视图分开布局，避免 WebContentsView 覆盖底部操作栏；当前网址会随账号窗口状态同步显示。
- 回归测试：工作台 `189/189` 通过。
