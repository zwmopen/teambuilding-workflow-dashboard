# 变更记录

## 0.9.2 - 2026-07-27

- 新增后台全库素材索引，逐分类扫描并持久化紧凑标签、次数、哈希和全局统计，不恢复启动时同步全库扫描。
- 自动读取 `素材链接记录.csv` 与 `制作日志.csv`，按生产事件去重后回填素材使用次数。
- 历史路径失效时只接受唯一文件夹名称匹配；多候选和未匹配记录进入待核对，不自动猜测。
- 历史回填幂等，同一证据重复刷新不会重复加次数；扩展后续真实发送继续在同一计数上累加。
- 元数据修改会同步更新持久化全库索引，真实文件夹移动后自动后台重建索引。

## 0.9.1 - 2026-07-27

- 素材读取改为真正的分类懒加载：首页只加载当前分类，扩展初始接口只返回顶层分类；展开其他分类时才递归扫描。
- 本机实测首页数据由约 21 MB / 42 秒降到约 0.37 MB / 1.1 秒，扩展分类索引约 3.7 KB / 22 毫秒。
- 设置页改为单页紧凑分组，主题从 6 个重复选项精简为 3 个明确模式。
- 提示词入口继续由浏览器扩展独立挂载，不依赖已停用的左侧会话树。

## 0.9.0 - 2026-07-27

- 为每个有效素材文件夹生成独有且可随同卷移动保持稳定的 SHA-256 身份；两份内容相同的文件夹仍是两个身份。
- 新增素材元数据台账，按文件夹哈希保存唯一母标签和使用次数，改名或移动后仍可恢复。
- 母标签限定为“团建游戏 / 团建转化 / 合集攻略”；聚会游戏、破冰游戏等同义关键词统一归入“团建游戏”。
- `/api/materials` 向扩展返回文件夹哈希、母标签、标签来源和使用次数；新增 `/api/extension/material-metadata` 供扩展人工改标签或增加次数。
- 文件夹身份与内容查重分层：唯一文件夹哈希绑定标签，图片与文案聚合指纹继续只负责内容防重。

## 0.8.9 - 2026-07-27

- 为独立 GPT 生产扩展新增受控文件夹移动接口：只允许在同一授权素材库或成品库内部移动真实文件夹。
- 拒绝移动库根目录、普通文件、软链接、跨库目标、自身子目录和会覆盖同名项的操作。
- 移动成功后清理素材扫描缓存，扩展刷新即可看到真实新位置。
- 新增真实临时目录移动及安全拒绝回归测试；Node 测试增至 40 项。

## 0.8.8 - 2026-07-26

- 素材使用台账增加内容指纹，防止素材改名或移动后被重复生产。
- 指纹按图片和文案内容计算，不依赖文件名。
- 仅在选中上传时计算，避免后台全库哈希造成卡顿。

## 0.8.7 - 2026-07-26

- 增加素材使用台账：区分已加入 GPT 与已真正发送。
- 台账只记录路径、状态、会话与时间，不移动或改名原素材。
- 素材接口返回使用状态，供拓展阻止重复上传。

## 0.8.6 - 2026-07-26

- 增加拓展专用完整成品目录树接口，不再用手机分发筛选结果代替本地文件浏览。
- 增加拓展工作包接口，由工作台直接执行现有 PowerShell 脚本，默认不再唤起 VBS。
- 增加目录越界保护、完整目录可见性和直接工作包链路测试。

## 0.8.5 - 2026-07-26

- 修复素材库顶层分类被重命名或删除后仍复用旧递归缓存的问题；扩展不再显示已经不存在的帖子路径。
- 素材缓存增加顶层目录签名校验，分类变化会自动失效并重新扫描；新增分类重命名回归测试。
- 完成真实 Edge + ChatGPT 扩展联动验收：5 个本地附件进入真实会话、生成 5 张图片、真实下载 5/5。
- 下载事件写入本地防重复台账，包含落盘路径、来源会话、文件大小与完成时间。

## 0.8.4 - 2026-07-26

- 修复真实 ChatGPT 页面访问本机目录接口时被浏览器私有网络访问策略拦截、扩展显示 `Failed to fetch` 的问题。
- 本机扩展协作接口现在为允许的 ChatGPT 与浏览器扩展来源返回私有网络预检许可；其他来源仍然拒绝跨域读取。

## 0.8.3 - 2026-07-25

- 工作台与“团建 GPT 数字作品生产助手”拆成两个独立项目；工作台继续承载本地数据和执行接口，扩展专门服务“生产数字作品”SOP。
- 新增扩展协作接口：读取当前素材/成品路径、作品集与作品、素材分类与帖子文件，并允许保存用户粘贴的路径设置。
- 生产防重复改为只读汇总既有作品历史库：当前 193 组图片均具备整组 SHA-256 与 64 位 dHash；精确哈希用于确定重复，视觉哈希只做近似预警。
- 分发防重复仍由手机组、公众号组、claim、使用日志和入口状态独立管理，不再把旧文本哈希误称为当前生产历史。
- 设置页新增生产历史覆盖状态与独立扩展项目入口；默认主题保持用户指定的“拟态悬浮”。
- 扩展协作接口只向真实 ChatGPT 域名和已安装浏览器扩展开放，不给任意网页开放整个中控 API。
- 修正发布级实机测试在“泛流量包”筛选下误查设备行的问题；34 项 Node 测试和完整桌面交互回归均通过。

## 0.8.2 - 2026-07-25

- 修复便携桌面版主工作区只占顶部、下方大面积白屏的问题，素材树和真实 ChatGPT 网页现在始终铺满窗口。
- 设置新增当前版本、检查版本、打开发布包目录、打开运行数据和复制诊断信息；便携版会打开真实 EXE 所在目录。
- 增加应用单实例保护，连续双击只保留一个窗口，并唤醒已有窗口。
- 修复点号开头但合法的 `[转]` 作品集被漏算，工作台库存与原分发 Skill 统一为 2 个团建转化包。
- 普通文件发送终态统一为 100%、`completed` 和“发送完成并确认接收”。
- 素材库加入运行时完整缓存，日常主数据读取由约 18–21 秒降至约 1.2–1.4 秒，手动“刷新文件树”仍会执行完整递归扫描。
- 将现有 ChatGPT 用户脚本及工作包 PowerShell/VBS 同步为应用内平行副本；个人会话历史只导入本机运行数据，不进入公开仓库或发布包。
- 新增真实桌面逐项验收：11/11 个素材分类、21/21 个作品集、图片/文本预览、三套分发子页、设备联动、应用内弹窗和六套主题全部通过。
- 通过应用接口向在线 1 号设备实际发送 165 字节验收文件，接收端提交成功。

## 0.8.1 - 2026-07-25

- 正式提供 Electron 便携桌面版；应用可自行启动本地服务，不依赖浏览器调试窗口。
- 素材生产右侧使用真实 ChatGPT 网页，支持持久登录、Google 授权弹窗和 3 个隔离账号空间。
- 生成桌面快捷方式“团建内容工作台”，浏览器版继续只作为调试与兼容入口。
- 分发设备行按钮统一为单行紧凑文字，修复“补泛流量”等按钮中文被拆行溢出。
- 作品集折叠箭头改为嵌入整行背景；列表/网格改为筛选面板右下角的单一低调图标。

## 0.8.0 - 2026-07-25

- 删除暂时不用的“发布复盘”导航、总览入口与整页，旧标签会安全回到素材生产。
- 作品集改为直接读取可粘贴、可选择的本地目录，并按文件夹名 `[泛]`、`[转]`、无标签分类。
- 删除小红书单平台筛选、全部已使用、搜索框及下方重复台账详情，只保留当前真正需要的筛选和作品树。
- 作品集增加紧凑列表/网格视图、所有合集独立展开、3:4 缩略图和图片 + TXT/Markdown 双预览。
- “拟态悬浮”升级为新安装和本次默认迁移主题。
- 桌面素材生产将真实 ChatGPT 网页继续放在右侧，新增本地素材文件与生产指令投递桥接；三个账号空间仍相互隔离。
- 兼容读取现有作品集目录和旧工作包历史，不修改原 VBS、PowerShell、油猴脚本或个人历史导出。
- 自动化测试增至 31 项。

## 0.7.0 - 2026-07-25

- 作品包分发由同步等待升级为可恢复任务：显示核对、设备响应、发送百分比、接收确认、写入记录和完成状态。
- 分发任务和普通文件任务统一显示在分发页，支持安全停止；刷新页面后可恢复当前及最近任务。
- 作品包选择设备后改用系统内二次确认，明确显示作品包、目标设备、内容类型和手机组使用规则。
- 删除前台关于软链接、Junction 等内部实现的提示；兼容层继续并行工作，但不干扰日常操作。
- 全站原生确认框、警告框和重命名输入框替换为统一的系统弹层，六套主题下保持一致视觉与可读状态。
- 接口错误统一转换为可读提示，不再把原始 JSON 或脚本输出直接弹给用户。
- 新增传输阶段解析测试，自动化测试增至 30 项。

## 0.6.0 - 2026-07-25

- 分发统计统一按当前真实可分发列表计算，统计卡与下方作品包数量保持一致。
- 设备状态改为后台低频自动刷新，始终显示“当前设备在线 N/总数”；刷新时只显示小型旋转图标。
- 在线设备置顶，离线设备明确禁用补笔记与上传；设备名称可直接编辑本机备注。
- 泛流量和团建转化卡可直接切换真实作品包列表；每个作品包独立选择当前在线设备，发送前保留最终确认。
- 设备选择弹窗统一使用 Android / Apple 图标、设备名称和“当前在线”状态，删除平台与作品数等重复信息。
- 新增“上传其他”：可选择文件或文件夹、拖放到在线设备、查看实时进度并取消指定传输任务。
- 指定作品包分发继续复用 `device-folder-transfer` 的真实传输与接收确认；成功后只删除已校验的活动平台 Junction，不再创建抖音归档入口。
- 增加三层防重复：发送前独占占用记录、接收成功 CSV 日志、活动 Junction 删除；已有成功记录的作品即使入口被误建回来也拒绝再发。
- 手机库存改为组级互斥：只有小红书与抖音两个同源入口同时有效才在前端显示和参与底层抽取；任一手机平台使用后整组退出，公众号资格独立保留。
- 公众号页改为“公众号补笔记”，加入“打开公众号官网”，状态统一为“公众号可用 / 已打开过 / 上传已完成”。
- 素材文件树改为紧凑列表，删除文案摘要，增加列表/小图标视图切换；“传 GPT”改为圆润紧凑按钮。
- 浏览器调试版删除仿制任务卡；桌面版素材生产右侧继续显示可登录、可多账号隔离的真实 ChatGPT 网页。
- 本地服务仅监听 `127.0.0.1`，并拒绝把整个磁盘作为拖放传输源。

## 0.5.0 - 2026-07-24

- 增加桌面双栏应用：素材生产左侧读取本地文件树，右侧直接加载真实 ChatGPT 网页；其他工作流自动隐藏 GPT。
- 增加 3 个隔离的 GPT 登录空间，登录状态保存在本机。
- 设备在线检测改为服务端直接返回数量和在线设备，自动检测与库存扫描拆开；在线检测每 60 秒低频执行，手动刷新仅作兜底。
- 修复 Android 图标缺少头部的问题，并改用 Windows / macOS 系统字体栈。
- 作品集改成可折叠树，可展开 14 个作品并点击预览；数量移动到右侧。
- 作品集标签台账可编辑并导出 CSV，Excel 可直接打开。
- 删除抖音归档入口与归档筛选；抖音旧归档统一按“已使用”展示。
- 公众号流程改成“公众号可用 → 已打开过 → 是否已上传？ → 上传已完成”。
- 主题恢复原项目名称“拟态悬浮”和“克制玻璃”。
- 素材根目录校正为 `01-素材库`，继续递归识别图片与文案齐全的帖子文件夹。

## 0.4.0 - 2026-07-24

- 素材生产顶部新增“项目目录设置”：支持粘贴本地路径后回车、系统文件夹选择器和递归扫描。
- 帖子识别收紧为“同一文件夹内同时存在图片和 TXT/Markdown 文案”；只有有效帖子显示“传 GPT”。
- 设置页直接读取并维护现有 `D:\Download\workpkg_config.json`，支持作品集目录、14 条成组与自动压缩设置；保存前保留 `.bak`，VBS/PowerShell 继续并行可用。
- 分发页接入 `send_to_device.py --status` 的真实在线明细；在线设备置顶并显示当前作品数，离线设备明确标记并禁用补笔记按钮。
- 设备统计补齐“台 / 个”单位，设备卡使用 Android / Apple 图形标识。
- 作品集新增并行 JSON 台账，可编辑人工分类、标签和备注；不删除旧 Junction 执行链。
- 删除不可实现的“发布调度”占位入口。
- 主题从 3 套扩展为 6 套：新增来自江湖转化助手规范的“江湖悬浮”，以及“纸墨编辑部”“深海夜航”。
- 自动化测试增至 26 项，并完成 331 个真实帖子、设备在线联动和 6 套主题的浏览器验收。

## 0.3.0 - 2026-07-24

- 删除没有实际价值的“总览”，旧状态自动迁移，软件启动后直接进入“素材生产”。
- 素材生产升级为双栏工作台：左侧真实读取本地文件夹树，右侧负责当前帖子、生产指令和真实 ChatGPT 联动。
- 兼容根目录直接存放帖子、分类目录下存放帖子两种素材结构；当前可读取 331 个本地帖子文件夹。
- 每个帖子右侧增加“传 GPT”，自动准备帖子路径与母版指令，并打开已登录的真实 ChatGPT 浏览器。
- 直接复用 `cgpt-workpkg://run`、`cgpt-workpkg://configure` 和已有网页脚本，不复制原脚本源码。
- 左侧增加“发布调度”预留入口；当前只展示开发边界，不伪造设备、账号或平台发布状态。
- 外部启动入口增加白名单，仅允许 ChatGPT HTTPS 地址与现有工作包协议。
- 自动测试增加到 23 项，并完成素材树展开、搜索、页面切换和浏览器错误回归。

## 0.2.1 - 2026-07-24

- 删除作品集页重复的顶部统计卡，把实时数量合并到内容类型和平台状态筛选按钮右侧。
- 筛选数量会随另一组筛选和搜索词联动，不显示固定的过期总数。
- 将分发页“手机补货”改为“手机补笔记”，并统一“已登记设备”“泛流量合集”“团建转化（精准流量）”等用户用语。
- 删除设备扫描后重复展示的长提醒块，保留统计卡、设备列表和扫描刷新能力。
- 自动测试增至 19 项，并完成浏览器交互回归。

## 0.2.0 - 2026-07-24

- 将顶部标签导航升级为左侧工作流栏：总览、素材生产、作品集、分发功能、发布复盘和设置。
- 新增作品集实时状态扫描，按 `[泛]`、`[转]`、平台资格、公众号领取和归档状态筛选。
- 新增作品集详情，显示条数、文件数、容量、三平台状态和使用记录。
- 新增分发功能四页签，接入 8 台登记设备、真实库存、设备扫描和现有安全分发 Skill。
- 新增公众号“已领取待上传 → 已确认上传”的人工确认日志。
- 新增雾绿实体、轻透玻璃和柔和拟态三种可持久化主题。
- 新增作品集状态、界面筛选和分发参数白名单测试。
- 识别并显示当前 2 个抖音归档 Junction 的源目录异常，不把它们冒充为可恢复库存。

## 0.1.0 - 2026-07-22

- 从江湖有旅人业务项目中提取独立软件源码。
- 将项目资产、软件源码、运行状态和共享 Skill 分层。
- 增加稳定启动入口、桌面入口和路径覆盖配置。
