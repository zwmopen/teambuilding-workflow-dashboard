# 变更记录

## 0.14.0 - 2026-07-31（正式版）

- 窗口右上角关闭默认改为退到系统托盘，保留 GPT 页面、登录状态和正在运行的生产任务；托盘提供打开工作台、暂停自动生产和彻底退出，运行任务时彻底退出需要二次确认。
- 删除启动时全量清理 Chromium 缓存的逻辑。GPT 页面首次创建后只做隐藏/恢复，手动刷新或异常恢复时才重新载入；素材树、成品树和其他浏览器档案继续按需加载。
- “添加浏览器”现在创建永久浏览器档案。档案名称、登录分区、额度组、隐藏状态和最后使用时间保存到本机数据文件；隐藏和移除记录默认不删除登录数据，真正清除登录分区是独立危险操作。
- 内容生产增加手动、单窗口自动和多窗口轮询三种模式。多窗口默认并行 3 个、最多 5 个，每个窗口独立记录上传、等待计划、自动确认、等待图片、请求文案、下载、查重打包、归档和完成阶段。
- 自动任务启动后锁定本批素材、模板、账号和任务数量；后续勾选只影响下一批。暂停按钮会变成“继续”，跳过只跳过当前作品，重启后从本机检查点恢复，无法可靠判断时停在人工确认。
- 上传与生图额度改为按账号额度组记录滚动三小时事件；显示逐笔释放时间，并在额度恢复时通过全局助手和系统通知提醒，默认不会自动开启下一周期任务。
- 增加定时启动、并行窗口数、自动确认文字、文案请求词、最低图片数、下载暂存、成品库、闲置网页回收时间及网页内工具开关。当前默认下载暂存为 `D:\Download`，成品库沿用本机真实配置。
- GPT 回复下方恢复提示词库、下载本组、下载并打包、复制文案和下载 TXT。下载与当前回复、当前批次绑定；图片不足或文案为空时不允许冒充打包成功。
- 本地工作包接口支持本次任务传入下载和成品目录，仍复用查重与 `work-package` 链路；只有图片、TXT、批次、数量、查重和成品目录全部核对成功后才归档素材。
- 工作台自动化测试 135/135、扩展测试 11/11、桌面隔离真机布局与核心 API 检查通过。0.13.0 正式版继续保留作为回退版本。

## 0.13.0 - 2026-07-30（正式版）

- “内容生产（自动）”改为工作台调度、浏览器扩展执行：工作台只管理本地素材、模板、账号、额度、队列、归档与备份；真实 ChatGPT 网页内的附件上传、发送、回复检测、图片下载和文案提取继续由同一套扩展能力完成。
- 素材树和模板树统一为可折叠的懒加载文件树。父目录支持三态全选，帖子文件夹是最小生产单位；图片与 TXT 只负责预览。多个父目录可同时展开，目录变化会自动刷新，未展开目录不会一次加载全部缩略图。
- 模板支持多选并按笛卡尔积计算任务：3 个素材 × 2 个模板生成 6 个作品。选择模板时，每个模板只建立一个新会话并依次处理本批素材；未选择模板时沿用当前已沉淀母版的 GPT 会话。
- 手动模式只上传当前任务并填入提示词，停在发送前；自动模式依次完成上传、等待计划、发送 `1`、等待图片、请求小红书文案、精确下载、TXT 写入、打包校验和素材归档。失败可重试当前阶段、跳过当前任务或暂停整批，已经完成的步骤不会重复执行。
- 每个账号使用独立持久登录分区和独立滚动三小时账本。默认估算上传 80 张、生成 50 张，开始前只安排能完整执行的作品，超额任务留在等待队列并显示预计恢复时间；该账本只是本地估算，网页实际限流优先。
- 只有图片与 TXT 均已落盘且批次一致，才允许打包成功并把复制素材移动到“已上传 N 次”。失败、下载不完整或打包失败均不移动；同名目录自动追加短指纹，手动移动后仍可通过内容指纹识别使用次数。
- 设置、帮助、图片和 TXT 预览打开时，原生 GPT 视图自动隐藏，关闭后恢复；修复多次快速切换账号时重复创建 ChatGPT 视图的问题。
- 本地设置导出/导入覆盖路径、开关、账号别名、额度账本、任务和归档记录，但不导出 Cookie。新增仅限本机的 GPT 登录档案恢复点，关闭网页会话并重启后复制完整登录分区，默认不上传坚果云。
- 扩展升级到 0.2.11，支持按失败阶段断点续跑、原生附件与发送确认、异常计划自动纠正、生成图片直接保存，以及小红书文案过短时自动补问并安全暂停；完整 Node 自动化测试 128/128、扩展测试 9/9 通过。
- 使用已登录的真实 ChatGPT Plus 分区完成官网链路验收：3 个真实附件进入官网、模型返回逐页计划、系统自动发送 `1`，随后真实生成并下载 1 张 1086×1448 的 3:4 PNG。图片、本地工作包和测试素材归档均已真实落盘。
- 本次官网连续返回被截断的小红书文案。0.2.11 会自动要求补齐一次；仍短于完整文案阈值时停止打包和归档，不把残缺文本冒充成品。为单独验证本地尾段，使用测试素材原 TXT 完成了一次工作包和“已上传 1 次”归档；该次不计为全自动文案闭环通过。
- 修复自动生产素材区把“尚未展开”误显示为 0 的问题。正式界面现在复用后台全局索引显示 16 个父目录、1941 个帖子；未知数量显示“…”而不是假 0，搜索不会破坏父目录三态全选。
- 正式便携版已构建并启动到 4327，登录后原 ChatGPT 会话和输入框仍存在；升级前完整备份 Electron 用户数据 2409 个文件、371,863,617 字节，更新未更换或清理 GPT 持久分区。
- 工作台自动化测试 130/130、扩展测试 9/9 通过。正式包大小 96,157,448 字节（91.70 MB），SHA-256 `D5C7CBACEBF546CC386E0585D72B1A85F31F9A2BEB670A6D654ED1A28F975F1E`。

## 0.12.2 - 2026-07-30（正式版候选）

- “内容制作（GPT内置测试）”正式更名为“内容生产（自动）”，按左侧已选素材文件夹数量串行生产；一套母版可连续对应多个素材，不再出现“做一套 / 做一批”逻辑。
- 自动流程补齐真实闭环：上传母版与当前素材、等待迁移计划、按设置发送 `1`、等待新图片、请求并复制小红书文案、逐张下载、生成 TXT、校验后打包。任一阶段失败会暂停或按设置跳过，不把计划完成冒充作品完成。
- 每个帖子使用唯一下载批次；本地只接收该批次明确命名的图片，校验图片张数、TXT 和成品目录后才返回成功，解决下载目录旧图串包风险。
- GPT 区新增前进、后退、刷新、首页、多账号标签和账号隔离。每个账号使用独立持久会话分区，互不共享 Cookie、历史和登录状态。
- 页面级设置新增自动确认、自动复制文案、自动打包、失败暂停、生产间隔随机范围、单帖超时和每轮最大套数。随机间隔只用于正常队列节奏；遇到登录、平台验证或限流会暂停，不绕过平台限制。
- 内嵌网页容器补齐圆角边界，生产区新增真实百分比、当前阶段和日志反馈；账号切换、删除和继续生产均有明确状态。
- Node 自动化测试增至 121/121；桌面隔离烟测确认 ChatGPT 登录页、内置扩展 0.2.4、账号标签、四个网页控制按钮和页面设置均可用。隔离账号未登录，因此真实付费生成仍需用户首次登录后完成一套人工验收。
- 正式便携版已构建并启动到 4327，桌面“团建工作台”快捷方式已切换到 0.12.2。旧 0.12.0、0.12.1 发布目录已移入回收站，本地发布目录只保留最新版。
- 0.12.2 便携包大小 96,144,357 字节（91.69 MB），SHA-256 `42D6B01596788C67D19B7DEC2B398BECC2B974D3C654B2CC513DD6F17C7BC82F`。

## 0.12.1 - 2026-07-30（开发版）

- 内容制作下新增“内容制作（GPT内置测试）”：左侧分别展开本地素材与母版，右侧直接承载真实 ChatGPT 网页；登录状态使用独立持久会话保存，不复制 Cookie、令牌或账号密码。
- 一个母版可同时对应多个已选素材文件夹；工作台按素材逐组排队，每组自动附带典型封面/内页母版、当前素材图片与 TXT，并注入长期母版迁移规则，不再提供“做一套 / 做一批”选择器。
- 复用现有浏览器扩展、提示词、下载与本地工作包链路；嵌入态隐藏扩展自身的重复侧栏，只保留真实附件上传、任务结果回传和原生 ChatGPT 生成能力，旧 VBS 仅保留兼容。
- 真实桌面烟测确认 ChatGPT WebContentsView、扩展加载、持久分区、任务桥接与失败反馈均可工作；隔离测试会话未登录，因此登录后的真实附件与生成链路仍需首次人工登录后验收。
- 正式便携版现在自带 GPT 扩展运行快照，不再依赖打包后不存在的相邻源码路径；开发态仍优先读取独立扩展仓库，发布前通过同步脚本更新快照。
- 生产链改为“首图校准优先”：确认计划后只生成第一个素材的 P1 封面，并在成功落盘后强制暂停；用户看图确认后才继续剩余页面与文案。
- 计划和按钮明确显示“仅 1 次调用”“失败不自动重试”和剩余张数，避免勾选多组素材后一次性烧完整批费用。
- 正式生产的付费生图请求关闭 408、429、5xx 和网络错误自动重试；失败停在当前校准图，不再悄悄追加第二、第三次请求。
- 每张成品在 `生产记录.json` 中保存实际请求数、尝试次数、参考图数量、供应商请求编号及供应商返回的 usage（若有）；失败首图也保存尝试记录。
- 首张确认后续跑会跳过已经存在的 P1，只生产剩余页面；失败重试仍只重试首张，不会误放行整套。
- 语法检查、无付费模拟接口、状态机和界面回归共 119 项自动化测试全部通过；未调用真实生图接口。用户确认打通后已构建并验收 0.12.1 正式便携版，更新 4327 桌面入口。

## 0.12.0 - 2026-07-29（正式版）

- 内容生产任务改为可恢复的后台任务：逐页落盘、逐页记录耗时与失败原因；单页失败不再拖垮整套，重启后可继续补跑缺页，也可主动停止。
- 生产区新增真实百分比、当前阶段、最近日志、失败页和结果检查报告；完成后可直接打开作品目录、生产记录与质量报告。
- 一套模板可同时对应左侧多个素材文件夹；作品数量完全跟随已选素材，不再显示“做一套 / 做一批”模式选择。
- ByteCat `gpt-image-2` 改用已验证的 JSON 参考图协议，并把母版与素材压缩为一张轻量参考板，解决标准 multipart 编辑接口无法识别模型和大图请求断线的问题。
- 本机 OpenAI 兼容生图通道与 ByteCat 均完成真实参考图请求；MiniMax 接口会显示真实认证结果，不把失败冒充为成功。
- 每套作品继续输出独立 3:4 PNG、`小红书文案.txt`、`出图计划.json`、`生产记录.json` 和新增的 `质量报告.json/txt`；测试任务支持文件夹前缀，便于与正式作品分开。
- Windows 桌面版目录选择改为现代系统文件夹窗口；路径也可直接粘贴，按回车或失焦后自动保存、扫描和读取。
- 坚果云补齐本机加密凭据、连接检测、工作台小数据备份/恢复与方案目录增量备份；跳过隐藏临时文件，单个文件失败不会立即中断整批。
- 新增生产恢复、技术质量检查、WebDAV 备份、即时停止和桌面真实交互回归；成品会与本次母版/素材原图做文件指纹比对，拒绝把输入原图冒充 AI 成品；自动化测试增至 115 项。
- 正式版仍与 4337 开发端隔离；发布成功后桌面快捷方式只指向 0.12.0，旧发布包清理，不删除源码依赖、素材、成品或运行数据。

## 0.11.7 - 2026-07-29（正式版）
- 流量转化页拆除嵌套卡片壳：取消外层留白、圆角、阴影和独立底色，嵌入内容背景改为透明，与团建工作台共享同一块大背景。
- 保留原有顶部功能标签、转化 SOP、搜索话术、按需求找方案、用户旅程和手机入口，不删减功能。
- 补充同层融合回归测试；完整自动化测试增至 105/105，并在 1280 像素宽度下确认无横向溢出。

## 0.11.6 - 2026-07-29（正式版）

- 内容分发补齐页面级设置、设备白名单、库存阈值、自动检测与自动补货配置；作品集整行点击即可选中，当前任务保留 3 分钟并支持立即清除，完整历史继续写入操作记录。
- 内容制作右侧改为真实成品目录浏览器，按“未打包 / 已打包 / 打包记录”查看帖子文件夹；图片与 TXT 可展开和预览，目录输入支持回车或失焦自动保存。
- 新增右下角“团建中控助手”：固定安全指令直接执行，其他自然语言先由已配置文本模型归类为允许动作；支持能力说明、状态查询、切换页面、检测设备、发送作品、补货、打开设置、触发生产与备份。所有发送仍受设备白名单、防重复和接收确认约束。
- 坚果云备份分为工作台小数据与方案文件增量备份；支持每天、每周或间隔运行，默认每月大文件上传上限 2560 MB，并记录进度、结果和下月续传清单。
- 方案备份源与“流量转化 → 按需求找方案”可使用同一个企微微盘同步目录；本机当前目录为 `D:\AICode\AI\data\01-团建策划-江湖有旅人\01-知识库\WXWork\1688855280811743\WeDrive\江湖有旅人\杭州江湖有旅人户外运动策划有限公司`。
- 修复打包应用访问坚果云时未沿用 Windows 代理环境、只显示 `fetch failed` 的问题；现在可通过系统代理建立连接，并把 401 明确显示为“坚果云应用密码无效”。
- 正式版与开发版继续隔离：桌面快捷方式启动 4327 便携正式版，源码开发使用 4337；正式更新不会覆盖运行数据或用户业务目录。
- 全部 Node 自动化测试增至 104/104，通过助手意图白名单、备份配置、内容分发、成品浏览、路径安全和界面回归。

## 0.11.5 - 2026-07-29（正式版）

- 修复自动分发随机选中作品集后，工作台没有记录脚本实际选中名称，导致手机已确认接收但电脑提示“自动移动失败：作品集名称无效”的问题；现在会从底层完成回执中回填真实作品集名称，再移动到“微信公众号”。
- 已核对并补齐本次真实发送记录：`作品集_050[转]` 未重复发送，原文件夹已从“抖音小红书”安全移动到“微信公众号”，操作记录可追溯。
- 内容分发统一把 `[转]` 定义为“精准流量（业务类）”；库存提醒、筛选、设备补货按钮、分类设置和任务状态不再显示“团建转化”。底层 `[转]` 文件名保持兼容，不改用户已有作品集名称。
- 内容分发设置中的开关控件修复内边距与小屏挤压；1280×720、1024×700 均无横向溢出。
- 自动化测试增至 93/93，通过自动选择名称回填、分段输出解析、真实文件夹流转与界面口径回归。
- 正式便携包补齐 `undici` 运行依赖；已用打包后的独立程序实际启动 4327 服务并确认版本、运行通道和依赖完整，不再出现“Cannot find module 'undici'”后直接退出。

- 完成工作台全界面细节巡检：素材生产、内容分发、流量转化、插件市场与设置页统一使用清晰的 SVG 图标；帮助与设置入口固定为 36×36 正圆，不再被通用按钮高度挤成椭圆。
- 修复设置页 12 栏网格中 API 卡片只占 1 栏而导致文字竖排、按钮挤压的问题；统一标题、路径、标签、设备状态和插件说明的换行与最小字号。
- 修复 1280、1024、900 像素宽度下生产工具栏、筛选按钮、三栏工作区、设置卡片、插件装饰和分发路径的横向溢出。
- 修复“流量转化”外部增强脚本仍访问工作台根 `/api` 的同源代理缺口；正式 SOP 与用户状态现在均从 `/conversion-integrated/api` 返回 JSON，不再误拿工作台 HTML。
- 新增圆形图标、响应式网格、转化脚本代理与兼容回退测试；完整自动化测试 91/91 通过。
- 修复手机端“准话助手”打开后无内容：新增轻量手机入口，不再让手机加载约 10MB 的完整知识库页面。
- 手机与电脑处于同一 Wi-Fi 时，可从工作台“流量转化”右下角的圆形手机按钮复制临时访问地址；手机端支持前端运营/后端转化切换、生成回复、备用回复和复制首选回复。
- 手机入口使用本机随机访问令牌和安全 Cookie；局域网只能访问受保护的准话助手，工作台、素材、设置和其他接口仍拒绝外部设备访问。
- 开发服务继续使用独立端口 4337 和独立运行数据，不影响 4327 正式使用版。
- 已完成真实 MiniMax 回复链路、未授权访问拦截、手机页面显示与交互验证；完整自动化测试 85/85 通过。
- 本轮更新正式使用版、桌面快捷方式、开源仓库与 0.11.5 发布包。

## 0.11.4 - 2026-07-29（本地热更新）

- 修复“开始生产”后计划已经生成、但确认入口被中栏底部绝对定位控件遮住的问题。
- 生产改为清晰的两步主按钮：首次点击生成计划并自动滚到可见区域；随后同一位置显示“返回调整 / 确认并开始生成”。
- 生图平台、生图模型、文案模型、页数和质量压缩为单行紧凑工具栏；计划、确认、进度和日志统一收进中栏底部操作坞。
- 新增确认入口位置、按钮状态切换、滚动定位与紧凑模型栏回归测试；完整自动化测试 79/79 通过。
- 使用本地 GPT 生图通道完成 1 套 9 页真实生产：全部输出 1200×1600 PNG，并附小红书文案、出图计划和生产记录；总墙钟耗时约 15 分 20 秒。
- 本轮仅更新本地源码，不重新打包；桌面源码启动器直接读取 0.11.4。

## 0.11.3 - 2026-07-29（本地热更新）

- 生产页新增阶段、百分比、进度条、当前状态和最近日志，并将生产控制区固定在中栏底部，普通窗口尺寸下始终可见。
- 所有关键状态增加统一的小圆点提醒；运行状态增加轻量呼吸动效。
- 压缩提示词和本次要求输入区，提高小字可读性；目录按钮统一简化为“选择 / 应用”。
- 新增生产状态和紧凑按钮回归测试，工作台自动化测试增至 74 项并全部通过。
- 本轮仅更新本地源码，不重新打包、不推送发布；真实生图仍等待可用的外部生图接口或凭据。

## 0.11.3 - 2026-07-28

- 流量转化改为工作台同源模块入口，不再暴露跨端口独立应用；外层可读取完整页面结构，便于直接点击、标注和维护。
- 原转化侧栏横移为内容区顶部标签，只保留搜索话术、转化 SOP、按需求找方案和用户旅程四个主任务。
- 聊天记录来源与知识入库归入搜索页，方案来源归入找方案页；两个来源目录独立保存、独立扫描，互不影响。
- 知识入库明确显示写入本机 `人工确认知识.json`，确认后才重建搜索索引。
- API 与模型设置从生产页移至工作台全局设置，供所有模块统一调用。
- 本轮继续使用源码热更新，不构建便携包；自动化测试 71 项通过。

## 0.11.2 - 2026-07-28

- 以“江湖团建转化助手”的拟态与克制玻璃视觉令牌作为全局唯一主题真源，统一素材生产、分发、插件和设置页面。
- 侧栏主题入口收敛为一个循环按钮，点击依次切换“拟态 → 玻璃 → 拟态”，不再显示两枚并排主题按钮。
- 外层工作台通过受限 `postMessage` 同步嵌入的流量转化主题；嵌入态隐藏其重复主题入口，但不覆盖或删减原应用功能。
- 本轮继续使用本地源码热更新，不重新打包。

## 0.11.1 - 2026-07-28

- 重整工作台自身的设置页，收敛为工作目录、备份与恢复、生产与分发数据、软件与诊断四块。
- 删除与侧栏重复的主题卡、与插件市场重复的 GPT 扩展卡，以及旧一键工作包、合集数量、自动压缩、发布包目录和历史工具入口。
- 设置页新增素材目录选择按钮；保存目录时保留既有后台成组参数，不再让已删除的旧控件覆盖配置。
- 桌面采用两列紧凑布局，窄窗口自动回落为单列；本轮继续使用本地源码热更新，不重新打包。

## 0.11.0 - 2026-07-28

- 产品更名为“团建工作台”，顶层导航移除重复的“作品集”，新增“流量转化”。
- 顶层同时移除“总览”；旧总览与作品集状态会迁移到素材生产。
- 流量转化保留团建工作台外层侧栏，右侧完整嵌入江湖团建转化助手原版界面；搜索话术、转化 SOP、需求方案、知识库完善、设置和用户旅程均不删减，只随内容区自适应缩小。
- 界面按江湖知识库原实现移植：168px 文字侧栏、无顶部横栏、冷灰蓝拟态面板、深灰主按钮、底部服务状态与“玻璃 / 拟态”切换。
- 素材生产、分发、转化、插件和设置均保留；生产三栏在 1280px 桌面窗口内不再横向溢出。
- 新增轻量产品初稿 `docs/PRODUCT-BRIEF.md`，后续开发先确认用户任务、数据真源、界面骨架、完成标准和验收场景。
- 桌面新增“团建工作台”源码快捷方式，继续采用本地热更新，不构建新便携包。
- 增加“原版转化助手完整嵌入、禁止渲染删减版转化 DOM”的回归测试；自动化测试 70 项通过，实页逐项验证原版六个转化栏目可用。

## 0.10.3 - 2026-07-28

- 左侧素材区明确为“素材分类文件夹 → 帖子文件夹 → 图片与 TXT/Markdown”三级结构；每一级都由用户点击展开，不再一次铺开整个分类的所有图片。
- 文件夹名称始终显示；帖子展开后，图片和文本附件在该帖子文件夹下方并排显示，图片不显示文件名，点击图片查看大图，点击 TXT/Markdown 查看完整正文。
- 选择框只保留在帖子文件夹上，图片和文本附件不再出现选择框；素材根目录继续自动扫描，无需“读取”按钮。
- 中间区彻底移除“一套/一批”模式表达，只显示当前模板规则、模型选择和已选帖子数量；实际处理对象完全由左侧帖子文件夹勾选决定。
- 生图结果统一标准化为 1200×1600 的精确 3:4 PNG，并记录模型原始尺寸；校准提示词禁止虚构页码或总页数。
- ByteCat 真实复测遇到上游连接超时；系统已完成三次自动重试，并把最终失败改成可读的 502 提示，保留当前素材与模板选择供直接重试。
- 本轮仍为本地开发热更新，不构建便携包；桌面图标继续启动源码版。

## 0.10.2 - 2026-07-28

- 素材与模板改为文件夹内联展开：点击条目后，缩略图直接在其下方横向排列，不显示图片名称；点击缩略图弹出大图。
- 素材根目录启动后自动扫描，上次使用的分类自动读取；切换分类或素材根目录时直接扫描，不再要求额外点击“读取”。
- 中间生产区移除模板图片预览和“做一套/批量做”提示，只保留母版规则、本批要求、页数、质量与模型控制。
- 主生产区新增生图平台、生图模型、文案模型三项独立选择；平台切换后读取该平台实际模型列表，图片与文案可以使用不同模型。
- 生图接口对 408、429、500、502、503、504 和短时网络故障自动重试，失败信息改为可读提示。
- 本轮只更新本地开发源码与桌面源码入口，不构建便携包。

## 0.10.1 - 2026-07-28

- ByteCat 密钥切换到 `image` 生图分组，默认模型改为 `gpt-image-2`。
- 完成真实参考图编辑校准：返回 1024×1536 PNG，并写入 `API生产待审` 和生成记录。
- 修复 ByteCat 图片分组无法调用文本模型的问题：图片继续走 ByteCat，标题与小红书文案自动走已验证的本地 OpenAI 兼容文本接口。
- 保持密钥只存于本机运行数据，不进入源码、日志、诊断信息或发布包。

## 0.10.0 - 2026-07-28

- 素材生产页改为三栏直线工作流：左侧选择素材和模板，中间对话生产，右侧查看成品并打包待发。
- 素材卡片显示图片数、文案、母标签与使用次数，支持多选、当前筛选全选和常用标签筛选。
- 模板增加“转化模板 / 游戏模板”一级分类、具体模板二级选择、封面/内页缩略预览与用途说明。
- 每个模板保存独立生产规则和对话历史；本次口述要求会自动合并进真实生产计划。
- 成品区同时读取 API 待审区和正式成品库，支持待审、待打包、已打包筛选和多选。
- 新增真实待发打包接口：复制完整作品文件夹到 `抖音小红书`，同名不覆盖并写入操作记录。
- 新增本地游戏模板资产识别；保留既有生产计划、逐页生成、文案生成、文件夹分发和防重复能力。
- 新增 ByteCat Image 2.0 配置入口和独立本机密钥存储；当前密钥分组尚未返回图片模型，因此只标记为“已配置、待校准”，不把充值或接口连通误报为生图可用。
- 自动化测试 63 项通过；最终便携版桌面烟测通过三栏生产、素材分类切换、游戏/转化模板、成品打包入口、作品集、分发、插件市场、帮助和两套主题。

## 0.9.9 - 2026-07-28

- 整体视觉收敛为“拟态 / 玻璃”两套主题，复用江湖知识库的蓝灰、低对比、柔和阴影语言；移除侧边栏黑色选中态。
- 侧边栏新增插件市场，集中展示 ChatGPT 作品助手、团建 GPT 数字作品生产助手、团建转化助手、素材下载与本地脚本，并提供本地启动、安装包、发布页和开源仓库入口。
- 所有主界面帮助入口由问号改为“说明”，内容补齐开发背景、方法论、真实数据来源、操作步骤与风险边界。
- 设置页新增坚果云备份：可安全沿用人生游戏系统的 WebDAV 配置，凭据使用 Windows 当前账户加密保存；备份设置、提示词、任务索引、设备备注、分发记录和防重复账本，不上传素材与成品大文件。
- 真实桌面冒烟覆盖 5 个主导航、插件筛选搜索、两套主题、详细帮助弹窗、目录分发与在线设备；本轮检测到 2 台在线设备。
- 本机人生游戏系统的坚果云配置已导入，但真实服务当前返回 HTTP 401；界面明确显示失败，不伪装成备份成功，更新应用密码后可重新导入。

## 0.9.8 - 2026-07-28

- 分发页顶部统一为“设备 / 抖音小红书 / 微信公众号 / 已发送 / 操作记录”，后三个库存标签直接对应真实文件夹。
- 抖音小红书和微信公众号新增“泛流量帖 / 精准流量帖 / 未分类”分类标签与实时数量。
- 每个作品集可在分发页直接修改分类；系统同步增删文件夹名中的 `[泛]`、`[转]`，同名冲突停止且不覆盖。
- 文件夹分类改名、移动到公众号、压缩归档并删除源目录都会写入本地操作记录；文件夹继续作为最终事实真源。
- 各主界面右上角新增问号帮助入口，悬浮显示简要说明，点击查看当前页面的真实数据来源和操作边界。
- 自动化测试增至 62 项，桌面实机烟测覆盖五个分发标签、三类作品标签、帮助入口、设备选择与核心 API。

## 0.9.7 - 2026-07-28

- 分发阶段改为直接读取真实目录：`抖音小红书`、`微信公众号`、`已发送`；资源管理器和工作台刷新后双向一致。
- 新增按历史记录整理：未发送作品进入抖音小红书，手机已接收作品进入微信公众号，已有有效已发送压缩包的残留原文件夹会安全清理。
- 三端发布完成后的最终态改为“已发送压缩包”：先生成并校验 ZIP，再删除原作品文件夹；压缩失败或同名冲突时保留原文件夹。
- 旧发布空间继续作为手机传送兼容入口，整理或流转后自动修复 Junction，现有设备传送逻辑不丢失。
- 界面同步显示三条真实路径、阶段数量与归档动作；空作品文件夹不允许误归档。
- 新增真实目录双向同步、历史迁移、压缩后清理、冲突保护等自动化测试。

## 0.9.5 - 2026-07-28

- 素材生产页新增同屏本地 API 生产区：直接设置生图平台、接口、模型、本机密钥、1/2/6 张数量、质量档和本批提示词，不再把生图入口藏在设置页。
- 接入本地 OpenAI 兼容图像网关与 MiniMax 双平台；本地 `gpt-image-2` 已完成真实模型发现、参考图上传和出图验证，MiniMax 当前密钥校验失败时会明确提示，不伪装成可用。
- 固化 A 类母版、B 类素材、C 类历史结果身份隔离，以及事实锁、江浙沪、10 人起接、无价格不写价格、独立 3:4、先计划确认再校准等规则。
- 校准图只写入运行数据的 `API生产待审`，不进入正式成品库、不增加完成数量；封面和典型内页分开生成与检查。
- 本机密钥迁移到运行数据的受限 secrets 文件，接口不会向前端回传密钥明文，仓库、日志和发布包均不包含密钥。
- 真实“径山素材 × 备忘录母版”端到端校准通过，返回 1086×1448 独立图片；事实锁修正了首次试验中擅自增加露营场景的问题。

## 0.9.4 - 2026-07-27

- 移除桌面应用右侧 ChatGPT 内嵌浏览器与多账号登录壳，工作台恢复为单一完整界面；生产协作统一打开系统受信任的 Chrome/Edge 并交给独立扩展。
- 素材生产页新增“选模板 → 选素材 → 生图迁移 → 作品与文案”四步 API 流程条，并提供不落盘密钥的接口/模型设置入口；实际生图执行仍待用户确定服务后接入。
- 设备发现改为合并本轮结果与 10 分钟内最近发现，并把最近状态持久化到运行数据，降低移动端后台节流导致的在线设备闪退。
- 设备卡片和选择设备窗口新增 Wi‑Fi、USB、远程连接方式图标与状态标签。
- 局域网发现结果明确标记为 Wi‑Fi；USB 备用能力、USB 实际连接和远程待接入不再混成笼统在线状态。
- 传送任务从底层完成结果读取并显示实际传输协议。
- 继续以物理阶段文件夹、独占 claim 和包含传输协议/接收确认的本地 CSV 账本三层防重；已有手机发送记录的作品集不会重新进入待发送库存。

## 0.9.3 - 2026-07-27

- 作品集页面收敛为“抖音、小红书 / 公众号 / 已使用”三个顶部阶段标签，不再要求理解平台状态筛选。
- 手机确认接收成功后，原始作品文件夹自动移动到公众号目录；公众号人工标记后真实移动到已使用目录。
- 三阶段直接读取磁盘目录：在工作台操作会改变文件夹，在资源管理器中移动文件夹后工作台刷新也会同步变化。
- 没有 `[泛]/[转]` 标签的新作品仍保留在第一阶段，并以“待补分类”禁用发送，不再形成界面外的隐藏状态。
- 每张作品卡只保留打开作品和当前阶段主动作，继续保留内容类型、设备选择、传输确认、日志与防重复规则。
- 新增真实目录双向流转、物理目录识别和同名冲突保护测试，自动化测试增至 47 项。

## 0.9.2 - 2026-07-27

- 新增后台全库素材索引，逐分类扫描并持久化紧凑标签、次数、哈希和全局统计，不恢复启动时同步全库扫描。
- 自动读取 `素材链接记录.csv` 与 `制作日志.csv`，按生产事件去重后回填素材使用次数。
- 历史路径失效时只接受唯一文件夹名称匹配；多候选和未匹配记录进入待核对，不自动猜测。
- 历史回填幂等，同一证据重复刷新不会重复加次数；扩展后续真实发送继续在同一计数上累加。
- 元数据修改会同步更新持久化全库索引，真实文件夹移动后自动后台重建索引。

## 0.9.1 - 2026-07-27

- 素材读取改为真正的分类懒加载：首页只加载当前分类，扩展初始接口只返回顶层分类；展开其他分类时才递归扫描。
- 本机实测首页数据由约 21 MB / 42 秒降到约 0.37 MB / 1.1 秒，扩展分类索引约 3.7 KB / 22 毫秒。
- 设置页改为单页紧凑分组，主题从 6 个重复选项精简为 3 个明确模式。
- 提示词入口继续由浏览器扩展独立挂载，不依赖已停用的左侧会话树。

## 0.9.0 - 2026-07-27

- 为每个有效素材文件夹生成独有且可随同卷移动保持稳定的 SHA-256 身份；两份内容相同的文件夹仍是两个身份。
- 新增素材元数据台账，按文件夹哈希保存唯一母标签和使用次数，改名或移动后仍可恢复。
- 母标签限定为“团建游戏 / 团建转化 / 合集攻略”；聚会游戏、破冰游戏等同义关键词统一归入“团建游戏”。
- `/api/materials` 向扩展返回文件夹哈希、母标签、标签来源和使用次数；新增 `/api/extension/material-metadata` 供扩展人工改标签或增加次数。
- 文件夹身份与内容查重分层：唯一文件夹哈希绑定标签，图片与文案聚合指纹继续只负责内容防重。

## 0.8.9 - 2026-07-27

- 为独立 GPT 生产扩展新增受控文件夹移动接口：只允许在同一授权素材库或成品库内部移动真实文件夹。
- 拒绝移动库根目录、普通文件、软链接、跨库目标、自身子目录和会覆盖同名项的操作。
- 移动成功后清理素材扫描缓存，扩展刷新即可看到真实新位置。
- 新增真实临时目录移动及安全拒绝回归测试；Node 测试增至 40 项。

## 0.8.8 - 2026-07-26

- 素材使用台账增加内容指纹，防止素材改名或移动后被重复生产。
- 指纹按图片和文案内容计算，不依赖文件名。
- 仅在选中上传时计算，避免后台全库哈希造成卡顿。

## 0.8.7 - 2026-07-26

- 增加素材使用台账：区分已加入 GPT 与已真正发送。
- 台账只记录路径、状态、会话与时间，不移动或改名原素材。
- 素材接口返回使用状态，供拓展阻止重复上传。

## 0.8.6 - 2026-07-26

- 增加拓展专用完整成品目录树接口，不再用手机分发筛选结果代替本地文件浏览。
- 增加拓展工作包接口，由工作台直接执行现有 PowerShell 脚本，默认不再唤起 VBS。
- 增加目录越界保护、完整目录可见性和直接工作包链路测试。

## 0.8.5 - 2026-07-26

- 修复素材库顶层分类被重命名或删除后仍复用旧递归缓存的问题；扩展不再显示已经不存在的帖子路径。
- 素材缓存增加顶层目录签名校验，分类变化会自动失效并重新扫描；新增分类重命名回归测试。
- 完成真实 Edge + ChatGPT 扩展联动验收：5 个本地附件进入真实会话、生成 5 张图片、真实下载 5/5。
- 下载事件写入本地防重复台账，包含落盘路径、来源会话、文件大小与完成时间。

## 0.8.4 - 2026-07-26

- 修复真实 ChatGPT 页面访问本机目录接口时被浏览器私有网络访问策略拦截、扩展显示 `Failed to fetch` 的问题。
- 本机扩展协作接口现在为允许的 ChatGPT 与浏览器扩展来源返回私有网络预检许可；其他来源仍然拒绝跨域读取。

## 0.8.3 - 2026-07-25

- 工作台与“团建 GPT 数字作品生产助手”拆成两个独立项目；工作台继续承载本地数据和执行接口，扩展专门服务“生产数字作品”SOP。
- 新增扩展协作接口：读取当前素材/成品路径、作品集与作品、素材分类与帖子文件，并允许保存用户粘贴的路径设置。
- 生产防重复改为只读汇总既有作品历史库：当前 193 组图片均具备整组 SHA-256 与 64 位 dHash；精确哈希用于确定重复，视觉哈希只做近似预警。
- 分发防重复仍由手机组、公众号组、claim、使用日志和入口状态独立管理，不再把旧文本哈希误称为当前生产历史。
- 设置页新增生产历史覆盖状态与独立扩展项目入口；默认主题保持用户指定的“拟态悬浮”。
- 扩展协作接口只向真实 ChatGPT 域名和已安装浏览器扩展开放，不给任意网页开放整个中控 API。
- 修正发布级实机测试在“泛流量包”筛选下误查设备行的问题；34 项 Node 测试和完整桌面交互回归均通过。

## 0.8.2 - 2026-07-25

- 修复便携桌面版主工作区只占顶部、下方大面积白屏的问题，素材树和真实 ChatGPT 网页现在始终铺满窗口。
- 设置新增当前版本、检查版本、打开发布包目录、打开运行数据和复制诊断信息；便携版会打开真实 EXE 所在目录。
- 增加应用单实例保护，连续双击只保留一个窗口，并唤醒已有窗口。
- 修复点号开头但合法的 `[转]` 作品集被漏算，工作台库存与原分发 Skill 统一为 2 个团建转化包。
- 普通文件发送终态统一为 100%、`completed` 和“发送完成并确认接收”。
- 素材库加入运行时完整缓存，日常主数据读取由约 18–21 秒降至约 1.2–1.4 秒，手动“刷新文件树”仍会执行完整递归扫描。
- 将现有 ChatGPT 用户脚本及工作包 PowerShell/VBS 同步为应用内平行副本；个人会话历史只导入本机运行数据，不进入公开仓库或发布包。
- 新增真实桌面逐项验收：11/11 个素材分类、21/21 个作品集、图片/文本预览、三套分发子页、设备联动、应用内弹窗和六套主题全部通过。
- 通过应用接口向在线 1 号设备实际发送 165 字节验收文件，接收端提交成功。

## 0.8.1 - 2026-07-25

- 正式提供 Electron 便携桌面版；应用可自行启动本地服务，不依赖浏览器调试窗口。
- 素材生产右侧使用真实 ChatGPT 网页，支持持久登录、Google 授权弹窗和 3 个隔离账号空间。
- 生成桌面快捷方式“团建内容工作台”，浏览器版继续只作为调试与兼容入口。
- 分发设备行按钮统一为单行紧凑文字，修复“补泛流量”等按钮中文被拆行溢出。
- 作品集折叠箭头改为嵌入整行背景；列表/网格改为筛选面板右下角的单一低调图标。

## 0.8.0 - 2026-07-25

- 删除暂时不用的“发布复盘”导航、总览入口与整页，旧标签会安全回到素材生产。
- 作品集改为直接读取可粘贴、可选择的本地目录，并按文件夹名 `[泛]`、`[转]`、无标签分类。
- 删除小红书单平台筛选、全部已使用、搜索框及下方重复台账详情，只保留当前真正需要的筛选和作品树。
- 作品集增加紧凑列表/网格视图、所有合集独立展开、3:4 缩略图和图片 + TXT/Markdown 双预览。
- “拟态悬浮”升级为新安装和本次默认迁移主题。
- 桌面素材生产将真实 ChatGPT 网页继续放在右侧，新增本地素材文件与生产指令投递桥接；三个账号空间仍相互隔离。
- 兼容读取现有作品集目录和旧工作包历史，不修改原 VBS、PowerShell、油猴脚本或个人历史导出。
- 自动化测试增至 31 项。

## 0.7.0 - 2026-07-25

- 作品包分发由同步等待升级为可恢复任务：显示核对、设备响应、发送百分比、接收确认、写入记录和完成状态。
- 分发任务和普通文件任务统一显示在分发页，支持安全停止；刷新页面后可恢复当前及最近任务。
- 作品包选择设备后改用系统内二次确认，明确显示作品包、目标设备、内容类型和手机组使用规则。
- 删除前台关于软链接、Junction 等内部实现的提示；兼容层继续并行工作，但不干扰日常操作。
- 全站原生确认框、警告框和重命名输入框替换为统一的系统弹层，六套主题下保持一致视觉与可读状态。
- 接口错误统一转换为可读提示，不再把原始 JSON 或脚本输出直接弹给用户。
- 新增传输阶段解析测试，自动化测试增至 30 项。

## 0.6.0 - 2026-07-25

- 分发统计统一按当前真实可分发列表计算，统计卡与下方作品包数量保持一致。
- 设备状态改为后台低频自动刷新，始终显示“当前设备在线 N/总数”；刷新时只显示小型旋转图标。
- 在线设备置顶，离线设备明确禁用补笔记与上传；设备名称可直接编辑本机备注。
- 泛流量和团建转化卡可直接切换真实作品包列表；每个作品包独立选择当前在线设备，发送前保留最终确认。
- 设备选择弹窗统一使用 Android / Apple 图标、设备名称和“当前在线”状态，删除平台与作品数等重复信息。
- 新增“上传其他”：可选择文件或文件夹、拖放到在线设备、查看实时进度并取消指定传输任务。
- 指定作品包分发继续复用 `device-folder-transfer` 的真实传输与接收确认；成功后只删除已校验的活动平台 Junction，不再创建抖音归档入口。
- 增加三层防重复：发送前独占占用记录、接收成功 CSV 日志、活动 Junction 删除；已有成功记录的作品即使入口被误建回来也拒绝再发。
- 手机库存改为组级互斥：只有小红书与抖音两个同源入口同时有效才在前端显示和参与底层抽取；任一手机平台使用后整组退出，公众号资格独立保留。
- 公众号页改为“公众号补笔记”，加入“打开公众号官网”，状态统一为“公众号可用 / 已打开过 / 上传已完成”。
- 素材文件树改为紧凑列表，删除文案摘要，增加列表/小图标视图切换；“传 GPT”改为圆润紧凑按钮。
- 浏览器调试版删除仿制任务卡；桌面版素材生产右侧继续显示可登录、可多账号隔离的真实 ChatGPT 网页。
- 本地服务仅监听 `127.0.0.1`，并拒绝把整个磁盘作为拖放传输源。

## 0.5.0 - 2026-07-24

- 增加桌面双栏应用：素材生产左侧读取本地文件树，右侧直接加载真实 ChatGPT 网页；其他工作流自动隐藏 GPT。
- 增加 3 个隔离的 GPT 登录空间，登录状态保存在本机。
- 设备在线检测改为服务端直接返回数量和在线设备，自动检测与库存扫描拆开；在线检测每 60 秒低频执行，手动刷新仅作兜底。
- 修复 Android 图标缺少头部的问题，并改用 Windows / macOS 系统字体栈。
- 作品集改成可折叠树，可展开 14 个作品并点击预览；数量移动到右侧。
- 作品集标签台账可编辑并导出 CSV，Excel 可直接打开。
- 删除抖音归档入口与归档筛选；抖音旧归档统一按“已使用”展示。
- 公众号流程改成“公众号可用 → 已打开过 → 是否已上传？ → 上传已完成”。
- 主题恢复原项目名称“拟态悬浮”和“克制玻璃”。
- 素材根目录校正为 `01-素材库`，继续递归识别图片与文案齐全的帖子文件夹。

## 0.4.0 - 2026-07-24

- 素材生产顶部新增“项目目录设置”：支持粘贴本地路径后回车、系统文件夹选择器和递归扫描。
- 帖子识别收紧为“同一文件夹内同时存在图片和 TXT/Markdown 文案”；只有有效帖子显示“传 GPT”。
- 设置页直接读取并维护现有 `D:\Download\workpkg_config.json`，支持作品集目录、14 条成组与自动压缩设置；保存前保留 `.bak`，VBS/PowerShell 继续并行可用。
- 分发页接入 `send_to_device.py --status` 的真实在线明细；在线设备置顶并显示当前作品数，离线设备明确标记并禁用补笔记按钮。
- 设备统计补齐“台 / 个”单位，设备卡使用 Android / Apple 图形标识。
- 作品集新增并行 JSON 台账，可编辑人工分类、标签和备注；不删除旧 Junction 执行链。
- 删除不可实现的“发布调度”占位入口。
- 主题从 3 套扩展为 6 套：新增来自江湖转化助手规范的“江湖悬浮”，以及“纸墨编辑部”“深海夜航”。
- 自动化测试增至 26 项，并完成 331 个真实帖子、设备在线联动和 6 套主题的浏览器验收。

## 0.3.0 - 2026-07-24

- 删除没有实际价值的“总览”，旧状态自动迁移，软件启动后直接进入“素材生产”。
- 素材生产升级为双栏工作台：左侧真实读取本地文件夹树，右侧负责当前帖子、生产指令和真实 ChatGPT 联动。
- 兼容根目录直接存放帖子、分类目录下存放帖子两种素材结构；当前可读取 331 个本地帖子文件夹。
- 每个帖子右侧增加“传 GPT”，自动准备帖子路径与母版指令，并打开已登录的真实 ChatGPT 浏览器。
- 直接复用 `cgpt-workpkg://run`、`cgpt-workpkg://configure` 和已有网页脚本，不复制原脚本源码。
- 左侧增加“发布调度”预留入口；当前只展示开发边界，不伪造设备、账号或平台发布状态。
- 外部启动入口增加白名单，仅允许 ChatGPT HTTPS 地址与现有工作包协议。
- 自动测试增加到 23 项，并完成素材树展开、搜索、页面切换和浏览器错误回归。

## 0.2.1 - 2026-07-24

- 删除作品集页重复的顶部统计卡，把实时数量合并到内容类型和平台状态筛选按钮右侧。
- 筛选数量会随另一组筛选和搜索词联动，不显示固定的过期总数。
- 将分发页“手机补货”改为“手机补笔记”，并统一“已登记设备”“泛流量合集”“团建转化（精准流量）”等用户用语。
- 删除设备扫描后重复展示的长提醒块，保留统计卡、设备列表和扫描刷新能力。
- 自动测试增至 19 项，并完成浏览器交互回归。

## 0.2.0 - 2026-07-24

- 将顶部标签导航升级为左侧工作流栏：总览、素材生产、作品集、分发功能、发布复盘和设置。
- 新增作品集实时状态扫描，按 `[泛]`、`[转]`、平台资格、公众号领取和归档状态筛选。
- 新增作品集详情，显示条数、文件数、容量、三平台状态和使用记录。
- 新增分发功能四页签，接入 8 台登记设备、真实库存、设备扫描和现有安全分发 Skill。
- 新增公众号“已领取待上传 → 已确认上传”的人工确认日志。
- 新增雾绿实体、轻透玻璃和柔和拟态三种可持久化主题。
- 新增作品集状态、界面筛选和分发参数白名单测试。
- 识别并显示当前 2 个抖音归档 Junction 的源目录异常，不把它们冒充为可恢复库存。

## 0.1.0 - 2026-07-22

- 从江湖有旅人业务项目中提取独立软件源码。
- 将项目资产、软件源码、运行状态和共享 Skill 分层。
- 增加稳定启动入口、桌面入口和路径覆盖配置。
# 0.9.6 - 2026-07-28

- 把“校准出图”的开发者界面改为普通用户能直接理解的“做一张 / 做一套 / 批量做”三个真实入口。
- 从既有生产聊天中提炼并结构化米字格、绿底键帽、森林路线节点和备忘录四类模板配方；模板管视觉、素材管事实、历史失败结果不得继承。
- 新增出图计划确认：系统按有效素材自动拆页，明确每页标题、封面/内页角色、总图片数和文案数；计划确认后才开始扣费生产。
- 新增后台生产任务与进度查询；“做一套”逐页使用不同内容与提示词生成独立 3:4 图片，并写入小红书文案、出图计划和生产记录。
- “批量做”从当前素材开始连续处理 2/3/5 套，同一模板复用但每套素材事实相互隔离。
- 接口地址、模型和密钥移入默认收起的高级区；主流程只保留用户真正需要的生产方式、页数、质量和额外要求。
- 新增模板配方、页数规则、标题清洗和本地文案接口测试；自动化测试增至 56 项。
## 0.11.6 - 2026-07-29

- 内容分发作品集选中后整张卡片上浮；名称区域不再套第二层小模块，移除“可发送到手机”冗余徽标，分类与操作控件统一高度。
- 已完成、失败和取消的任务支持直接清除，并在当前操作区保留 3 分钟后自动收起；完整审计历史继续保存在“操作记录”。
- 移除顶部“按历史记录整理”和底部 Codex 命令条，新增全局右下角“团建中控助手”。助手可检测设备、打开页面设置，并按设备白名单与防重复规则执行指定作品或指定分类分发。
- 设置页新增每天/每周/按小时的自动备份计划、方案目录选择和每月大文件流量上限。方案文件使用增量清单上传，达到月额度后留待下月继续。
- 修复流量转化嵌入脚本把 `/conversion-integrated` 重复添加两次、造成内部接口 404 的问题，并加入回归测试。
- 内容制作右侧成品区升级为与素材区一致的文件夹展开结构，图片和 TXT 可直接预览；未打包、已打包和打包记录分别读取真实本地目录。
- README 重写为面向实际使用者的完整说明，补充开发背景、最快使用方法、文件流转、备份、技巧、安全和故障排查。
